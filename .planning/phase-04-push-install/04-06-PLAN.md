---
phase: 04-push-install
plan: 06
type: execute
wave: 4
depends_on: [04-01, 04-02, 04-03, 04-04, 04-05]
files_modified:
  - tests/e2e/test_push_install_e2e.py
  - tests/unit/test_policy_backward_compat_v01_agent.py
  - tests/integration/test_audit_event_roundtrip.py
autonomous: true
requirements: [PUSH-01, PUSH-02, PUSH-03, PUSH-04]
must_haves:
  truths:
    - "End-to-end: admin publishes a policy with 1 skill + 1 agent + 1 MCP entry + 1 managed CLAUDE.md block → agent sync applies all four → /audit shows a success event with applied_count=4"
    - "End-to-end rollback: when one of the four writes fails, the snapshot is restored and /audit shows a rollback event with reason and failed_file populated"
    - "Backward-compat: a simulated v0.1 agent (parsing the policy with the v0.1 schema that lacks the 4 new sections) successfully parses the extended policy and ignores the new fields without raising"
    - "Audit roundtrip: a posted PolicyApplyEvent appears on GET /audit?event_source=policy_apply within the same request cycle"
    - "Phase 4 brings ≥25 new tests; total ≥570 tests, all green"
  artifacts:
    - path: "tests/e2e/test_push_install_e2e.py"
      provides: "full apply-cycle e2e with snapshot restore"
      contains: "policy_apply"
    - path: "tests/unit/test_policy_backward_compat_v01_agent.py"
      provides: "simulated v0.1 agent reads extended policy and survives"
      contains: "extra"
    - path: "tests/integration/test_audit_event_roundtrip.py"
      provides: "POST /api/v1/audit → GET /audit roundtrip"
      contains: "policy_apply"
  key_links:
    - from: "tests/e2e/test_push_install_e2e.py"
      to: "publish flow + agent sync + audit endpoint"
      via: "TestClient + temp $HOME + push_install.apply"
      pattern: "publish.*sync.*audit"
---

<objective>
Lock Phase 4 with cross-cutting verification: a full e2e cycle from admin
publish → agent sync → apply → audit event → audit page rendering; a
backward-compat test proving v0.1 agents survive the extended schema (D-1);
and an audit endpoint→page roundtrip test.

Purpose: Plans 01–05 each cover their own slice with focused tests. This
plan ensures the slices compose correctly under realistic conditions and
that no plan introduced a silent regression. Brings the new-test count for
Phase 4 to ≥25 and locks the 545+ baseline.

Locked decisions reaffirmed:
- D-1 schema_version=1 + Pydantic extra='ignore' — proven by simulated v0.1 agent
- D-2 snapshot scope strictly targeted — restore verified per file
- D-3 orphan deletion SKIPPED — e2e does NOT assert deletion of stale files
- D-4 CLAUDE.md home-only — e2e only touches ~/CLAUDE.md
- D-5 skill content full file — e2e verifies frontmatter+body land verbatim
- D-7 _managed_by identification — MCP merge e2e asserts the field is the
  sole discriminator
</objective>

<execution_context>
@/Users/timderbak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/timderbak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phase-04-push-install/04-CONTEXT.md
@.planning/phase-04-push-install/04-01-SUMMARY.md
@.planning/phase-04-push-install/04-02-SUMMARY.md
@.planning/phase-04-push-install/04-03-SUMMARY.md
@.planning/phase-04-push-install/04-04-SUMMARY.md
@.planning/phase-04-push-install/04-05-SUMMARY.md
@src/ccguard/schemas/policy.py
@src/ccguard/agent/push_install.py
@src/ccguard/agent/sync.py
@src/ccguard/server/api/audit.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: End-to-end push-install cycle test (publish → apply → audit success and rollback)</name>
  <files>tests/e2e/test_push_install_e2e.py</files>
  <behavior>
    HAPPY PATH:
    - Use the existing TestClient + temp DB fixture from prior phases
    - Author a draft policy via POST /policy/draft containing: 1 required_skill (name=sec, content="---\nname: sec\n---\nbody"), 1 required_agent (name=rev, content=full agent .md), 1 required_mcp_server (name=stripe, command="/usr/bin/x", args=["-y"], env={}), 1 managed_claude_md_blocks (id=security-rules, content="X")
    - POST /policy/publish — revision bumps
    - In a tmp_path-based fake $HOME (monkeypatch Path.home()), invoke the sync command (or call sync.py's apply hook directly) against the TestClient base URL
    - Assert files on disk: ~/.claude/skills/sec/SKILL.md (full frontmatter+body bytes per D-5), ~/.claude/agents/rev.md, ~/.claude.json contains mcpServers["stripe"] with _managed_by=ccguard (D-7), ~/CLAUDE.md contains the managed block between markers (D-3, D-4)
    - Assert a PolicyApplyEvent row exists with result=success, applied_count=4, policy_revision=N
    - GET /audit?event_source=policy_apply renders the success pill with locked classes

    ROLLBACK PATH:
    - Repeat the publish step with the same content
    - Pre-create ~/.claude/agents as a file (not a directory) so the agent .md write triggers a PermissionError / IsADirectoryError
    - Invoke sync → assert push_install.apply returned rollback; snapshot was restored byte-for-byte (verify the pre-apply state of ~/.claude.json and ~/CLAUDE.md is unchanged after rollback)
    - Assert a PolicyApplyEvent row exists with result=rollback, reason set, failed_file set
    - GET /audit?event_source=policy_apply renders the rollback pill with reason highlighted in text-amber-600
    - Assert sync CLI exit code is 0 (best-effort, never raises)

    IDEMPOTENCY:
    - Run sync TWICE against the same policy in a clean tmp $HOME
    - Assert both invocations succeed; assert there are exactly 2 PolicyApplyEvent rows (one per sync); assert files on disk are byte-equal after both runs
  </behavior>
  <action>
    Compose the test using fixtures already established in Phase 1+3 tests (TestClient, isolated DB, machine-id mock). Use `monkeypatch.setattr(Path, "home", lambda: tmp_path)` to redirect agent writes. For the rollback induction, `os.chmod(tmp_path / ".claude" / "agents", 0)` after creating it OR write the agents path as a file. Drive the apply through `src/ccguard/agent/sync.py::_apply_and_report` directly to keep the test independent of click/argparse plumbing. Assertions on the locked Tailwind classes (`bg-emerald-600`, `bg-red-600`, `text-amber-600`) come from rendering GET /audit and grepping the response body. NO new dependencies. NO sleeps — use direct calls. Target ≥10 assertions across happy + rollback + idempotency.
  </action>
  <verify>
    <automated>uv run pytest tests/e2e/test_push_install_e2e.py -x -q</automated>
  </verify>
  <done>
    E2E happy path + rollback + idempotency all green; PolicyApplyEvent rows exist with correct shape; /audit page renders the correct pills.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backward-compat for v0.1 agents + audit endpoint roundtrip</name>
  <files>tests/unit/test_policy_backward_compat_v01_agent.py, tests/integration/test_audit_event_roundtrip.py</files>
  <behavior>
    BACKWARD-COMPAT (test_policy_backward_compat_v01_agent.py):
    - Define an inline "v0.1 Policy" Pydantic model in the test (subset of fields — no required_*, no managed_claude_md_blocks); set model_config = ConfigDict(extra="ignore")
    - Construct an extended policy dict containing all 4 new sections + arbitrary `future_unknown_field`
    - Call `V01Policy.model_validate(extended_policy_dict)` — assert no exception, assert the resulting object lacks the new fields (they were ignored)
    - Variant: same dict but with `schema_version: 1` (D-1) — also passes
    - Variant: same dict with a hypothetical `schema_version: 999` — also passes because v0.1 was tolerant
    - Negative: a malformed required field type (e.g. required_mcp_servers=42 instead of list) — v0.1 still ignores; v0.2 Policy.model_validate REJECTS the same payload (proves the new validation is strictly additive in the v0.2 model)

    ROUNDTRIP (test_audit_event_roundtrip.py):
    - POST /api/v1/audit with event_source=policy_apply payload (one success + one rollback in a batch of two)
    - GET /audit?event_source=policy_apply — assert both rows render; success row shows green pill + applied={n} details; rollback row shows red pill + reason= details
    - Assert ordering is ts desc (newest first)
    - Assert machine filter combined with policy_apply works (only events for the seeded machine_id appear)
    - Assert timeframe filter works (limit to last 1h includes both, last 0s excludes both)
  </behavior>
  <action>
    Write `test_policy_backward_compat_v01_agent.py` defining a minimal V01Policy class inline (do not import any production v0.1 surface — the point is to simulate an old agent's parser). Use Pydantic v2 BaseModel + ConfigDict(extra="ignore"). Test all variants listed in Behavior. Write `test_audit_event_roundtrip.py` using the existing TestClient fixture; POST a batch of two policy_apply events via the audit endpoint; GET /audit?event_source=policy_apply with various filter combinations; parse the HTML response (Phase 1+3 tests already use BeautifulSoup or simple string checks — match the existing style). NO new test infrastructure. Aim for 4–6 assertions per file.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_policy_backward_compat_v01_agent.py tests/integration/test_audit_event_roundtrip.py -x -q</automated>
  </verify>
  <done>
    Backward-compat proven for v0.1 agents (D-1); audit endpoint roundtrip works with combined filters; ordering and timeframe behaviors correct.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/e2e/test_push_install_e2e.py tests/unit/test_policy_backward_compat_v01_agent.py tests/integration/test_audit_event_roundtrip.py -x -q` exits 0
- `uv run pytest -q` exits 0 with at least 570 tests reported (545 baseline + ≥25 new across plans 01–06)
- `uv run pytest tests/ -q --collect-only 2>/dev/null | grep -c "test_" ` ≥ 570
</verification>

<success_criteria>
- E2E publish → apply → audit cycle works end-to-end
- Rollback path proven: snapshot restored, audit recorded, CLI exit 0
- Idempotent re-runs leave files byte-equal
- Backward-compat for v0.1 agents proven via simulated parser
- Audit page roundtrip with filter combinations verified
- ≥25 new tests across the phase; 545 baseline stays green
- Zero new external dependencies introduced anywhere in Phase 4
</success_criteria>

<output>
Create `.planning/phases/04-push-install/04-06-SUMMARY.md` documenting:
- Final test counts (baseline + phase 4 additions)
- Confirmation that all 4 PUSH requirements (PUSH-01..04) are covered
- Confirmation that schema_version remains 1 (no migration introduced)
</output>
