---
phase: 04-push-install
plan: 05
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - src/ccguard/server/web/routes.py
  - src/ccguard/server/web/templates/audit_feed.html
  - tests/integration/test_audit_page_policy_apply_filter.py
autonomous: true
requirements: [PUSH-04]
must_haves:
  truths:
    - "GET /audit?event_source=policy_apply renders only PolicyApplyEvent rows; the filter option «События политики» appears in the existing event_source select"
    - "When event_source=policy_apply is active, a new rightmost column «Результат» renders a success or rollback pill per row using the locked Tailwind classes from 04-UI-SPEC.md"
    - "The «Подробности» cell renders the locked format strings: `applied={n}, snapshot={snapshot_id[:8]}` for success; `reason={reason}, failed_file={failed_file}, snapshot={snapshot_id[:8]}` for rollback with `reason=` highlighted in text-amber-600"
    - "When event_source != policy_apply, the «Результат» column is OMITTED entirely — existing tool_use audit page layout is unchanged byte-for-byte"
    - "Empty results show locked copy «Событий нет.» with the existing empty-row pattern"
  artifacts:
    - path: "src/ccguard/server/web/templates/audit_feed.html"
      provides: "added <option value='policy_apply'>События политики</option>; conditional Результат column; details rendering for policy_apply"
      contains: "policy_apply"
    - path: "src/ccguard/server/web/routes.py"
      provides: "GET /audit handler reads event_source query, branches to PolicyApplyEvent query"
      contains: "policy_apply"
  key_links:
    - from: "src/ccguard/server/web/routes.py::audit_feed"
      to: "src/ccguard/server/db/models.py::PolicyApplyEvent"
      via: "SQLModel select with ORDER BY ts DESC using ix_policy_apply_result_ts index"
      pattern: "PolicyApplyEvent"
    - from: "templates/audit_feed.html"
      to: "PolicyApplyEvent rows passed in `events` context"
      via: "Jinja loop with event_source conditional branch"
      pattern: "policy_apply|Результат"
---

<objective>
Extend the existing `/audit` page with the new `event_source=policy_apply`
filter value and a conditional «Результат» column showing success/rollback
pills. Per 04-UI-SPEC.md, the additions are strictly additive: when the
filter is anything other than `policy_apply`, the page renders identically
to v0.1. No new pages, no new routes — pure extension.

Purpose: Closes the UI half of PUSH-04 surface (the audit/observability
side). Together with plan 02 (mandatory editor) this gives the admin full
visibility into policy authoring AND apply outcomes.

Russian UI strings are LOCKED per 04-UI-SPEC.md Copywriting Contract:
- «События политики» (filter option)
- «Результат» (column header)
- `success` / `rollback` (pill text — opaque tokens, never translated)
- `applied=N, snapshot=ABCDEF` / `reason=..., failed_file=..., snapshot=...` (details format)
- «Событий нет.» (empty state)

Output: Extended audit_feed.html, updated GET /audit handler, integration
tests for filter + column rendering.
</objective>

<execution_context>
@/Users/timderbak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/timderbak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phase-04-push-install/04-CONTEXT.md
@.planning/phase-04-push-install/04-UI-SPEC.md
@.planning/phase-04-push-install/04-01-SUMMARY.md
@src/ccguard/server/web/routes.py
@src/ccguard/server/web/templates/audit_feed.html
@src/ccguard/server/db/models.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /audit handler branches on event_source=policy_apply</name>
  <files>src/ccguard/server/web/routes.py, tests/integration/test_audit_page_policy_apply_filter.py</files>
  <behavior>
    - GET /audit (no query) renders the existing tool_use feed exactly as in v0.1 (regression: byte-equal `<thead>` columns and order)
    - GET /audit?event_source=policy_apply queries PolicyApplyEvent ordered by ts desc; passes events into the template with `event_source='policy_apply'` context var
    - The select element renders `<option value="policy_apply">События политики</option>` ONLY in addition to existing options; selection round-trips via the form submit
    - Pagination/timeframe filters (existing) still work; combine with the new branch when event_source=policy_apply (LIMIT, time window)
    - Empty result set passes events=[] into the template
  </behavior>
  <action>
    Locate the existing GET /audit handler in `routes.py`. Add a branch: when `event_source == "policy_apply"`, build a SQLModel select on PolicyApplyEvent applying the same time-window and machine filters used for tool_use events, order by ts desc, limit by the existing page-size constant. Pass into the template with the same `events` list name plus a `result_column_visible: bool` context var (True only for policy_apply). For other event_source values keep the v0.1 code path verbatim. Reuse the composite index `ix_policy_apply_result_ts` from plan 01 (no new index work needed). Integration tests: (1) /audit no-query baseline screenshot/snapshot test — assert response contains the existing 4 columns and NOT «Результат»; (2) /audit?event_source=policy_apply shows the new column header «Результат»; (3) seeded success event renders the green pill; (4) seeded rollback event renders the red pill; (5) empty results show «Событий нет.»; (6) machine+timeframe filter combined with policy_apply works.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_audit_page_policy_apply_filter.py -x -q</automated>
  </verify>
  <done>
    GET /audit?event_source=policy_apply returns the new filtered view; default /audit unchanged; filter combinations honored.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: audit_feed.html — add filter option, conditional «Результат» column, details rendering</name>
  <files>src/ccguard/server/web/templates/audit_feed.html, tests/integration/test_audit_page_policy_apply_filter.py</files>
  <behavior>
    - The `<select name="event_source">` contains the locked option `<option value="policy_apply" {% if event_source == 'policy_apply' %}selected{% endif %}>События политики</option>`
    - Inside `<thead>` a new `<th>Результат</th>` is rendered ONLY when `event_source == 'policy_apply'`; it appears as the RIGHTMOST column
    - Inside the row loop, when `event_source == 'policy_apply'`: render the «Подробности» cell using the locked format and render the «Результат» cell pill
    - Success pill: `<span class="inline-block rounded-full px-2 py-0.5 text-xs font-semibold bg-emerald-600 text-white">success</span>`
    - Rollback pill: `<span class="inline-block rounded-full px-2 py-0.5 text-xs font-semibold bg-red-600 text-white">rollback</span>`
    - Details cell for success: `<span class="font-mono text-xs text-slate-700">applied={{ e.details.applied_count }}, snapshot={{ e.details.snapshot_id[:8] }}</span>` (or rendering directly from the PolicyApplyEvent row fields if the route passes the row directly)
    - Details cell for rollback: same wrapper with `<span class="text-amber-600">reason=</span>{{ reason }}, failed_file={{ failed_file }}, snapshot={{ snapshot_id[:8] }}`
    - Empty state row: `<tr><td colspan="{{ N }}" class="py-6 text-center text-slate-400">Событий нет.</td></tr>` (colspan adjusts based on result_column_visible)
    - When event_source != policy_apply, the template renders byte-equal to v0.1 (no leaked columns, no leaked filter option appearance changes beyond the added <option>)
  </behavior>
  <action>
    Edit `audit_feed.html` per 04-UI-SPEC.md «`/audit` table — extension» section and «Details cell rendering for `policy_apply` events» block. Add the new `<option value="policy_apply">События политики</option>` inside the existing `<select name="event_source">`. Wrap the new `<th>Результат</th>` in `{% if event_source == 'policy_apply' %}...{% endif %}`. In the row loop add a Jinja branch on `e.event_source == 'policy_apply'` rendering the details and pill exactly per UI-SPEC; the pill column is wrapped in the same `{% if %}` so non-policy_apply rows do not get a stray empty cell. Adjust the empty-state row's colspan to `{% if event_source == 'policy_apply' %}5{% else %}4{% endif %}` (or the equivalent dynamic value matching the existing column count). All Russian copy is locked verbatim — do not paraphrase. No new JS, no new HTMX. Tests in same file as Task 1 cover both rendering branches and assert the locked Tailwind classes are present.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_audit_page_policy_apply_filter.py -x -q</automated>
  </verify>
  <done>
    Template renders the new filter option, conditional column, and per-row pills with locked classes and copy; default view byte-equal to v0.1.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/integration/test_audit_page_policy_apply_filter.py -x -q` exits 0
- `uv run pytest -q` exits 0
- `grep -v '^#' src/ccguard/server/web/templates/audit_feed.html | grep -c "События политики"` ≥ 1
- `grep -v '^#' src/ccguard/server/web/templates/audit_feed.html | grep -c "bg-emerald-600"` ≥ 1
- `grep -v '^#' src/ccguard/server/web/templates/audit_feed.html | grep -c "bg-red-600"` ≥ 1
- `grep -v '^#' src/ccguard/server/web/templates/audit_feed.html | grep -c "Событий нет"` ≥ 1
</verification>

<success_criteria>
- Admin can filter /audit by «События политики» and see success/rollback outcomes per machine
- Default /audit view is unchanged from v0.1 (no regression for tool_use users)
- Russian UI copy matches 04-UI-SPEC.md verbatim (locked Copywriting Contract)
- Zero new dependencies; pure server-side Jinja
- ≥6 new integration tests; 545 baseline tests stay green
</success_criteria>

<output>
Create `.planning/phases/04-push-install/04-05-SUMMARY.md` documenting:
- The conditional column pattern (result_column_visible flag)
- Confirmation that v0.1 audit page renders unchanged
</output>
