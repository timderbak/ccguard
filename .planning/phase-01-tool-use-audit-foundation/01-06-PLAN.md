---
phase: 01-tool-use-audit-foundation
plan: 06
type: execute
wave: 5
depends_on: ["01-02", "01-03", "01-04", "01-05"]
files_modified:
  - tests/integration/test_audit_flush_e2e.py
  - tests/integration/test_audit_smoke.py
  - tests/conftest.py
autonomous: true
requirements: [TUA-01, TUA-02, TUA-03]
must_haves:
  truths:
    - "Agent flusher -> server happy path works end-to-end against the ASGI TestClient"
    - "Buffer is drained on success (delete_ids called for accepted batch)"
    - "Buffer retains events on server 5xx; retry/backoff path exercised"
    - "Server 422 (schema mismatch) does NOT drop events from buffer"
    - "/audit page with 1000 seeded events renders correctly: 200 visible rows + footer + populated timeline"
    - "All 185+ existing v0.1 tests still green"
    - "At least 20 NEW tests added across phase (per ROADMAP success criterion 5)"
  artifacts:
    - path: "tests/integration/test_audit_flush_e2e.py"
      provides: "Agent buffer -> POST /api/v1/audit end-to-end test using patched httpx client"
    - path: "tests/integration/test_audit_smoke.py"
      provides: "1000-event seeded /audit page render + filter narrowing"
  key_links:
    - from: "agent flusher._run_flush_loop"
      to: "server POST /api/v1/audit"
      via: "patched httpx client targeting in-process TestClient"
      pattern: "_patch_httpx_to_testclient"
---

<objective>
Validate the full Phase 1 stack end-to-end: agent buffer (PLAN 01) -> flusher (PLAN 02) -> server endpoint (PLAN 03) -> events table + timeline (PLANS 04, 05). No production code changes — only tests + minimal fixture helpers. Confirms TUA-01/02/03 acceptance criteria operationally and meets the ROADMAP "20+ new tests" success criterion.

Purpose: Catch integration regressions that unit tests cannot see (e.g., schema version negotiation, delete_ids called on the agent buffer after a real 200 response, timeline+table coherence on a realistic data shape).
Output: two new integration test files and a small conftest extension; full pytest suite green.
</objective>

<execution_context>
@/Users/timderbak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/timderbak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phase-01-tool-use-audit-foundation/01-CONTEXT.md
@.planning/phase-01-tool-use-audit-foundation/01-RESEARCH.md
@.planning/phase-01-tool-use-audit-foundation/01-01-PLAN.md
@.planning/phase-01-tool-use-audit-foundation/01-02-PLAN.md
@.planning/phase-01-tool-use-audit-foundation/01-03-PLAN.md
@.planning/phase-01-tool-use-audit-foundation/01-04-PLAN.md
@.planning/phase-01-tool-use-audit-foundation/01-05-PLAN.md

# Reuse existing harness
@tests/integration/conftest.py
@tests/integration/test_agent_sync.py
@tests/integration/test_web_smoke.py

<interfaces>
Reused fixtures from tests/integration/conftest.py:
- `client` — FastAPI TestClient with isolated DB
- `agent_token` — valid X-CCGuard-Token bound to a test machine
- `admin_session` (or equivalent cookie-auth fixture)
- `_patch_httpx_to_testclient` (referenced in test_agent_sync.py) — monkeypatches httpx so agent code calls hit the in-process TestClient

Produced by this plan:
- New helper `_seed_tool_use_events(session, *, count, machine_id, tool_name, decision, base_ts)` in tests/conftest.py — bulk-insert helper used by multiple test files.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Conftest seed helper + agent flusher end-to-end test</name>
  <files>
    tests/conftest.py,
    tests/integration/test_audit_flush_e2e.py
  </files>
  <action>
    Add to `tests/conftest.py`:
    - Function `seed_tool_use_events(session, *, count, machine_id="laptop-test", tool_name="Bash", decision="allow", result_status="success", base_ts=None, ts_step_seconds=60) -> list[ToolUseEvent]`. Inserts `count` rows with `ts = base_ts - i*ts_step_seconds` and commits. Returns list of inserted rows. base_ts defaults to `datetime.now(UTC)`.
    - Function `random_fingerprint() -> str` returning 16 random hex chars (use `secrets.token_hex(8)`).

    Create `tests/integration/test_audit_flush_e2e.py`. Reuse `_patch_httpx_to_testclient` pattern from `test_agent_sync.py`. Test cases:

    1. **Happy path drain**: Pre-populate the agent buffer (via `ToolBufferDB`) with 5 events at `tmp_path/audit_buffer.db`. Patch `default_config_dir` to `tmp_path`. Patch httpx to route to the TestClient. Patch agent config to return the test `agent_token` + a fake `machine_id`. Call `flusher._run_flush_loop()`. Assert: server `tooluseevent` table has 5 rows; agent buffer is empty.

    2. **Schema major version mismatch**: Monkeypatch `SCHEMA_VERSION_AUDIT` on the agent side to `"1.0"`. Pre-populate buffer with 3 events. Run `_run_flush_loop`. Assert: server returns 422; events REMAIN in buffer (no delete_ids); server table unchanged.

    3. **Batch size > 200**: Pre-populate buffer with 350 events. Run `_run_flush_loop`. Assert: at least two POSTs occurred (200 + 150); all 350 rows on server; buffer empty.

    4. **Server 5xx retry/backoff**: Inject a counter into the patched httpx so first 2 POSTs return 500, third returns 200. Pre-populate 5 events. Run `_run_flush_loop`. Assert: 3 attempts observed; rows eventually persisted; buffer empty.

    5. **Auth failure (no token)**: Patch agent config to return empty token. Run loop. Assert: 401 returned; events stay in buffer.

    6. **trim_to_cap called after success**: Pre-populate buffer with 10_100 events. Run loop. Assert: buffer row count <= 10_000 at end (drained + trimmed); server has 10_100 rows.

    7. **Privacy boundary regression**: Pre-populate buffer with a row whose `tool_name="Bash"` and `fingerprint=` a known 16-hex value; intercept the POST body; assert the request JSON contains NO key named `tool_input`, `command`, or `file_path` anywhere (recursive scan).

    Use `pytest.mark.integration`.
  </action>
  <verify>
    <automated>cd /Users/timderbak/dev/ccguard &amp;&amp; python -m pytest tests/integration/test_audit_flush_e2e.py -x -q</automated>
  </verify>
  <done>
    All 7 flush-e2e cases green. The seed helper is importable from `tests/conftest.py` and used by Task 2. No raw `tool_input` ever appears in HTTP payload (privacy regression test passes).
  </done>
</task>

<task type="auto">
  <name>Task 2: 1000-event /audit smoke + final regression run</name>
  <files>
    tests/integration/test_audit_smoke.py
  </files>
  <action>
    Create `tests/integration/test_audit_smoke.py` (mirrors `test_web_smoke.py` style). Test cases:

    1. **Bulk seed + page render**: Call `seed_tool_use_events(session, count=1000, base_ts=datetime.now(UTC))` spread over 24h (ts_step_seconds=86). GET `/audit` with admin session. Assert: 200; HTML contains 200 `<tr class="border-b` rows (limit), footer line `Показано 200 из 1000 событий за период.`; timeline has at least 20 non-zero bars (`min-height: 2px`).

    2. **Filter narrowing**: Re-seed with 500 Bash + 500 Edit events. GET `/audit?tool_name=Bash`. Assert: only Bash rows in table; footer reflects `из 500`; timeline bar tooltips reflect Bash-only counts.

    3. **Filter narrowing via machine_id LIKE**: Seed events for `machine_id=laptop-a` (300) and `laptop-b` (300). GET `/audit?machine_id=laptop-a`. Assert: 200 rows, all link to `/machines/laptop-a`.

    4. **Decision color codepath**: Seed mixed allow/deny/error events. GET `/audit`. Assert: HTML contains `text-emerald-600`, `text-red-600`, `text-amber-600` in cells.

    5. **HTMX wiring assertion**: GET `/audit` body contains the literals `hx-get="/_partials/audit/timeline"`, `hx-trigger="every 30s"`, `hx-include="closest form"`.

    6. **Russian copy lockdown**: GET `/audit` body contains all of: `Аудит`, `Активность за 24 часа`, `Фильтр`, `Сбросить`, `все решения`, `за 24 часа`, `Когда`, `Машина`, `Инструмент`, `Решение`, `Результат`, `Fingerprint`.

    7. **Timeline partial standalone**: GET `/_partials/audit/timeline` after the bulk seed -> 200, body has 24 bar divs with `bg-slate-700`, response does NOT contain `<html>` (it is a partial).

    8. **Timeline partial empty**: With empty DB, GET `/_partials/audit/timeline` -> body contains `Нет данных за выбранный период.` and DOES NOT contain `flex items-end`.

    9. **Sidebar regression**: GET `/` (overview) -> sidebar has the new `Аудит` link AND the existing `Находки`, `Политика`, `Машины` links (verify nav was not broken by base.html edit).

    10. **Backward-compat smoke**: Verify the existing endpoints that v0.1 agents hit still return 200: GET `/api/v1/health`, POST `/api/v1/inventory` with the existing payload schema, GET `/api/v1/policy`. These regress if anything in PLAN 03 accidentally touched routing.

    Then run the full suite to confirm 185+ existing tests are unaffected.
  </action>
  <verify>
    <automated>cd /Users/timderbak/dev/ccguard &amp;&amp; python -m pytest tests/ -x -q</automated>
  </verify>
  <done>
    Full suite green. Count the new tests added across PLAN 01-06: `grep -rc "^def test_" tests/unit/test_audit_*.py tests/unit/test_tool_use_service.py tests/integration/test_audit_*.py` should sum to >= 20 (target per ROADMAP success criterion 5). Phase 1 closure complete: TUA-01, TUA-02, TUA-03 all green, no regressions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new boundaries — this plan adds tests only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-25 | Information Disclosure | flusher privacy regression | mitigate | Explicit test #7 in Task 1 scans POST body for any key resembling raw tool_input; recursive check catches accidental field additions |
| T-01-SC | Tampering | new packages | mitigate | No new packages — N/A |
</threat_model>

<verification>
- `pytest tests/ -x` green (full suite, 185+ existing + 20+ new).
- `grep -rc "^def test_" tests/unit/test_audit_fingerprint.py tests/unit/test_audit_buffer.py tests/unit/test_audit_schemas.py tests/unit/test_audit_hook_main.py tests/unit/test_audit_flusher.py tests/unit/test_tool_use_service.py tests/integration/test_audit_api.py tests/integration/test_audit_page.py tests/integration/test_audit_timeline_partial.py tests/integration/test_audit_flush_e2e.py tests/integration/test_audit_smoke.py | awk -F: '{s+=$NF} END {print s}'` reports >= 20.
- All ROADMAP Phase 1 success criteria visibly satisfied:
  1. Agent logs tool-use to local buffer (PLAN 01/02 + flush e2e test #1)
  2. POST /api/v1/audit accepts batches (PLAN 03 + flush e2e test #1)
  3. /audit page with filters (PLAN 04 + smoke tests 1-3)
  4. Timeline at 24h with hourly grouping (PLAN 05 + smoke tests 1, 7)
  5. 185+ existing green + 20+ new (this plan)
</verification>

<success_criteria>
- All 6 Phase 1 plans (01..06) integrate without regression.
- TUA-01, TUA-02, TUA-03 demonstrably satisfied via integration tests.
- New test count >= 20.
- Privacy invariant (no raw tool_input on the wire or in DB) explicitly tested.
</success_criteria>

<output>
Create `.planning/phase-01-tool-use-audit-foundation/01-06-SUMMARY.md` when done. Also append a Phase 1 closure note at the end of the SUMMARY: list new tests count, confirm ROADMAP success criteria 1-5 with file:line evidence.
</output>
