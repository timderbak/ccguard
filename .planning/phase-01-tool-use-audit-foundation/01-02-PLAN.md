---
phase: 01-tool-use-audit-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/ccguard/agent/audit_hook/hook_main.py
  - src/ccguard/agent/audit_hook/flusher.py
  - src/ccguard/agent/audit_hook/flusher_main.py
  - src/ccguard/agent/audit_main.py
  - src/ccguard/agent/install.py
  - src/ccguard/schemas/tool_use.py
  - pyproject.toml
autonomous: true
requirements: [TUA-01, TUA-02]
must_haves:
  truths:
    - "PostToolUse hook invocation completes in <20ms (INSERT only; flush detached)"
    - "Hook never raises — fails open even on malformed stdin"
    - "Detached flusher subprocess survives parent (hook) exit"
    - "ccguard install registers PostToolUse with matcher [\"*\"] capturing all tools"
    - "Schema-version constant SCHEMA_VERSION_AUDIT='0.2' is single-sourced from ccguard.schemas.tool_use"
  artifacts:
    - path: "src/ccguard/agent/audit_hook/hook_main.py"
      provides: "main_cli() entrypoint that reads PostToolUse stdin → fingerprint → buffer.insert → maybe_spawn_flusher"
      contains: "def main_cli"
    - path: "src/ccguard/agent/audit_hook/flusher.py"
      provides: "maybe_spawn_flusher (double-fork) + _run_flush_loop (drain → POST → delete_ids)"
      contains: "def maybe_spawn_flusher"
    - path: "src/ccguard/agent/audit_main.py"
      provides: "ccguard-audit-bin console_script entrypoint"
      contains: "def main"
    - path: "src/ccguard/schemas/tool_use.py"
      provides: "SCHEMA_VERSION_AUDIT constant + AuditBatchIn/Out + ToolUseEventIn pydantic v2"
      contains: "SCHEMA_VERSION_AUDIT"
    - path: "pyproject.toml"
      provides: "[project.scripts] ccguard-audit-bin entry"
      contains: "ccguard-audit-bin"
  key_links:
    - from: "hook_main.py"
      to: "buffer.ToolBufferDB"
      via: "context manager INSERT"
      pattern: "ToolBufferDB"
    - from: "hook_main.py"
      to: "flusher.maybe_spawn_flusher"
      via: "function call after INSERT"
      pattern: "maybe_spawn_flusher"
    - from: "flusher._run_flush_loop"
      to: "POST /api/v1/audit"
      via: "httpx.post with X-CCGuard-Token header + AuditBatchIn body"
      pattern: "/api/v1/audit"
    - from: "install.py"
      to: "~/.claude/settings.json PostToolUse[].matcher"
      via: "idempotent merge with matcher=*"
      pattern: "PostToolUse"
---

<objective>
Wire the agent-side audit hook end-to-end: stdin parser + flush dispatcher (PLAN 01 primitives consumed), detached subprocess flusher, console_script binary, schema module shared between agent and server (so PLAN 03 imports the same constants), and `ccguard install` extension that registers PostToolUse in `~/.claude/settings.json`.

Purpose: TUA-02. Convert the inert primitives from PLAN 01 into a working PostToolUse hook that ships events to a server endpoint (defined in PLAN 03). The schema module is created here because it is the single source of truth shared between the agent flusher and the server router.
Output: working `ccguard-audit-bin` binary that, when configured via `ccguard install`, captures every Claude Code tool call and batch-POSTs to `/api/v1/audit` without blocking the hook hot path beyond 20ms.
</objective>

<execution_context>
@/Users/timderbak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/timderbak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phase-01-tool-use-audit-foundation/01-CONTEXT.md
@.planning/phase-01-tool-use-audit-foundation/01-RESEARCH.md
@.planning/phase-01-tool-use-audit-foundation/01-01-PLAN.md

# Existing precedent — mirror these
@src/ccguard/agent/enforce.py
@src/ccguard/agent/enforce_main.py
@src/ccguard/agent/install.py
@src/ccguard/agent/sync.py
@src/ccguard/agent/config.py
@src/ccguard/schemas/_base.py
@src/ccguard/schemas/audit.py
@pyproject.toml

<interfaces>
<!-- Consumed from PLAN 01 -->

From src/ccguard/agent/audit_hook/buffer.py:
- `ToolBufferDB(path)` context manager
- `insert(ts, tool_name, fingerprint, decision, result_status)`
- `row_count()`, `drain(limit=200)`, `delete_ids(ids)`, `trim_to_cap(cap=10_000)`

From src/ccguard/agent/audit_hook/fingerprint.py:
- `compute_fingerprint(tool_name, tool_input) -> str`

From src/ccguard/agent/config.py:
- `default_config_dir() -> Path`
- existing config-loading helper that exposes `server_url` + `token` (used by sync.py — replicate)

From src/ccguard/agent/sync.py:
- httpx client pattern with `X-CCGuard-Token` header
- machine_id resolution via `ccguard.agent.machine_id`

<!-- New contracts this plan defines (consumed by PLAN 03) -->

Module `ccguard.schemas.tool_use`:
- `SCHEMA_VERSION_AUDIT: Final[str] = "0.2"`
- `class ToolUseEventIn(SchemaBase)`:
  - `ts: datetime`
  - `tool_name: str` (min_length=1, max_length=128)
  - `fingerprint: str` (pattern=r"^[0-9a-f]{16}$")
  - `decision: Literal["allow", "deny", "error"]`
  - `result_status: Literal["success", "error", "blocked"]`
- `class AuditBatchIn(SchemaBase)`:
  - `schema_version: str`
  - `machine_id: str` (min_length=1)
  - `events: list[ToolUseEventIn]` (max_length=200, min_length=1)
- `class AuditBatchOut(SchemaBase)`:
  - `accepted: bool`
  - `stored: int`
  - `rejected: int`
  - `server_schema_version: str`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schemas module + hook_main + flusher with detached subprocess</name>
  <files>
    src/ccguard/schemas/tool_use.py,
    src/ccguard/agent/audit_hook/hook_main.py,
    src/ccguard/agent/audit_hook/flusher.py,
    src/ccguard/agent/audit_hook/flusher_main.py,
    tests/unit/test_audit_schemas.py,
    tests/unit/test_audit_hook_main.py,
    tests/unit/test_audit_flusher.py
  </files>
  <behavior>
    Schemas:
    - `SCHEMA_VERSION_AUDIT == "0.2"`.
    - `AuditBatchIn.model_validate({..., events: []})` raises ValidationError (min_length=1).
    - `AuditBatchIn.model_validate({..., events: [<201 items>]})` raises (max_length=200).
    - `ToolUseEventIn.model_validate({fingerprint: "NOTHEX!"})` raises (pattern).
    - `ToolUseEventIn.model_validate({decision: "ignore"})` raises (Literal).

    hook_main:
    - `main_cli(stdin_text='{"tool_name":"Bash","tool_input":{"command":"git status"},"tool_response":{}}')` returns 0 and inserts one row into the test-injected buffer path.
    - Per locked decision: `decision` is always `"allow"` (PostToolUse fires only after PreToolUse permitted).
    - `result_status` derivation:
      - if `tool_response.get("error")` truthy OR `tool_response.get("success") is False` → `"error"`
      - elif `tool_response.get("interrupted")` truthy → `"blocked"`
      - else → `"success"`
    - Malformed JSON in stdin → `main_cli` returns 0 (fail-open, no exception).
    - Missing tool_name → uses `"(unknown)"` placeholder, still inserts row.
    - tool_input dict is dropped from local scope after fingerprinting (no reference retained in any return value — verified by inspecting buffer row: only the 16-hex fingerprint is stored).
    - Total execution under 20ms on a warm buffer (measure with `time.perf_counter` in a test; assert < 100ms wall to be lenient in CI).
    - After insert, `maybe_spawn_flusher` is called (mocked in test; assert call).

    flusher:
    - `_should_spawn(row_count=10)` with no recent flush → returns True if 10 ≥ 0 and lock-pidfile age > 30s.
    - `_should_spawn(row_count=51, last_flush_age=2s)` → True (threshold met).
    - `_should_spawn(row_count=10, last_flush_age=2s)` → False (under threshold + recent).
    - `maybe_spawn_flusher` with no fork available (mock `os.fork`) falls back to `subprocess.Popen` with `start_new_session=True, stdin/stdout/stderr=DEVNULL, close_fds=True`.
    - PID-file lock at `~/.ccguard/audit_flush.lock` prevents concurrent flushers (second invocation no-op if pidfile mtime < 5s).
    - `_run_flush_loop` (called inside flusher_main): drains up to 200 rows → builds `AuditBatchIn(schema_version=SCHEMA_VERSION_AUDIT, machine_id=..., events=[...])` → POSTs to `f"{server_url}/api/v1/audit"` with `X-CCGuard-Token` header → on 200: `delete_ids(ids)` → exits.
    - On HTTP failure: exponential backoff (1s, 2s, 4s; max 3 attempts), then exit (rows remain). NO daemon thread — must be a separate process per RESEARCH Pitfall #1.
    - On batch size > 200: drain repeatedly in chunks of 200 until row_count() == 0 or attempt fails.
    - After successful flush, calls `buffer.trim_to_cap(10_000)` (drop-oldest if any overflow remained).
  </behavior>
  <action>
    **Schemas (`src/ccguard/schemas/tool_use.py`)** — Pydantic v2 inheriting `SchemaBase` from `_base.py`. Use `Literal`, `Field(min_length=, max_length=, pattern=)`. Include module docstring explaining privacy contract (no raw tool_input field exists). Export `SCHEMA_VERSION_AUDIT`, `ToolUseEventIn`, `AuditBatchIn`, `AuditBatchOut`.

    **Hook entry (`src/ccguard/agent/audit_hook/hook_main.py`)** — implement per RESEARCH "Pattern 1" with these specifics:
    - `def main_cli(stdin_text: str | None = None) -> int`
    - Wrap entire body in `try/except Exception: return 0` (fail-open).
    - Parse stdin JSON with `extra="ignore"` semantics (use plain `json.loads`; we only read `tool_name`, `tool_input`, `tool_response`).
    - `decision = "allow"` (hardcoded — per user decision #2 in PLANNING brief).
    - `_result_status_from_response(tool_response)` helper per behavior bullets.
    - `ts = datetime.now(UTC).isoformat()`.
    - `fp = compute_fingerprint(tool_name, tool_input)` — then immediately delete the `tool_input` local var with `del tool_input` to make privacy intent explicit.
    - Open `ToolBufferDB(default_config_dir() / "audit_buffer.db")`, insert, capture `row_count` from the same context.
    - Call `maybe_spawn_flusher(row_count_hint=row_count)` outside the context manager (so the connection is closed before fork).
    - Return 0 always.

    **Flusher (`src/ccguard/agent/audit_hook/flusher.py`)** — implement per RESEARCH "Async flush — detached subprocess":
    - Module constants: `_FLUSH_LOCK = default_config_dir() / "audit_flush.lock"`, `_BATCH_THRESHOLD = 50`, `_TIME_THRESHOLD_S = 30`, `_MAX_BATCH = 200`, `_BACKOFF_SECONDS = (1, 2, 4)`, `_MAX_ATTEMPTS = 3`.
    - `def _should_spawn(row_count: int) -> bool` — True if `row_count >= _BATCH_THRESHOLD` OR pidfile missing OR `(time.time() - pidfile.stat().st_mtime) > _TIME_THRESHOLD_S`.
    - `def _acquire_lock() -> bool` — write current PID to `_FLUSH_LOCK` atomically (write to `.tmp` then rename); return False if a fresh lock (< 5s old) already exists.
    - `def maybe_spawn_flusher(row_count_hint: int) -> None`:
      - if not `_should_spawn(...)` return.
      - if not `_acquire_lock()` return.
      - if `hasattr(os, "fork")`: double-fork pattern (parent forks; child setsid + forks; grandchild calls `_run_flush_loop()` then `os._exit(0)`).
      - else: `subprocess.Popen([sys.executable, "-m", "ccguard.agent.audit_hook.flusher_main"], start_new_session=True, stdin=DEVNULL, stdout=DEVNULL, stderr=DEVNULL, close_fds=True)`.
    - `def _run_flush_loop() -> None`:
      - Load config (server_url, token, machine_id) via existing `ccguard.agent.config` and `ccguard.agent.machine_id` helpers (look at sync.py for the exact pattern).
      - Open buffer; in a loop while `row_count() > 0` and `attempts < _MAX_ATTEMPTS`:
        - `rows = buffer.drain(_MAX_BATCH)`; if empty break.
        - Build `AuditBatchIn` (convert `ts` strings back to `datetime` via `datetime.fromisoformat` — let Pydantic do this; ToolUseEventIn declares `datetime` so pydantic parses ISO-8601 string).
        - POST to `f"{server_url}/api/v1/audit"` with header `X-CCGuard-Token: {token}`, JSON body `AuditBatchIn.model_dump(mode="json")`, timeout 10s.
        - On 2xx: `buffer.delete_ids([r["id"] for r in rows])`; reset attempt counter.
        - On non-2xx or exception: increment attempts, `time.sleep(_BACKOFF_SECONDS[attempt-1])`, continue.
      - After loop: `buffer.trim_to_cap(10_000)`.

    **Flusher main (`src/ccguard/agent/audit_hook/flusher_main.py`)** — `def main(): _run_flush_loop()` wrapped in try/except (write to local rotating log on failure — pattern from `agent/audit.py`).

    **Tests:**
    - `tests/unit/test_audit_schemas.py` — pydantic validation matrix.
    - `tests/unit/test_audit_hook_main.py` — monkeypatch `default_config_dir` to `tmp_path`, monkeypatch `flusher.maybe_spawn_flusher` to a `MagicMock`. Drive `main_cli(stdin_text=...)` through happy + malformed + missing-fields cases. Assert buffer row count + that `tool_input` content never appears as a substring in any sqlite cell (`SELECT * FROM events` → join all values → assert original command string is NOT present, only fingerprint).
    - `tests/unit/test_audit_flusher.py` — mock httpx via `respx` if available else use a small fake; assert: drains correctly, calls delete_ids on success, backs off on failure, exits after 3 attempts; assert pidfile freshness check; assert subprocess fallback uses correct argv.
  </action>
  <verify>
    <automated>cd /Users/timderbak/dev/ccguard && python -m pytest tests/unit/test_audit_schemas.py tests/unit/test_audit_hook_main.py tests/unit/test_audit_flusher.py -x -q</automated>
  </verify>
  <done>
    All three unit suites green. `main_cli` runs in under 100ms wall-clock per invocation in test. No raw command string appears in any buffer row when verified by SQL inspection. mypy strict passes on the new modules.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Console-script entrypoint + ccguard install PostToolUse registration</name>
  <files>
    src/ccguard/agent/audit_main.py,
    src/ccguard/agent/install.py,
    pyproject.toml,
    tests/unit/test_install.py
  </files>
  <behavior>
    - `ccguard-audit-bin` is registered as a `[project.scripts]` entry pointing to `ccguard.agent.audit_main:main`.
    - `audit_main.main()` reads stdin and dispatches to `hook_main.main_cli(sys.stdin.read())`, returns its exit code.
    - `ccguard install` (existing typer command in agent/cli.py) now also writes a PostToolUse entry to `~/.claude/settings.json`:
      ```json
      "PostToolUse": [
        {"matcher": "*", "hooks": [{"type": "command", "command": "<path-to-ccguard-audit-bin>", "timeout": 3}]}
      ]
      ```
    - The merge is idempotent: running `ccguard install` twice does not create duplicate PostToolUse entries.
    - The PreToolUse section is left UNTOUCHED (existing v0.1 behavior preserved). Verified by snapshot assertion in `tests/unit/test_install.py`.
    - The new audit hook coexists with PreToolUse — both sections present, both matcher sets correct.
    - `verify_installation()` (existing function) is extended to ALSO check that PostToolUse is registered when audit is enabled; reports `"audit_hook_registered": True/False` in its result.
    - Running install on a settings.json missing the `hooks` key creates it from scratch with both Pre and Post sections.
    - `disableAllHooks=true` in settings.json is still flagged by verify (existing pattern preserved).
  </behavior>
  <action>
    **Console script (`src/ccguard/agent/audit_main.py`)** — minimal shim mirroring `enforce_main.py`:
    ```
    from __future__ import annotations
    import sys
    from ccguard.agent.audit_hook.hook_main import main_cli
    def main() -> int:
        return main_cli(sys.stdin.read())
    if __name__ == "__main__":
        sys.exit(main())
    ```

    **pyproject.toml** — add under `[project.scripts]` (alongside existing entries):
    ```
    ccguard-audit-bin = "ccguard.agent.audit_main:main"
    ```
    Leave existing `ccguard`, `ccguard-server`, `ccguard-enforce-bin` entries unchanged.

    **install.py extension** — read the existing module first to understand the data model. Then:
    - Introduce a constant `AUDIT_HOOK_MATCHERS = ["*"]` next to existing `HOOK_MATCHERS`.
    - Refactor the existing per-PreToolUse hook-write helper to accept a `hook_event: Literal["PreToolUse", "PostToolUse"]` parameter, a `matchers: list[str]` parameter, and the binary path. The PreToolUse call site uses `HOOK_MATCHERS` + `ccguard-enforce-bin`; new PostToolUse call site uses `AUDIT_HOOK_MATCHERS` + `ccguard-audit-bin`.
    - Resolve the audit binary path the same way enforce binary path is resolved (look at the existing helper that produces `~/.ccguard/bin/ccguard-enforce` symlink path or equivalent).
    - Update idempotency check: when scanning existing PostToolUse entries, match by `(matcher set, command path)` — skip insertion if equivalent entry already present.
    - Extend `verify_installation()` return dict with `"audit_hook_registered": bool`.
    - PreToolUse handling MUST be untouched (existing tests in `tests/unit/test_install.py` must still pass without modification).

    **Tests** — extend `tests/unit/test_install.py`:
    - Add cases asserting PostToolUse section is created with `matcher=*`, correct command path, timeout 3.
    - Idempotency: run install twice → still one PostToolUse entry.
    - Coexistence: after install, BOTH PreToolUse (with v0.1 matchers) and PostToolUse (with `*`) present.
    - `verify_installation()` returns `audit_hook_registered=True` after install.
    - Existing PreToolUse-only assertions still pass (regression).
  </action>
  <verify>
    <automated>cd /Users/timderbak/dev/ccguard && python -m pytest tests/unit/test_install.py -x -q</automated>
  </verify>
  <done>
    `pip install -e .` exposes `ccguard-audit-bin` on PATH. `ccguard install` produces a settings.json with both PreToolUse and PostToolUse correctly populated. All existing install tests + new audit tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code → ccguard-audit-bin | Untrusted stdin (Claude Code-controlled but parsed without validation history) |
| Agent → server `/api/v1/audit` | Network boundary; auth via X-CCGuard-Token |
| Hook proc → detached flusher | Process-boundary; flusher inherits no privileged state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Denial of Service | hook_main on malformed stdin | mitigate | Top-level `try/except Exception: return 0`; tested with malformed JSON |
| T-01-06 | Spoofing | flusher → /api/v1/audit | mitigate | Reuse existing `X-CCGuard-Token` header pattern from sync.py |
| T-01-07 | Information Disclosure | tool_input in transit | mitigate | Only fingerprint (16-hex) crosses the boundary; verified by test inspecting buffer rows |
| T-01-08 | Tampering | settings.json idempotency | mitigate | Match by (matcher_set, command_path); duplicate insertion guarded; tested |
| T-01-09 | Denial of Service | hook latency budget | mitigate | Detached subprocess for flush; hook never makes a network call; perf assertion in test (<100ms wall) |
| T-01-10 | Elevation of Privilege | new console script | accept | No new privileged ops; ccguard-audit-bin runs as the same user as Claude Code |
| T-01-SC | Tampering | npm/pip installs | mitigate | No new third-party packages — only stdlib + already-pinned deps; Package Legitimacy Audit N/A |
</threat_model>

<verification>
- All three new unit suites + extended test_install.py green.
- Full regression: `pytest tests/ -x` passes (185+ existing tests untouched).
- `ruff check src/ccguard/agent/audit_hook/ src/ccguard/agent/audit_main.py src/ccguard/schemas/tool_use.py` clean.
- `mypy --strict src/ccguard/agent/audit_hook/ src/ccguard/schemas/tool_use.py` clean.
- After `pip install -e .` + `ccguard install`, `cat ~/.claude/settings.json | jq '.hooks.PostToolUse'` shows a `*`-matcher entry pointing to `ccguard-audit-bin`.
</verification>

<success_criteria>
- TUA-01 satisfied at agent layer: PostToolUse hook fingerprints + buffers without storing raw tool_input.
- TUA-02 satisfied at agent layer: detached subprocess flushes batches of up to 200 events to `/api/v1/audit` with retry/backoff.
- Existing agent functionality unaffected.
</success_criteria>

<output>
Create `.planning/phase-01-tool-use-audit-foundation/01-02-SUMMARY.md` when done.
</output>
