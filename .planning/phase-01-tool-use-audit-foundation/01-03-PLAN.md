---
phase: 01-tool-use-audit-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - src/ccguard/server/db/models.py
  - src/ccguard/server/db/session.py
  - src/ccguard/server/api/audit.py
  - src/ccguard/server/main.py
  - src/ccguard/server/services/tool_use_service.py
  - tests/unit/test_db_models.py
  - tests/integration/test_audit_api.py
autonomous: true
requirements: [TUA-02]
must_haves:
  truths:
    - "ToolUseEvent table is created on server startup via SQLModel.metadata.create_all (NO Alembic per locked decision)"
    - "POST /api/v1/audit accepts a valid AuditBatchIn batch and persists each event"
    - "Missing/invalid X-CCGuard-Token returns 401"
    - "Major schema_version mismatch returns 422"
    - "Batch exceeding MAX_BATCH=200 returns 413"
    - "Composite indexes (machine_id,ts), (tool_name,ts), (decision,ts) exist after init_db"
    - "v0.1 agents (which never call /api/v1/audit) continue to function — no existing endpoint touched"
  artifacts:
    - path: "src/ccguard/server/db/models.py"
      provides: "ToolUseEvent SQLModel"
      contains: "class ToolUseEvent"
    - path: "src/ccguard/server/api/audit.py"
      provides: "POST /api/v1/audit router"
      exports: ["router"]
      contains: "@router.post(\"/audit\""
    - path: "src/ccguard/server/services/tool_use_service.py"
      provides: "list_events + timeline_buckets query functions (timeline used by PLAN 05)"
      contains: "def timeline_buckets"
    - path: "src/ccguard/server/db/session.py"
      provides: "init_db extended with composite index DDL"
      contains: "ix_tooluseevent_machine_ts"
  key_links:
    - from: "src/ccguard/server/api/audit.py"
      to: "src/ccguard/schemas/tool_use.py"
      via: "import AuditBatchIn/Out + SCHEMA_VERSION_AUDIT"
      pattern: "from ccguard.schemas.tool_use import"
    - from: "src/ccguard/server/main.py"
      to: "src/ccguard/server/api/audit.py"
      via: "app.include_router(audit.router)"
      pattern: "audit\\.router"
    - from: "POST /api/v1/audit"
      to: "ToolUseEvent table"
      via: "session.add + session.commit"
      pattern: "ToolUseEvent\\("
---

<objective>
Server-side ingest path for tool-use events: the `ToolUseEvent` SQLModel, the `POST /api/v1/audit` FastAPI router, composite-index DDL bootstrap, and a thin service layer (`tool_use_service.py`) holding two query functions (`list_events`, `timeline_buckets`) that PLANS 04/05 will consume.

Purpose: TUA-02 server half. Single source of truth for the fleet; absorbs batches from the agent flusher; exposes query helpers that the UI plans build on without duplicating SQL.

Output: working endpoint reachable with the existing X-CCGuard-Token auth, new SQL table with three composite indexes, and a service module that PLANS 04/05 can import.
</objective>

<execution_context>
@/Users/timderbak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/timderbak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phase-01-tool-use-audit-foundation/01-CONTEXT.md
@.planning/phase-01-tool-use-audit-foundation/01-RESEARCH.md
@.planning/phase-01-tool-use-audit-foundation/01-02-PLAN.md

# Existing precedent — mirror these
@src/ccguard/server/db/models.py
@src/ccguard/server/db/session.py
@src/ccguard/server/api/findings.py
@src/ccguard/server/api/inventory.py
@src/ccguard/server/api/deps.py
@src/ccguard/server/main.py
@src/ccguard/server/services/finding_service.py
@tests/integration/conftest.py
@tests/integration/test_server_inventory.py

<interfaces>
<!-- Consumed from PLAN 02 -->

From src/ccguard/schemas/tool_use.py:
- `SCHEMA_VERSION_AUDIT: Final[str] = "0.2"`
- `AuditBatchIn`, `AuditBatchOut`, `ToolUseEventIn`

<!-- Pre-existing server helpers -->

From src/ccguard/server/api/deps.py:
- `get_session(request: Request) -> Iterator[Session]`
- `require_token(...)` — auth dependency

From src/ccguard/server/db/models.py:
- `_utcnow() -> datetime` helper
- `AuditRecord` (unchanged — DO NOT modify)
- `Machine`, `InventorySnapshot`, etc.

From src/ccguard/server/db/session.py:
- `init_db(engine: Engine) -> None` — currently calls `SQLModel.metadata.create_all(engine)`

<!-- New contracts this plan defines (consumed by PLANS 04, 05, 06) -->

SQL table `tooluseevent` (SQLModel autonaming from `ToolUseEvent`):
- `id INTEGER PRIMARY KEY AUTOINCREMENT`
- `machine_id TEXT NOT NULL`
- `ts DATETIME NOT NULL`
- `received_at DATETIME NOT NULL`
- `tool_name TEXT NOT NULL`
- `fingerprint TEXT NOT NULL`
- `decision TEXT NOT NULL`
- `result_status TEXT NOT NULL`

Composite indexes (idempotent DDL applied in `init_db`):
- `ix_tooluseevent_machine_ts ON tooluseevent(machine_id, ts DESC)`
- `ix_tooluseevent_tool_ts ON tooluseevent(tool_name, ts DESC)`
- `ix_tooluseevent_decision_ts ON tooluseevent(decision, ts DESC)`

Module `ccguard.server.services.tool_use_service`:
- `def list_events(session, *, machine_id_like: str | None, tool_name: str | None, decision: str | None, timeframe: Literal["1h","24h","7d"] = "24h", limit: int = 200) -> tuple[list[ToolUseEvent], int]` — returns (rows, total_matched_count)
- `def timeline_buckets(session, *, hours: int = 24, machine_id_like: str | None, tool_name: str | None, decision: str | None) -> list[BucketDict]` — dense list of `hours` buckets oldest-first
- `BucketDict = TypedDict("BucketDict", {"bucket_iso": str, "hour_label": str, "count": int})`
- `_TIMEFRAMES: dict[str, int] = {"1h": 1, "24h": 24, "7d": 168}` — hours window
- `hour_label` format: `"%H:%M %d.%m"` (matches UI-SPEC chart label)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ToolUseEvent model + composite indexes + service layer</name>
  <files>
    src/ccguard/server/db/models.py,
    src/ccguard/server/db/session.py,
    src/ccguard/server/services/tool_use_service.py,
    tests/unit/test_db_models.py,
    tests/unit/test_tool_use_service.py
  </files>
  <behavior>
    Model:
    - Importing `ToolUseEvent` from `ccguard.server.db.models` succeeds.
    - After `init_db(engine)`, the table `tooluseevent` exists with columns matching the interfaces block.
    - The composite indexes `ix_tooluseevent_machine_ts`, `ix_tooluseevent_tool_ts`, `ix_tooluseevent_decision_ts` exist (verify via `PRAGMA index_list('tooluseevent')`).
    - `init_db` is idempotent — second call does NOT raise (uses `CREATE INDEX IF NOT EXISTS`).
    - Inserting and selecting back a ToolUseEvent round-trips ts as UTC `datetime`.
    - Existing `AuditRecord` table is untouched (regression test: assert row count unchanged).

    Service `list_events`:
    - Empty filter, empty table → returns `([], 0)`.
    - With 5 seeded events, default filter → returns (5 rows, 5).
    - `machine_id_like="laptop"` filters via `LIKE %laptop%`.
    - `tool_name="Bash"` filters exact match.
    - `decision="allow"` filters exact match.
    - `timeframe="1h"` excludes events older than 1 hour from now.
    - `timeframe="24h"` includes everything in the last 24h.
    - `limit=200` enforced; `total_matched_count` reflects pre-limit count.
    - Result ordered by `ts DESC` (newest first).

    Service `timeline_buckets`:
    - Empty table → returns exactly 24 buckets, all with `count=0`, oldest first, newest last.
    - Seeding 3 events all in the most-recent hour → 23 zero buckets + 1 bucket with count=3 at the end.
    - Filter parameters honored.
    - `hour_label` format `"HH:MM DD.MM"` for each bucket.
    - Bucket count exactly equals `hours` parameter (no gaps).
    - All bucket timestamps are aligned to hour boundary (minute=0).
  </behavior>
  <action>
    **Models (`src/ccguard/server/db/models.py`)** — APPEND (do not modify existing classes). Add:
    ```
    class ToolUseEvent(SQLModel, table=True):
        """Tool-use events from PostToolUse hook. Privacy: no raw tool_input is ever stored.
        Auto-created via SQLModel.metadata.create_all (no Alembic per phase decision)."""
        id: int | None = Field(default=None, primary_key=True)
        machine_id: str = Field(index=True)
        ts: datetime = Field(index=True)
        received_at: datetime = Field(default_factory=_utcnow)
        tool_name: str = Field(index=True)
        fingerprint: str = Field(index=True)
        decision: str = Field(index=True)
        result_status: str
    ```
    Reuse the existing `_utcnow` helper. DO NOT add Alembic. DO NOT touch AuditRecord.

    **Session init (`src/ccguard/server/db/session.py`)** — extend `init_db(engine)` to ALSO execute the three composite-index DDL statements after `SQLModel.metadata.create_all(engine)`:
    ```
    from sqlalchemy import text
    with engine.begin() as conn:
        conn.execute(text("CREATE INDEX IF NOT EXISTS ix_tooluseevent_machine_ts  ON tooluseevent(machine_id, ts DESC)"))
        conn.execute(text("CREATE INDEX IF NOT EXISTS ix_tooluseevent_tool_ts     ON tooluseevent(tool_name, ts DESC)"))
        conn.execute(text("CREATE INDEX IF NOT EXISTS ix_tooluseevent_decision_ts ON tooluseevent(decision, ts DESC)"))
    ```
    Existing init_db body preserved.

    **Service (`src/ccguard/server/services/tool_use_service.py`)** — new module:
    - `_TIMEFRAMES: dict[str, int] = {"1h": 1, "24h": 24, "7d": 168}`.
    - `list_events(session, *, machine_id_like=None, tool_name=None, decision=None, timeframe="24h", limit=200) -> tuple[list[ToolUseEvent], int]`:
      - Build a base `select(ToolUseEvent)` filter with `(ToolUseEvent.ts >= now - timedelta(hours=_TIMEFRAMES[timeframe]))`.
      - Conditionally apply `ToolUseEvent.machine_id.like(f"%{machine_id_like}%")`, `ToolUseEvent.tool_name == tool_name`, `ToolUseEvent.decision == decision`.
      - Total = `session.exec(select(func.count()).select_from(filtered_subquery)).one()`.
      - Rows = `session.exec(stmt.order_by(ToolUseEvent.ts.desc()).limit(limit)).all()`.
      - Return `(list(rows), int(total))`.
    - `timeline_buckets(session, *, hours=24, machine_id_like=None, tool_name=None, decision=None) -> list[BucketDict]`:
      - Implement per RESEARCH "Timeline SQL aggregation". Use raw `text()` SQL with bind params for the `strftime('%Y-%m-%d %H', ts)` GROUP BY (Pydantic/SQLModel doesn't expose strftime ergonomically).
      - Build dense 24-bucket output as in the example: walk `range(hours)`, look up `counts.get(key, 0)`, format `hour_label = b.strftime("%H:%M %d.%m")`.
      - `start = datetime.now(UTC).replace(minute=0, second=0, microsecond=0) - timedelta(hours=hours - 1)`.

    **Tests:**
    - Extend `tests/unit/test_db_models.py` with `ToolUseEvent` round-trip + index-existence (use `PRAGMA index_list` via raw connection).
    - New `tests/unit/test_tool_use_service.py` covering all behavior bullets. Use `make_engine(":memory:")` then `init_db(engine)` then seed via `Session(engine)`. Be sure timestamps are timezone-aware UTC.
  </action>
  <verify>
    <automated>cd /Users/timderbak/dev/ccguard && python -m pytest tests/unit/test_db_models.py tests/unit/test_tool_use_service.py -x -q</automated>
  </verify>
  <done>
    `ToolUseEvent` imports cleanly, table + 3 composite indexes created on init_db, both service functions behave per the bullets, all existing model tests still green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /api/v1/audit endpoint + integration tests</name>
  <files>
    src/ccguard/server/api/audit.py,
    src/ccguard/server/main.py,
    tests/integration/test_audit_api.py
  </files>
  <behavior>
    - `POST /api/v1/audit` without `X-CCGuard-Token` header → 401.
    - With invalid token → 401.
    - With valid token + `schema_version="0.2"` + 3 valid events → 200, body `AuditBatchOut(accepted=True, stored=3, rejected=0, server_schema_version="0.2")`; 3 rows in `tooluseevent` table.
    - With `schema_version="1.0"` → 422 with detail mentioning incompatibility (major-version mismatch).
    - With `schema_version="0.5"` (minor diff, same major) → 200 (server is graceful per phase decision).
    - With 201 events → 413.
    - With 0 events → 422 (Pydantic min_length=1).
    - With malformed event (fingerprint not 16 hex) → 422.
    - With `decision="ignore"` → 422 (Literal).
    - `received_at` is server-set (close to `datetime.now(UTC)`); `ts` is preserved from request.
    - `machine_id` from the request body is stored as-is on each row (note: v0.2 does NOT pin token↔machine_id; this is documented limitation per RESEARCH security domain).
    - Concurrent batches from two clients persist all rows correctly.
    - v0.1 endpoints unaffected: a regression call to `GET /api/v1/inventory` still returns 200 with valid token.
  </behavior>
  <action>
    **Endpoint (`src/ccguard/server/api/audit.py`)** — implement per RESEARCH "POST /api/v1/audit endpoint":
    ```
    from __future__ import annotations
    from datetime import UTC, datetime
    from fastapi import APIRouter, Depends, HTTPException, status
    from sqlmodel import Session
    from ccguard.schemas.tool_use import AuditBatchIn, AuditBatchOut, SCHEMA_VERSION_AUDIT
    from ccguard.server.api.deps import get_session, require_token
    from ccguard.server.db.models import ToolUseEvent

    router = APIRouter(prefix="/api/v1")
    MAX_BATCH = 200

    @router.post("/audit", response_model=AuditBatchOut)
    def post_audit(payload: AuditBatchIn,
                   session: Session = Depends(get_session),
                   _token: str = Depends(require_token)) -> AuditBatchOut:
        if payload.schema_version.split(".")[0] != SCHEMA_VERSION_AUDIT.split(".")[0]:
            raise HTTPException(status_code=422, detail=f"schema_version {payload.schema_version} incompatible with server {SCHEMA_VERSION_AUDIT}")
        if len(payload.events) > MAX_BATCH:
            raise HTTPException(status_code=413, detail=f"batch too large (max {MAX_BATCH})")
        now = datetime.now(UTC)
        for e in payload.events:
            session.add(ToolUseEvent(
                machine_id=payload.machine_id,
                ts=e.ts, received_at=now,
                tool_name=e.tool_name, fingerprint=e.fingerprint,
                decision=e.decision, result_status=e.result_status,
            ))
        session.commit()
        return AuditBatchOut(accepted=True, stored=len(payload.events), rejected=0,
                             server_schema_version=SCHEMA_VERSION_AUDIT)
    ```
    Add a docstring referencing TUA-02 and explicitly stating that no raw tool_input field exists on the request schema by design.

    **App wiring (`src/ccguard/server/main.py`)** — in `create_app()`, alongside the existing `app.include_router(...)` calls, add `app.include_router(audit.router)`. Mirror the existing import style.

    **Tests (`tests/integration/test_audit_api.py`)** — use the existing `client` and `agent_token` fixtures from `tests/integration/conftest.py`:
    - Happy path 3-event batch → assert response body + DB row count.
    - Auth: missing header → 401; invalid token → 401.
    - schema_version: 1.0 → 422; 0.5 → 200 (graceful within major); missing field → 422.
    - Batch size: 201 events → 413; 0 events → 422.
    - Validation: bad fingerprint pattern → 422; bad decision Literal → 422.
    - Server stamps `received_at` close to now (assert within 5s).
    - Regression: an inventory POST in the same test session still succeeds.
    - Use `datetime.now(UTC).isoformat()` for `ts` in test payloads.
  </action>
  <verify>
    <automated>cd /Users/timderbak/dev/ccguard && python -m pytest tests/integration/test_audit_api.py -x -q</automated>
  </verify>
  <done>
    All integration cases pass; full suite `pytest tests/ -x` green; openapi schema (assert via `client.get("/openapi.json").json()["paths"]["/api/v1/audit"]["post"]`) advertises the new endpoint.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → POST /api/v1/audit | Untrusted batch payload across network |
| /api/v1/audit → SQLite | Validated payload writes to fleet database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Spoofing | POST /api/v1/audit | mitigate | `require_token` dep — existing X-CCGuard-Token (sha256-hashed at rest); constant-time compare in token_service |
| T-01-12 | Tampering | machine_id in request body | accept | Documented v0.2 limitation: token is not pinned to machine_id; received_at + token association allows post-hoc detection. v0.3 hardening (RESEARCH security domain) |
| T-01-13 | Denial of Service | unbounded batch size | mitigate | MAX_BATCH=200 enforced; 413 on overflow |
| T-01-14 | Tampering | SQL injection via filter inputs | mitigate | Pydantic Literal + parameterized SQL via SQLModel; raw SQL in service uses bind params |
| T-01-15 | Information Disclosure | endpoint reveals row counts to bad-token | mitigate | 401 returned before any DB query |
| T-01-16 | Repudiation | who sent which event | mitigate | `received_at` server-stamped; future enhancement (v0.3) to log token-id alongside |
| T-01-SC | Tampering | new packages | mitigate | No new packages introduced — N/A |
</threat_model>

<verification>
- `pytest tests/unit/test_db_models.py tests/unit/test_tool_use_service.py tests/integration/test_audit_api.py -x` green.
- Full suite `pytest tests/` green (185+ existing + new).
- `mypy --strict src/ccguard/server/api/audit.py src/ccguard/server/services/tool_use_service.py` green.
- OpenAPI doc includes `/api/v1/audit`.
- `PRAGMA index_list('tooluseevent')` on a freshly-initialized DB shows all three composite indexes.
</verification>

<success_criteria>
- TUA-02 server side complete: endpoint accepts batches, persists with proper indexes, fails closed on auth/validation issues.
- v0.1 agent compatibility preserved (no v0.1 endpoint touched).
- Service module exposes the timeline + list contracts that PLANS 04/05 will call.
</success_criteria>

<output>
Create `.planning/phase-01-tool-use-audit-foundation/01-03-SUMMARY.md` when done.
</output>
