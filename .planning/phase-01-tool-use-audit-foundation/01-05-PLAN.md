---
phase: 01-tool-use-audit-foundation
plan: 05
type: execute
wave: 4
depends_on: ["01-04"]
files_modified:
  - src/ccguard/server/web/templates/components/_audit_timeline.html
  - src/ccguard/server/web/templates/audit_feed.html
  - src/ccguard/server/web/routes.py
  - tests/integration/test_audit_timeline_partial.py
autonomous: true
requirements: [TUA-03]
must_haves:
  truths:
    - "_audit_timeline.html partial renders 24 vertical bars from a buckets context var"
    - "GET /_partials/audit/timeline returns rendered partial HTML (not JSON)"
    - "Bar heights are proportional to count/max_count with 2px floor for non-zero buckets"
    - "Filter params from the polled form are honored by the partial endpoint via hx-include"
    - "HTMX-polled refresh every 30s replaces only the timeline container"
    - "Empty state shows 'Нет данных за выбранный период.' when all buckets are 0"
  artifacts:
    - path: "src/ccguard/server/web/templates/components/_audit_timeline.html"
      provides: "Timeline bar-chart partial (CSS-only, no JS library)"
      contains: "role=\"img\""
    - path: "src/ccguard/server/web/routes.py"
      provides: "GET /_partials/audit/timeline endpoint"
      contains: "def audit_timeline_partial"
  key_links:
    - from: "audit_feed.html timeline card"
      to: "/_partials/audit/timeline"
      via: "hx-get + hx-trigger=every 30s + hx-include=closest form"
      pattern: "hx-get=\"/_partials/audit/timeline\""
    - from: "GET /_partials/audit/timeline"
      to: "tool_use_service.timeline_buckets"
      via: "filter params -> service -> buckets context"
      pattern: "timeline_buckets"
---

<objective>
Complete TUA-03 by adding the CSS-only timeline bar chart partial and its HTMX-polled endpoint. After this plan, the /audit page polls /_partials/audit/timeline every 30s, passing the current filter form values via hx-include, and re-renders only the bar chart with no full-page reload.

Purpose: UI-SPEC § Timeline chart spec — 24 bars, hourly buckets, no JS chart library, accessibility-ready.
Output: working timeline that visually updates as new events flow in.
</objective>

<execution_context>
@/Users/timderbak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/timderbak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phase-01-tool-use-audit-foundation/01-UI-SPEC.md
@.planning/phase-01-tool-use-audit-foundation/01-RESEARCH.md
@.planning/phase-01-tool-use-audit-foundation/01-03-PLAN.md
@.planning/phase-01-tool-use-audit-foundation/01-04-PLAN.md

# Mirror these for HTMX-partial precedent
@src/ccguard/server/web/templates/overview.html
@src/ccguard/server/web/templates/components/_fleet_table.html
@src/ccguard/server/web/routes.py

<interfaces>
Consumed from PLAN 03:
- timeline_buckets(session, *, hours, machine_id_like, tool_name, decision) -> list[BucketDict]
  Each bucket: keys bucket_iso, hour_label (format "HH:MM DD.MM"), count.

This plan adds:
- _audit_timeline.html partial context schema:
  - buckets: list[BucketDict] (length always equals window hours; default 24)
  - max_count: int (precomputed max of all bucket counts; 0 when all empty)
- Bar height percent computed in template: (bucket.count / max_count * 100) when max_count > 0 else 0
- Bar min-height: 2px when count > 0 else 0
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Timeline partial template + flip audit_feed.html to use it</name>
  <files>
    src/ccguard/server/web/templates/components/_audit_timeline.html,
    src/ccguard/server/web/templates/audit_feed.html
  </files>
  <behavior>
    - Partial renders a flex container `<div class="flex items-end gap-1 h-32" role="img" aria-label="Гистограмма аудит-событий по часам">` with one `<div>` per bucket.
    - Each bar uses class `flex-1 bg-slate-700 rounded-sm`, inline style `height: {percent}%; min-height: 2px;` when count > 0, `min-height: 0;` when count == 0, and a `title` attribute formatted as `{hour_label} — {count} событий`.
    - When `max_count == 0` (all empty) the bar container is REPLACED with `<p class="text-sm text-slate-400 py-6 text-center">Нет данных за выбранный период.</p>`.
    - Below the bars, an x-axis row with class `flex justify-between text-xs text-slate-400 mt-2` containing two `<span>` elements: first and last hour_label only (per UI-SPEC).
    - Card sub-heading `<h3 class="text-sm font-semibold text-slate-700 mb-3">Активность за 24 часа</h3>` is INSIDE the polled partial so it updates atomically with the bars.
    - When polled, the response body contains EXACTLY the partial markup (no `<html>`, no `<body>`, no DOCTYPE) so HTMX can swap inner content of the polled container.
    - `audit_feed.html` is updated to unconditionally include the timeline partial. The `timeline_partial_available` flag from PLAN 04 is dropped.
    - The outer card div keeps `hx-get`, `hx-trigger="every 30s"`, `hx-include="closest form"` attrs; the partial is what gets swapped.
  </behavior>
  <action>
    Create `src/ccguard/server/web/templates/components/_audit_timeline.html` per UI-SPEC § Timeline chart spec. Use a Jinja `{% if max_count == 0 %}` branch for the empty state. For the populated state, render an h3 sub-heading, the bar container `<div class="flex items-end gap-1 h-32" role="img" aria-label="...">` with a `{% for bucket in buckets %}` loop that computes the percent via `{% set pct = (bucket.count / max_count * 100) if max_count > 0 else 0 %}` and emits each bar with the inline style + title attribute described in behavior. After the bar container, render the x-axis labels row with `buckets[0].hour_label` and `buckets[-1].hour_label`. Russian strings exactly per UI-SPEC § Copywriting (`Активность за 24 часа`, `Нет данных за выбранный период.`, `Гистограмма аудит-событий по часам`).

    Update `src/ccguard/server/web/templates/audit_feed.html`: remove the PLAN-04 stub branch that gated the timeline include on `timeline_partial_available`. The timeline card body now is unconditionally `{% include "components/_audit_timeline.html" %}`. The outer card `<div>` keeps the existing hx-get, hx-trigger, hx-include attrs and the wrapper Tailwind classes (`bg-white rounded-lg shadow p-4 mb-4`). The route handler in Task 2 will pass `buckets` + `max_count` into the page context so the server-rendered page already shows real bars (no flash-of-empty).
  </action>
  <verify>
    <automated>cd /Users/timderbak/dev/ccguard &amp;&amp; python -c "from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('src/ccguard/server/web/templates'), autoescape=True); env.get_template('components/_audit_timeline.html').render(buckets=[{'hour_label':'10:00 25.05','count':3}]*24, max_count=3); env.get_template('audit_feed.html'); print('OK')"</automated>
  </verify>
  <done>
    Both templates parse and the partial renders sample buckets without exception. Russian copy matches UI-SPEC verbatim (grep for `Активность за 24 часа` and `Нет данных за выбранный период.`).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET /_partials/audit/timeline endpoint + integration tests</name>
  <files>
    src/ccguard/server/web/routes.py,
    tests/integration/test_audit_timeline_partial.py
  </files>
  <behavior>
    - `GET /_partials/audit/timeline` without admin session -> redirect to /login (or 401, matching existing behavior).
    - With admin session, empty DB -> 200, body contains `Нет данных за выбранный период.`, content-type `text/html`.
    - With 5 seeded events all in the most recent hour -> body contains 24 bar divs with class `flex-1 bg-slate-700 rounded-sm`; exactly 1 bar has `min-height: 2px;` style fragment, 23 bars have `min-height: 0;`.
    - Response body does NOT contain `<html>` or `<body>` (it is a partial).
    - Filter parameters honored: GET `/_partials/audit/timeline?tool_name=Bash` with mixed Bash + Edit events returns counts that match only Bash.
    - Filter parameters honored: `machine_id` does LIKE substring matching; `decision` exact match.
    - `timeframe` parameter accepted (default 24h) but the partial endpoint ALWAYS renders 24 buckets regardless of timeframe value in v0.2 (PRD timeline is fixed-24h per UI-SPEC card heading "Активность за 24 часа"; document this in handler docstring). For future-proofing, accept the param but ignore it. NOTE: the page-level route in PLAN 04 honors timeframe for the events table; only this partial fixes the chart window at 24h.
    - Route handler also updates the `/audit` (page) route from PLAN 04 to pass `buckets` and `max_count` in the page context, so the initial server-rendered page already shows real bars.
    - The audit_feed.html `timeline_partial_available` reference is removed.
    - HTMX polling smoke: assert the page contains `hx-get="/_partials/audit/timeline"`, `hx-trigger="every 30s"`, `hx-include="closest form"`.
  </behavior>
  <action>
    Add to `src/ccguard/server/web/routes.py`:

    1. New handler:
       - Path: `GET /_partials/audit/timeline`
       - Response class: HTMLResponse
       - Query params: `machine_id: str = ""`, `tool_name: str = ""`, `decision: str = ""`, `timeframe: str = "24h"` (accepted but unused for chart window; document via docstring).
       - Deps: `session: Session = Depends(get_session)`, `_admin = Depends(require_session)`.
       - Whitelist `decision` to one of `{allow, deny, error, ""}`, coerce others to "".
       - Call `timeline_buckets(session, hours=24, machine_id_like=machine_id or None, tool_name=tool_name or None, decision=decision or None)`.
       - Compute `max_count = max((b["count"] for b in buckets), default=0)`.
       - Return `templates.TemplateResponse("components/_audit_timeline.html", {"request": request, "buckets": buckets, "max_count": max_count})`.

    2. Update the existing `audit_page` handler from PLAN 04:
       - Same `timeline_buckets` call + max_count computation.
       - Add `buckets` and `max_count` to the template context dict.
       - Remove the `timeline_partial_available` key (no longer used).

    3. Import: `from ccguard.server.services.tool_use_service import list_events, timeline_buckets` (extend existing import).

    Create `tests/integration/test_audit_timeline_partial.py` covering every behavior bullet:
    - Auth: anonymous -> redirect/401.
    - Empty DB -> empty state copy present.
    - Seed 5 events in the most recent hour -> exactly 1 bar has `min-height: 2px`; 23 have `min-height: 0`.
    - Filter `tool_name=Bash` excludes Edit events from counts.
    - Filter `machine_id=laptop` substring-matches.
    - Response is a partial (no `<html>` or `<body>` in body).
    - Concurrent verification: `GET /audit` page already includes real bars (assert `min-height: 2px` present when DB has data, NOT just the placeholder).
    - HTMX wiring smoke: `GET /audit` body contains the literal strings `hx-get="/_partials/audit/timeline"`, `hx-trigger="every 30s"`, `hx-include="closest form"`.
  </action>
  <verify>
    <automated>cd /Users/timderbak/dev/ccguard &amp;&amp; python -m pytest tests/integration/test_audit_timeline_partial.py tests/integration/test_audit_page.py -x -q</automated>
  </verify>
  <done>
    All timeline partial tests + PLAN-04 page tests still green. Manual smoke: in a running server, `/audit` shows real bars at initial load; opening DevTools shows HTMX poll requests to `/_partials/audit/timeline` every 30s, with current filter form values appended as query string. TUA-03 fully delivered.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser HTMX poll -> /_partials/audit/timeline | Cookie-authenticated; GET request |
| Filter params (echoed by hx-include) -> SQL aggregation | User-controlled strings reach strftime GROUP BY query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-21 | Spoofing | /_partials/audit/timeline | mitigate | require_session cookie dep |
| T-01-22 | Tampering | filter params in raw strftime SQL | mitigate | Bind-param SQL in tool_use_service (already in PLAN 03); whitelist decision Literal in handler |
| T-01-23 | XSS via hour_label or aria-label | mitigate | Jinja2 autoescape on (.html); hour_label is server-formatted via strftime (no user input) |
| T-01-24 | Information Disclosure via bar tooltips | accept | Tooltip count is aggregated; no per-event details leaked |
| T-01-SC | Tampering | new packages | mitigate | No new packages introduced — N/A |
</threat_model>

<verification>
- `pytest tests/integration/test_audit_timeline_partial.py tests/integration/test_audit_page.py -x` green.
- Full suite `pytest tests/` green.
- Manual smoke: `/audit` shows bars; HTMX poll requests visible in browser network panel every 30s; filter values are appended to poll URL.
</verification>

<success_criteria>
- TUA-03 fully delivered: filter table + 24h timeline + HTMX polling all working with the locked UI-SPEC copy and visuals.
- No JS chart library introduced (CSS-only bars).
- All 4 task-1 templates + 2 routes covered by integration tests.
</success_criteria>

<output>
Create `.planning/phase-01-tool-use-audit-foundation/01-05-SUMMARY.md` when done.
</output>
