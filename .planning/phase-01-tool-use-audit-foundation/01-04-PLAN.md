---
phase: 01-tool-use-audit-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - src/ccguard/server/web/templates/base.html
  - src/ccguard/server/web/templates/audit_feed.html
  - src/ccguard/server/web/templates/components/_audit_events_table.html
  - src/ccguard/server/web/routes.py
  - tests/integration/test_audit_page.py
autonomous: true
requirements: [TUA-03]
must_haves:
  truths:
    - "Sidebar shows new 'Аудит' link between 'Находки' and 'Политика'"
    - "GET /audit (with admin session) renders the page with filter form, events table, and a placeholder for the timeline card"
    - "Filter form GET params machine_id/tool_name/decision/timeframe are honored and echoed in selected/value attrs"
    - "Empty filter result renders the 'Аудит-событий нет.' empty-state row"
    - "Each event row links machine_id[:12] to /machines/{full_id}"
    - "Russian copy in UI-SPEC § Copywriting is matched verbatim"
  artifacts:
    - path: "src/ccguard/server/web/templates/audit_feed.html"
      provides: "Main /audit page extending base.html"
      contains: "{% extends \"base.html\" %}"
    - path: "src/ccguard/server/web/templates/components/_audit_events_table.html"
      provides: "Events table partial (mirrors findings_feed table)"
      contains: "<table"
    - path: "src/ccguard/server/web/routes.py"
      provides: "GET /audit route + filter parsing"
      contains: "def audit_page"
  key_links:
    - from: "src/ccguard/server/web/routes.py audit_page"
      to: "src/ccguard/server/services/tool_use_service.list_events"
      via: "filter params → service call → template context"
      pattern: "list_events"
    - from: "templates/base.html"
      to: "/audit"
      via: "<a href=\"/audit\">Аудит</a>"
      pattern: "href=\"/audit\""
---

<objective>
Build the `/audit` page scaffold per UI-SPEC § File-Level Implementation Inventory: nav link, page template, events table partial, and the route handler that wires filters to `list_events`. The timeline card is rendered as an HTMX-polled container in this template but its inner partial is defined in PLAN 05 (PLAN 05 also adds the polling endpoint). For Wave 3 visual completeness, this plan includes an inline-empty placeholder so the page renders coherently even before PLAN 05 lands.

Purpose: TUA-03 first half. Static page rendering with full filter behaviour and Russian copy verbatim from UI-SPEC.
Output: navigable `/audit` page with working filter form + paginated events table; PLAN 05 fills the timeline card with real bars.
</objective>

<execution_context>
@/Users/timderbak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/timderbak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phase-01-tool-use-audit-foundation/01-CONTEXT.md
@.planning/phase-01-tool-use-audit-foundation/01-UI-SPEC.md
@.planning/phase-01-tool-use-audit-foundation/01-RESEARCH.md
@.planning/phase-01-tool-use-audit-foundation/01-03-PLAN.md

# Existing precedent — UI-SPEC mandates literal copy from these
@src/ccguard/server/web/templates/base.html
@src/ccguard/server/web/templates/findings_feed.html
@src/ccguard/server/web/templates/overview.html
@src/ccguard/server/web/routes.py
@tests/integration/test_web_smoke.py
@tests/integration/test_web_auth.py

<interfaces>
<!-- Consumed from PLAN 03 -->

From src/ccguard/server/services/tool_use_service.py:
- `list_events(session, *, machine_id_like, tool_name, decision, timeframe, limit=200) -> tuple[list[ToolUseEvent], int]`

From src/ccguard/server/web/routes.py:
- `require_session(...)` cookie-auth dependency (existing)
- Existing template render helper (look for the `templates.TemplateResponse` pattern in `findings_page`)
- `_csrf_for(request)` helper

From src/ccguard/server/api/deps.py:
- `get_session` SQLModel session dep
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Templates — base.html nav, audit_feed.html, events table partial</name>
  <files>
    src/ccguard/server/web/templates/base.html,
    src/ccguard/server/web/templates/audit_feed.html,
    src/ccguard/server/web/templates/components/_audit_events_table.html
  </files>
  <behavior>
    - `base.html` sidebar order is exactly: Обзор / Машины / Находки / Аудит / Политика / История / Настройки. New link uses class `block hover:bg-slate-800 px-3 py-2 rounded`, points to `/audit`, text `Аудит`.
    - All other existing nav entries unchanged (do not modify hrefs, classes, order of others).
    - `audit_feed.html` extends `base.html`, sets page title block to `ccguard — аудит`, renders `<h2 class="text-2xl font-semibold mb-6">Аудит</h2>`, then a filter form card, timeline card container, table card.
    - Filter form: GET to `/audit`, contains four controls with EXACT param names `machine_id`, `tool_name`, `decision`, `timeframe`; submit button `<button class="bg-slate-900 text-white text-sm rounded px-4 py-2">Фильтр</button>`; reset link `<a href="/audit" class="text-sm text-slate-500 hover:underline self-center">Сбросить</a>`.
    - Decision `<select>` options: `<option value="">все решения</option>`, then literal `allow`, `deny`, `error` values; the `selected` attribute is set when matching `filters.decision`.
    - Timeframe `<select>` options: `1h → за 1 час`, `24h → за 24 часа`, `7d → за 7 дней`; `24h` is selected by default.
    - Each `<input>` and `<select>` has `class="..."` ending in `focus:ring-2 focus:ring-slate-400 focus:border-slate-500` for accessibility per UI-SPEC.
    - Each input/select has a paired `<label class="sr-only" for="...">` (machine_id → `Машина`, tool_name → `Инструмент`, decision → `Решение`, timeframe → `Период`).
    - Timeline card: `<div class="bg-white rounded-lg shadow p-4 mb-4" hx-get="/_partials/audit/timeline" hx-trigger="every 30s" hx-include="closest form">` with inline include `{% include "components/_audit_timeline.html" %}` if the file exists, else a placeholder paragraph (Jinja `{% if exists ... %}` not possible directly — use a `timeline_partial_available` context var passed by the route handler; if False render placeholder `<p class="text-sm text-slate-400 py-6 text-center">Нет данных за выбранный период.</p>`). PLAN 05 creates the partial.
    - Events table: card class `bg-white rounded-lg shadow p-4`, includes `components/_audit_events_table.html`.
    - Table partial: `<thead>` with columns Когда / Машина / Инструмент / Решение / Результат / Fingerprint, `<tr class="text-sm text-slate-500">`, exact column-count.
    - Each row: `class="border-b last:border-0"`; cells per UI-SPEC table column spec; `machine_id` cell is a link `/machines/{event.machine_id}` showing `event.machine_id[:12]`; decision cell color-coded (`text-emerald-600` for allow, `text-red-600` for deny, `text-amber-600` for error); fingerprint cell `<td class="font-mono text-slate-500 py-2">{{ event.fingerprint[:10] }}</td>`; tool_name cell `<td class="font-mono py-2">{{ event.tool_name }}</td>`.
    - Empty-state row: `<tr><td colspan="6" class="py-6 text-center text-slate-400">Аудит-событий нет.</td></tr>`.
    - When `total > LIMIT`: footer row `<tfoot><tr><td colspan="6" class="py-3 text-center text-slate-400 text-sm">Показано {{ events|length }} из {{ total }} событий за период. Сузьте фильтры если нужно больше.</td></tr></tfoot>`.
    - Chart aria: container has `role="img" aria-label="Гистограмма аудит-событий по часам"` when the partial is rendered (PLAN 05 owns the actual bar render; the wrapping div with role+label lives in `audit_feed.html`'s timeline card OR is delegated to the partial — pick the partial in PLAN 05; in this plan the outer card has `hx-*` attrs only).
  </behavior>
  <action>
    **`base.html`** — locate the existing sidebar `<nav>` (in PRD it has Обзор/Машины/Находки/Политика/История/Настройки). Insert ONE new `<a>` between the Находки link and the Политика link:
    ```
    <a href="/audit" class="block hover:bg-slate-800 px-3 py-2 rounded">Аудит</a>
    ```
    Do not touch any other line.

    **`audit_feed.html`** — create per UI-SPEC § File-Level Implementation Inventory + Pattern 3 in RESEARCH. Render variables provided by the route handler:
    - `filters` (dict with keys `machine_id`, `tool_name`, `decision`, `timeframe`)
    - `events` (list of ToolUseEvent)
    - `total` (int)
    - `limit` (int = 200)
    - `timeline_partial_available` (bool; False until PLAN 05 ships, then True)

    Layout exactly per UI-SPEC § Page structure (heading → filter card → timeline card → table card). All Tailwind classes per UI-SPEC § Visual System. All copy from UI-SPEC § Copywriting verbatim (no paraphrasing).

    **`components/_audit_events_table.html`** — create new file. Render `<table class="w-full">` with `<thead>`/`<tbody>`. Columns and row classes exactly per UI-SPEC § Table columns. Include the `<tfoot>` row-count footer when `total > limit`.

    **Decision color helper** — inline `{% if event.decision == 'allow' %}text-emerald-600{% elif event.decision == 'deny' %}text-red-600{% else %}text-amber-600{% endif %}` for the Решение column. No new template filter needed.

    All Russian strings MUST be UTF-8 NFC (default). No escaping issues — Jinja autoescape is on for HTML templates (`templates.env.autoescape`).
  </action>
  <verify>
    <automated>cd /Users/timderbak/dev/ccguard && python -c "from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('src/ccguard/server/web/templates'), autoescape=True); env.get_template('audit_feed.html'); env.get_template('components/_audit_events_table.html'); env.get_template('base.html'); print('OK')"</automated>
  </verify>
  <done>
    All three templates parse without syntax errors. The nav insertion is the only change to base.html (verified by reading the diff). Russian strings from UI-SPEC § Copywriting present verbatim (grep for `Активность за 24 часа`, `Аудит-событий нет.`, `Сбросить`, `Фильтр`).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET /audit route + filter handling + integration tests</name>
  <files>
    src/ccguard/server/web/routes.py,
    tests/integration/test_audit_page.py
  </files>
  <behavior>
    - `GET /audit` without admin session → redirect to `/login` (existing `require_session` behavior).
    - `GET /audit` with admin session, empty DB → 200, body contains `Аудит-событий нет.`, contains `<h2 class="text-2xl font-semibold mb-6">Аудит</h2>`, contains `<a href="/audit" class="block hover:bg-slate-800 px-3 py-2 rounded">Аудит</a>` (sidebar link).
    - `GET /audit?machine_id=foo&tool_name=Bash&decision=deny&timeframe=7d` → filter values echoed: form input `value="foo"`, decision `<option value="deny" selected>`, timeframe `<option value="7d" selected>`.
    - `decision` is silently coerced to empty string if not in `{allow, deny, error}` (defensive; do NOT 422 — UI form may send empty).
    - `timeframe` is silently coerced to `24h` if not in `{1h, 24h, 7d}`.
    - With 5 seeded events in last 24h, default filter → table contains 5 rows. With seeded `tool_name="Bash"` and filter `tool_name=Edit` → table shows empty-state row.
    - With seeded 250 events → table shows 200 rows + the footer copy `Показано 200 из 250 событий за период.`
    - Each row's machine_id cell renders as link to `/machines/{full_id}` and shows the first 12 chars of the id.
    - Decision color classes present in HTML for each decision variant (assert via substring `text-emerald-600`, `text-red-600`, `text-amber-600`).
    - Timeframe filter `7d` actually expands the window — test by seeding an event 3 days old and asserting it appears with `7d` and not with `24h`.
    - Page title `<title>ccguard — аудит</title>` present.
  </behavior>
  <action>
    **Route handler in `src/ccguard/server/web/routes.py`** — add after the existing `findings_page` definition (mirror its pattern):
    ```
    @router.get("/audit", response_class=HTMLResponse)
    def audit_page(
        request: Request,
        machine_id: str = "",
        tool_name: str = "",
        decision: str = "",
        timeframe: str = "24h",
        session: Session = Depends(get_session),
        _admin = Depends(require_session),
    ) -> HTMLResponse:
        if decision not in ("allow", "deny", "error", ""):
            decision = ""
        if timeframe not in ("1h", "24h", "7d"):
            timeframe = "24h"
        events, total = list_events(
            session,
            machine_id_like=machine_id or None,
            tool_name=tool_name or None,
            decision=decision or None,
            timeframe=timeframe,
            limit=200,
        )
        return templates.TemplateResponse(
            "audit_feed.html",
            {
                "request": request,
                "filters": {"machine_id": machine_id, "tool_name": tool_name, "decision": decision, "timeframe": timeframe},
                "events": events,
                "total": total,
                "limit": 200,
                "timeline_partial_available": False,  # PLAN 05 flips this to True
            },
        )
    ```
    Imports: `from ccguard.server.services.tool_use_service import list_events`. Match the existing import block style.

    **Tests (`tests/integration/test_audit_page.py`)** — use existing `client` + `admin_session` fixtures (look in `conftest.py` for the cookie-auth fixture used by `test_web_smoke.py`):
    - Auth: GET /audit anonymous → 302 to /login (or 401 — match existing behavior).
    - Empty DB → 200, contains all the strings listed in `<behavior>`.
    - Seed 5 ToolUseEvent rows directly via session, GET /audit → table has 5 `<tr class="border-b...">`.
    - Filter echoes: GET with query string → assert form re-renders with selected/value attributes.
    - Filter behavior: tool_name mismatch → empty-state row.
    - Pagination footer: seed 250 → 200 rows + footer string.
    - Decision color classes present.
    - Timeframe expansion: seed event 3 days old → only visible with `timeframe=7d`.
    - Sidebar link present in rendered HTML.
    - Russian copy regression: assert `Активность за 24 часа` AND `Сбросить` AND `Фильтр` are all in the response body.
  </action>
  <verify>
    <automated>cd /Users/timderbak/dev/ccguard && python -m pytest tests/integration/test_audit_page.py -x -q</automated>
  </verify>
  <done>
    All audit_page tests green; full suite still green; navigating to `/audit` in a running server returns a properly styled page; PLAN 05 will replace the placeholder timeline with a real bar chart.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → /audit | Cookie-authenticated session; CSRF not required for GET |
| Filter query params → SQL | User-controlled strings reach LIKE filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-17 | Spoofing | /audit | mitigate | `require_session` cookie dep (existing) |
| T-01-18 | Tampering | filter params → SQL | mitigate | Pydantic/FastAPI query coercion + parameterized SQL via `LIKE %?%` bind param in service; decision/timeframe whitelisted to fixed strings in handler |
| T-01-19 | XSS via tool_name in rendered table | mitigate | Jinja2 autoescape on (default for `.html`); test asserts payload like `<script>` is escaped |
| T-01-20 | Information Disclosure | rendered fingerprints | accept | Fingerprint is a non-secret grouping ID per RESEARCH Pitfall #5; design choice |
| T-01-SC | Tampering | new packages | mitigate | No new packages — N/A |
</threat_model>

<verification>
- `pytest tests/integration/test_audit_page.py -x` green.
- `pytest tests/` full suite green.
- Manually start server (`ccguard-server`), browse to `/audit` while logged in — page renders with sidebar link active, filter form, placeholder timeline, table.
- `grep -n "Аудит" src/ccguard/server/web/templates/base.html` shows exactly one new occurrence.
</verification>

<success_criteria>
- TUA-03 page scaffold complete: filter form behaves per spec, table renders correctly, empty/full/overflow states all covered.
- All Russian UI-SPEC copy present verbatim.
- PLAN 05 has a hook: the timeline card `<div hx-get="/_partials/audit/timeline">` is in place, awaiting partial.
</success_criteria>

<output>
Create `.planning/phase-01-tool-use-audit-foundation/01-04-SUMMARY.md` when done.
</output>
