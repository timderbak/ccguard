---
phase: 02-anomaly-detection
plan: 06
type: execute
wave: 6
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
  - 02-05
files_modified:
  - tests/integration/test_anomaly_routes.py
  - tests/integration/test_scheduler_tick.py
  - tests/unit/test_anomaly_edge_cases.py
autonomous: true
requirements:
  - ANO-01
  - ANO-02
  - ANO-03
must_haves:
  truths:
    - "All Phase 1 tests remain green (356 baseline)"
    - "Phase 2 ships at least 30 new tests across unit + integration + edge-case + regression layers"
    - "Integration tests confirm tick() emits findings end-to-end and routes render those findings"
    - "Scheduler is mocked or invoked directly in tests (never via real APScheduler timer) — TestClient never hangs"
    - "Routes for /anomalies, /anomalies/{m}/{metric}, /_partials/anomalies/overview, /_partials/anomalies/matrix return 200 (auth provided) or 404 (unknown metric)"
    - "Edge cases covered: warm-up suppression, same-day dedup, different-day re-emit, stdev=0 degenerate case, unknown machine in URL"
  artifacts:
    - path: "tests/integration/test_anomaly_routes.py"
      provides: "Route-level integration tests (auth + render + finding visibility)"
    - path: "tests/integration/test_scheduler_tick.py"
      provides: "End-to-end: seed data, call tick(), assert finding emitted, assert route shows it"
    - path: "tests/unit/test_anomaly_edge_cases.py"
      provides: "Edge cases beyond the per-module test files"
  key_links:
    - from: "test_scheduler_tick.py"
      to: "anomaly_service.tick"
      via: "direct call (NOT via APScheduler timer)"
      pattern: "from ccguard.server.services.anomaly_service import tick"
---

<objective>
Phase 2 quality gate: prove the slice end-to-end without flake. Plans 02-01..02-05 each ship their own focused tests. This plan adds the integration glue and the edge cases that span multiple modules, plus a regression sweep confirming all 356 Phase 1 tests still pass.

Purpose: A solo developer cannot afford a flaky scheduler test that hangs CI. This plan locks in (a) direct-call invocation of tick() in tests, never the real APScheduler timer, and (b) a route-level harness that seeds a finding then asserts it appears in /anomalies and /_partials/anomalies/overview.

Output: Three new test files totaling at least 12 tests (combined with the 18+ in 02-01..02-03 and any added by 02-04..02-05 manual checks, total ≥30 new tests for Phase 2).
</objective>

<execution_context>
@/Users/timderbak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/timderbak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phase-02-anomaly-detection/02-CONTEXT.md
@.planning/phases/02-anomaly-detection/02-01-SUMMARY.md
@.planning/phases/02-anomaly-detection/02-02-SUMMARY.md
@.planning/phases/02-anomaly-detection/02-03-SUMMARY.md
@.planning/phases/02-anomaly-detection/02-04-SUMMARY.md
@.planning/phases/02-anomaly-detection/02-05-SUMMARY.md

@tests/conftest.py
@src/ccguard/server/services/anomaly_service.py
@src/ccguard/server/services/anomaly_constants.py
@src/ccguard/server/db/models.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integration tests for anomaly routes (auth, render, empty-state, 404)</name>
  <files>tests/integration/test_anomaly_routes.py</files>
  <behavior>
    Use the existing integration test pattern (TestClient + authed-admin fixture, look at tests/integration/test_audit_routes.py or similar Phase 1 file as a template).

    - Test: unauthenticated GET /anomalies returns redirect-to-login or 401 (matches Phase 1 convention).
    - Test: authed GET /anomalies returns 200 and HTML contains the literal "Аномалии" heading.
    - Test: authed GET /_partials/anomalies/overview with no anomaly findings returns 200 and contains "Аномалий нет."
    - Test: seed a FindingRecord with rule_id="anomaly.bash_calls_per_day", machine_id="mtest", payload_json containing observed_value/sigma_distance/mean/stdev — then GET /_partials/anomalies/overview returns 200 and HTML contains "mtest"[:12] and "bash_calls_per_day".
    - Test: authed GET /_partials/anomalies/matrix with no Machine rows returns 200 and contains "Машин нет."
    - Test: seed a Machine and a MachineBaseline(baseline_ready=False) — matrix partial returns 200 and contains "накопление" for that cell.
    - Test: seed a Machine and a MachineBaseline with baseline_ready=True, recent_points_json with the last value > mean+3*stdev — matrix shows "выброс" badge.
    - Test: GET /anomalies/mtest/unknown_metric returns 404.
    - Test: GET /anomalies/mtest/bash_calls_per_day returns 200 and contains "Все аномалии" back link and "Baseline" card.
    - Test: GET /anomalies/mtest/bash_calls_per_day with no MachineBaseline row returns 200 and contains "Недостаточно данных для baseline".
  </behavior>
  <action>Create tests/integration/test_anomaly_routes.py. Use whatever authed-client fixture Phase 1 already provides (read tests/conftest.py — if there is no admin-login helper, build one inline using the same auth flow Phase 1 integration tests use). Seed DB rows via the same session fixture used by Phase 1 integration tests. ≥10 tests.</action>
  <verify>
    <automated>pytest tests/integration/test_anomaly_routes.py -x -q</automated>
  </verify>
  <done>≥10 route-level tests pass; covers auth, empty state, populated state, warm-up cell, outlier cell, unknown-metric 404, drill-down empty-baseline state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scheduler tick integration test (direct call, no real timer)</name>
  <files>tests/integration/test_scheduler_tick.py</files>
  <behavior>
    - Test: seed Machine "m1" + 14 days of ToolUseEvent rows (tool_name=Bash) where the last day has a count > mean+3*stdev of the prior 13 days. Call anomaly_service.tick(session) directly. Assert: (a) a MachineBaseline row exists with baseline_ready=True; (b) a FindingRecord with rule_id="anomaly.bash_calls_per_day", machine_id="m1", severity="warn", inventory_id IS NULL exists.
    - Test: call tick(session) twice in a row on the same data — second call produces 0 new findings (same-day dedup).
    - Test: seed only 5 days of data — tick produces a MachineBaseline with baseline_ready=False and 0 findings.
    - Test: with the finding from the first test in place, call GET /_partials/anomalies/overview via TestClient — response HTML contains "m1"[:12] and "bash_calls_per_day". (End-to-end: tick wrote it, route renders it.)
    - Test: tick handles a machine whose aggregator raises (monkeypatch one aggregator to raise) — tick completes without raising, summary dict includes the error, other machines still processed.
    - Test: confirm CCGUARD_DISABLE_SCHEDULER=1 is set in the test process (assert via os.environ) and that importing the FastAPI app does NOT start any APScheduler thread (check `app.state.scheduler` is unset OR the scheduler's `running` attribute is False).
  </behavior>
  <action>Create tests/integration/test_scheduler_tick.py. Import anomaly_service.tick directly. Do NOT import build_scheduler or start_scheduler — those are tested only by import side-effects (Task 2 in 02-03). The end-to-end test uses TestClient + the authed-admin fixture. ≥6 tests.</action>
  <verify>
    <automated>pytest tests/integration/test_scheduler_tick.py -x -q</automated>
  </verify>
  <done>tick() proven to produce findings on real outliers; dedup proven; warm-up suppression proven; route-level visibility of tick output proven; env guard confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cross-cutting edge cases + Phase 1 regression sweep</name>
  <files>tests/unit/test_anomaly_edge_cases.py</files>
  <behavior>
    - Test: stdev=0 degenerate (14 identical points). Latest point equals mean -> not an outlier -> no finding. Latest point differs by 1 -> outlier (since any deviation from a flat history > 0*sigma).
    - Test: 14 zero points -> mean=0, stdev=0, latest=0 -> not an outlier -> no finding (proves "calm" machines stay quiet).
    - Test: 14-day window with exactly 7 nonzero points spread across days -> sample_count=14, baseline_ready=True (we don't gate on non-zero count, only window length per locked decision).
    - Test: clock-rollover edge — call evaluate_one near UTC midnight (simulate via freezegun or by patching datetime.now). Same-day dedup uses func.date(discovered_at) so a finding at 23:59 UTC and another at 00:01 UTC the next day are NOT considered duplicates.
    - Test: empty Machine table — tick() returns {machines_evaluated: 0, findings_emitted: 0, errors: []}.
    - Test: rule_id_for("bash_calls_per_day") returns "anomaly.bash_calls_per_day" — sanity check the contract used by routes.
  </behavior>
  <action>Create tests/unit/test_anomaly_edge_cases.py. Use freezegun if already in test deps (check pyproject); otherwise use unittest.mock.patch to patch datetime.now inside anomaly_service. ≥6 tests.</action>
  <verify>
    <automated>pytest tests/unit/test_anomaly_edge_cases.py -x -q &amp;&amp; pytest tests/ -x -q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>≥6 edge-case tests pass; full pytest suite (Phase 1 + Phase 2) passes; final total test count is ≥356 + ≥30 new = ≥386.</done>
</task>

</tasks>

<verification>
- Full suite: `pytest tests/ -v` — all green, no hangs (CCGUARD_DISABLE_SCHEDULER=1 enforced by conftest).
- Test count: `pytest tests/ --collect-only -q | tail -1` reports ≥386 tests (was 356 in Phase 1).
- Per-layer counts:
  - Unit (per 02-01/02/03/06): ≥30 new unit tests across test_machine_baseline_model.py, test_metric_aggregators.py, test_baseline_service.py, test_anomaly_service.py, test_anomaly_edge_cases.py.
  - Integration (per 02-06): ≥16 integration tests across test_anomaly_routes.py, test_scheduler_tick.py.
- No Phase 1 audit code path touched: `git diff --stat HEAD~N src/ccguard/server/api/audit.py src/ccguard/server/services/tool_use_service.py` shows zero modifications.
</verification>

<success_criteria>
- Phase 1 tests stay green (356 baseline preserved).
- ≥30 new Phase 2 tests pass.
- Integration confirms tick → finding → route render chain works end-to-end.
- Scheduler env-guard confirmed in tests; no flaky timer-driven assertions.
- Unknown metric returns 404; warm-up suppresses findings; same-day dedup works; different-day re-emission works.
</success_criteria>

<output>
Create `.planning/phases/02-anomaly-detection/02-06-SUMMARY.md` — final test counts (per file and total), confirmation that Phase 1 audit code paths were not modified, and any flake notes for future maintainers.
</output>
