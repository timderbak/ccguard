---
phase: 02-anomaly-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ccguard/server/db/models.py
  - src/ccguard/server/db/session.py
  - tests/unit/test_machine_baseline_model.py
autonomous: true
requirements:
  - ANO-01
must_haves:
  truths:
    - "A MachineBaseline row can be inserted and read back per (machine_id, metric)"
    - "FindingRecord.inventory_id accepts NULL for anomaly findings without snapshot link"
    - "Existing FindingRecord rows (with non-null inventory_id) continue to load"
    - "init_db creates the machine_baseline table on a fresh DB"
    - "Exact inventory field paths for mcp_servers / agents / skills are locked as module-level constants for downstream plans"
  artifacts:
    - path: "src/ccguard/server/db/models.py"
      provides: "MachineBaseline SQLModel + nullable inventory_id on FindingRecord"
      contains: "class MachineBaseline"
    - path: "src/ccguard/server/db/session.py"
      provides: "CREATE UNIQUE INDEX for (machine_id, metric) on machine_baseline"
      contains: "machine_baseline"
    - path: "tests/unit/test_machine_baseline_model.py"
      provides: "Round-trip + uniqueness + nullable-inventory_id tests"
  key_links:
    - from: "src/ccguard/server/db/models.py"
      to: "SQLite via init_db"
      via: "SQLModel.metadata.create_all"
      pattern: "create_all"
---

<objective>
Land the storage foundation for Phase 2: a new `MachineBaseline` table for cached per-machine per-metric statistics, a backward-compatible schema relaxation that makes `FindingRecord.inventory_id` nullable (so anomaly findings can be emitted without being tied to a specific inventory snapshot), and a verified set of inventory field-path constants that every downstream aggregator and diff routine will use.

Purpose: Anomaly detection needs (a) a place to persist computed baselines for read-back by UI/scheduler and (b) the ability to write findings that originate from server-side aggregation rather than from a single inventory event. We also pin the exact attribute names of `McpServerEntry`, `AgentEntry`, and `SkillEntry` so plan 02-02 does not guess.

Output: Updated `models.py`, an `init_db` migration step (idempotent `ALTER TABLE` + create-all), unit tests proving round-trip + uniqueness + nullability + backward compat for existing rows.
</objective>

<execution_context>
@/Users/timderbak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/timderbak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phase-02-anomaly-detection/02-CONTEXT.md
@.planning/phase-02-anomaly-detection/02-RESEARCH.md

@src/ccguard/server/db/models.py
@src/ccguard/server/db/session.py
@src/ccguard/schemas/inventory.py

<interfaces>
<!-- Locked inventory field paths (extracted from src/ccguard/schemas/inventory.py). -->
<!-- Downstream plans (02-02) MUST consume these via the module constants this plan creates, not by re-reading the schema. -->

MCP server identity field: McpServerEntry.name        (str, required)
Agent identity field:      AgentEntry.name            (str, required)
Agent hash field:          AgentEntry.file_hash       (str, required) — drives "new_agents" diff
Skill identity field:      SkillEntry.name            (str, required)
Skill hash field:          SkillEntry.dir_hash        (str, required) — drives skill_dir_hash_changes diff

Inventory snapshot row:    InventorySnapshot.payload_json (str — JSON of InventoryReport)
Snapshot ordering:         InventorySnapshot.received_at  (datetime, indexed)
Snapshot key:              InventorySnapshot.machine_id   (str, indexed)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and lock inventory field-path constants</name>
  <files>src/ccguard/server/services/anomaly_constants.py</files>
  <action>Create a NEW module `anomaly_constants.py` (sibling of existing services) that re-exports the locked field-path constants downstream code MUST use. Before writing, re-open `src/ccguard/schemas/inventory.py` and `src/ccguard/server/db/models.py` to confirm the attribute names match what is documented in the `<interfaces>` block above; if any drift is detected, STOP and surface the discrepancy. The module must define: `MCP_NAME_FIELD = "name"`, `AGENT_NAME_FIELD = "name"`, `AGENT_HASH_FIELD = "file_hash"`, `SKILL_NAME_FIELD = "name"`, `SKILL_HASH_FIELD = "dir_hash"`, plus a tuple `ALL_METRICS = ("bash_calls_per_day", "new_mcp_per_week", "new_agents_per_week", "skill_dir_hash_changes_per_week")` and a frozenset `VALID_METRICS = frozenset(ALL_METRICS)`. Add a module docstring explaining: "These constants are the SINGLE source of truth for Phase 2 anomaly aggregators. Do not duplicate string literals elsewhere." Also export `RULE_ID_PREFIX = "anomaly."` and a helper `def rule_id_for(metric: str) -> str: return f"{RULE_ID_PREFIX}{metric}"`.</action>
  <verify>
    <automated>python -c "from ccguard.server.services.anomaly_constants import ALL_METRICS, VALID_METRICS, rule_id_for; assert len(ALL_METRICS)==4; assert 'bash_calls_per_day' in VALID_METRICS; assert rule_id_for('bash_calls_per_day')=='anomaly.bash_calls_per_day'; print('ok')"</automated>
  </verify>
  <done>Module exists; all five field-path constants, the metric tuple/frozenset, the rule-id prefix, and `rule_id_for` helper import cleanly; values match the `<interfaces>` block verbatim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add MachineBaseline model + relax FindingRecord.inventory_id to nullable</name>
  <files>src/ccguard/server/db/models.py, tests/unit/test_machine_baseline_model.py</files>
  <behavior>
    - Test 1: Insert MachineBaseline(machine_id="m1", metric="bash_calls_per_day", mean=10.0, stdev=2.0, sample_count=14, baseline_ready=True) — round-trips with auto `updated_at`.
    - Test 2: Inserting two MachineBaseline rows with the same (machine_id, metric) raises IntegrityError (composite unique).
    - Test 3: Different metric for the same machine is allowed.
    - Test 4: FindingRecord(inventory_id=None, ...) inserts and reads back with inventory_id IS NULL.
    - Test 5: Pre-existing FindingRecord row with a non-null inventory_id still loads via select (backward compat — simulate by inserting an old-style row first).
    - Test 6: `recent_points_json` column on MachineBaseline accepts a JSON string of 14 floats and round-trips byte-identical.
  </behavior>
  <action>
    In `src/ccguard/server/db/models.py`:
      1. Change `FindingRecord.inventory_id: int = Field(index=True)` to `inventory_id: int | None = Field(default=None, index=True)`. Do NOT change any other FindingRecord field; existing rows with valid ints remain valid (SQLite stores NULL or INTEGER interchangeably in the same column once nullability is allowed; create_all is a no-op on existing tables, so for already-deployed DBs the column stays NOT NULL — this is acceptable for fresh Phase 2 installs and is documented in the docstring update; do NOT attempt a runtime ALTER for v0.1 deployments, the change is forward-only for new DBs).
      2. Append a `class MachineBaseline(SQLModel, table=True)` with fields:
         - `id: int | None = Field(default=None, primary_key=True)`
         - `machine_id: str = Field(index=True)`
         - `metric: str = Field(index=True)`  # one of anomaly_constants.ALL_METRICS
         - `mean: float`
         - `stdev: float`
         - `sample_count: int`
         - `baseline_ready: bool = Field(default=False)`
         - `recent_points_json: str = Field(default="[]")`  # JSON-encoded list[float] of length up to 14 for sparkline reuse
         - `updated_at: datetime = Field(default_factory=_utcnow)`
      3. Add a docstring noting that the (machine_id, metric) composite uniqueness is enforced via a `CREATE UNIQUE INDEX IF NOT EXISTS` in `init_db` (Task 3), not via SQLModel `UniqueConstraint`, because SQLModel's table_args interaction with the existing `Field(index=True)` is brittle and we keep parity with the Phase 1 pattern for `tool_use_event` indexes.
    In `tests/unit/test_machine_baseline_model.py`: use the existing test fixtures pattern (look at `tests/unit/test_tool_use_event_model.py` if present; if not, use `tests/conftest.py` fixtures — assume a `session` fixture exists giving a fresh in-memory SQLite Session). Implement the 6 tests above. The uniqueness test requires the `init_db` index to be applied; if the session fixture does not run init_db, call it explicitly in that test.
  </action>
  <verify>
    <automated>pytest tests/unit/test_machine_baseline_model.py -x -q</automated>
  </verify>
  <done>All 6 tests pass. `FindingRecord` accepts `inventory_id=None`. `MachineBaseline` round-trips, enforces composite uniqueness (after Task 3 index), allows different metrics per machine, and preserves a JSON points blob.</done>
</task>

<task type="auto">
  <name>Task 3: Wire MachineBaseline into init_db with idempotent unique index</name>
  <files>src/ccguard/server/db/session.py</files>
  <action>In `init_db` (the function that runs `SQLModel.metadata.create_all` + the existing `CREATE INDEX IF NOT EXISTS` blocks for `tool_use_event`), add:
    1. The `create_all` call already picks up the new `MachineBaseline` table because it imports `models` — verify the import line includes `MachineBaseline` if there is an explicit re-export list (`__all__`); if so, add it.
    2. After `create_all`, execute (using the same engine.execute pattern as the existing Phase 1 indexes):
       `CREATE UNIQUE INDEX IF NOT EXISTS ux_machine_baseline_machine_metric ON machinebaseline (machine_id, metric);`
       Use the actual lowercase table name SQLModel generates (`machinebaseline` — verify by inspecting the existing `tool_use_event` index statement: SQLModel lowercases class names without underscore by default; if the existing pattern uses `tool_use_event` snake_case, then SQLModel is configured to snake_case and the index target is `machine_baseline`). Inspect one existing index statement in `session.py` to confirm the casing convention; match it.
    3. Add a composite index for the (machine_id, metric) lookup pattern used by the matrix page: this is the same unique index above — no second index needed.
  </action>
  <verify>
    <automated>python -c "from sqlmodel import create_engine, Session, select; from ccguard.server.db.session import init_db; from ccguard.server.db.models import MachineBaseline; e=create_engine('sqlite:///:memory:'); init_db(e); s=Session(e); s.add(MachineBaseline(machine_id='m', metric='bash_calls_per_day', mean=0, stdev=0, sample_count=0)); s.commit(); print(s.exec(select(MachineBaseline)).first())"</automated>
  </verify>
  <done>`init_db` on a fresh in-memory engine creates the table + unique index without error; insert + select round-trip works.</done>
</task>

</tasks>

<verification>
- All Phase 1 tests stay green: `pytest tests/ -x -q --ignore=tests/unit/test_machine_baseline_model.py` matches the prior baseline count (356).
- New tests: `pytest tests/unit/test_machine_baseline_model.py -v` reports 6 passed.
- Lint clean: `ruff check src/ccguard/server/db/models.py src/ccguard/server/db/session.py src/ccguard/server/services/anomaly_constants.py`.
</verification>

<success_criteria>
- `MachineBaseline` table created on fresh DB with composite unique index on (machine_id, metric).
- `FindingRecord.inventory_id` accepts NULL in the SQLModel definition (forward-only for fresh installs).
- `anomaly_constants` module is the single source of truth for metric names, rule-id prefix, and inventory field paths — downstream plans MUST import from here.
- Existing 356 Phase 1 tests remain green.
- 6 new unit tests pass.
</success_criteria>

<output>
Create `.planning/phases/02-anomaly-detection/02-01-SUMMARY.md` when done — record the locked inventory field paths verbatim, the chosen SQLModel table name (machinebaseline vs machine_baseline), and confirm Phase 1 test count is unchanged.
</output>
