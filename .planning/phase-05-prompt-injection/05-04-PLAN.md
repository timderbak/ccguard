---
phase: 05-prompt-injection
plan: 04
type: execute
wave: 3
depends_on: [05-01]
files_modified:
  - src/ccguard/agent/findings_hook/__init__.py
  - src/ccguard/agent/findings_hook/buffer.py
  - src/ccguard/agent/findings_hook/flusher.py
  - src/ccguard/agent/findings_hook/flusher_main.py
  - src/ccguard/server/api/findings.py
  - tests/agent/test_findings_hook_buffer.py
  - tests/agent/test_findings_hook_flusher.py
autonomous: true
requirements: [PI-01, PI-03]

must_haves:
  truths:
    - "emit_finding() пишет запись в локальный SQLite WAL буфер `~/.ccguard/findings_buffer.db` менее чем за 10 ms и не блокирует hot path"
    - "Flusher subprocess читает буфер, POST'ит batch'ем на `/api/v1/findings`, помечает delivered и оставляет fail'd для retry"
    - "Сервер принимает batch и сохраняет в существующую Finding-таблицу (из v0.1) — НЕ создаём новую таблицу"
    - "mask_secrets() применяется к matched_pattern ПЕРЕД persist в буфер"
    - "Симметрия с audit_hook/ — buffer.py + flusher.py + flusher_main.py — clone-not-extend per D-1"
  artifacts:
    - path: "src/ccguard/agent/findings_hook/buffer.py"
      provides: "emit_finding() local SQLite write"
      contains: "def emit_finding"
    - path: "src/ccguard/agent/findings_hook/flusher.py"
      provides: "flush() reads buffer + POSTs to server"
      contains: "def flush"
    - path: "src/ccguard/agent/findings_hook/flusher_main.py"
      provides: "CLI entrypoint for detached subprocess"
    - path: "src/ccguard/server/api/findings.py"
      provides: "POST /api/v1/findings batch endpoint (или extension if exists)"
  key_links:
    - from: "emit_finding"
      to: "~/.ccguard/findings_buffer.db"
      via: "sqlite3.connect WAL mode"
      pattern: "PRAGMA journal_mode=WAL"
    - from: "flusher"
      to: "POST /api/v1/findings"
      via: "httpx.Client с X-CCGuard-Token header"
      pattern: "/api/v1/findings"
---

<objective>
Создать `findings_hook/` модуль — clone of `audit_hook/` pattern (per locked D-1) для async-emit findings без блокировки hot-path. Один buffer-таблица с FindingRecord shape, отдельный flusher subprocess.

Purpose: Hot-path emit_finding (вызывается из enforce.decide() план 03) должен быть <10 ms. Network POST уходит в детач'ный subprocess. Это разделение proven на Phase 1.

Output:
- `findings_hook/{__init__.py, buffer.py, flusher.py, flusher_main.py}`
- Server endpoint `POST /api/v1/findings` (batch) — если ещё не существует; иначе extend
- Unit tests для buffer и flusher
</objective>

<execution_context>
@/Users/timderbak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/timderbak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phase-05-prompt-injection/05-CONTEXT.md
@.planning/phase-05-prompt-injection/05-RESEARCH.md
@src/ccguard/agent/audit_hook/buffer.py
@src/ccguard/agent/audit_hook/flusher.py
@src/ccguard/agent/audit_hook/flusher_main.py
@src/ccguard/agent/masking.py
@src/ccguard/schemas/finding.py
@src/ccguard/server/api/findings.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: findings_hook/buffer.py — clone audit_hook/buffer.py с Finding schema</name>
  <files>src/ccguard/agent/findings_hook/__init__.py, src/ccguard/agent/findings_hook/buffer.py, tests/agent/test_findings_hook_buffer.py</files>
  <behavior>
    - Test 1: emit_finding() создаёт `~/.ccguard/findings_buffer.db` если не существует; SQLite в WAL mode
    - Test 2: emit_finding(rule_id="prompt_injection.role_swap", severity="warn", title="...", source="regex", matched_pattern="...", tool_name="Bash") вставляет ровно одну строку в таблицу findings_buffer
    - Test 3: вызов < 10 ms (per-call assert)
    - Test 4: matched_pattern проходит через mask_secrets() — если содержит "sk-xxx..." API key shape → в буфере хранится маскированная версия
    - Test 5: matched_pattern truncated до 200 chars (если ввели длиннее)
    - Test 6: Идемпотентность — повторный вызов init не пересоздаёт таблицу
    - Test 7: WAL journal_mode подтверждён через `PRAGMA journal_mode` → "wal"
    - Test 8: Schema-столбцы: id (autoinc), ts (ISO timestamp utc), rule_id, severity, title, source, matched_pattern, tool_name, delivered (BOOL default 0), retry_count (INT default 0). Минимально достаточный для server POST.
    - Test 9: emit_finding принимает kwargs только (positional args не принимаются — defensive против shape drift)
    - Test 10: HOME-override через env var `CCGUARD_HOME` (если установлен — пишет туда вместо `~/.ccguard`), для test isolation
  </behavior>
  <action>
    Скопировать `src/ccguard/agent/audit_hook/buffer.py` в `src/ccguard/agent/findings_hook/buffer.py` (per D-1: clone, not extend). Адаптировать:

    1) Создать `src/ccguard/agent/findings_hook/__init__.py` (пустой, маркер пакета).

    2) В `buffer.py`:
    - Table name: `findings_buffer` (не `audit_buffer`)
    - DB filename: `findings_buffer.db` в `~/.ccguard/` (или `$CCGUARD_HOME/`)
    - Schema CREATE TABLE:
      ```
      CREATE TABLE IF NOT EXISTS findings_buffer (
          id INTEGER PRIMARY KEY AUTOINCREMENT,
          ts TEXT NOT NULL,
          rule_id TEXT NOT NULL,
          severity TEXT NOT NULL,
          title TEXT NOT NULL,
          source TEXT NOT NULL,
          matched_pattern TEXT NOT NULL,
          tool_name TEXT NOT NULL,
          delivered INTEGER NOT NULL DEFAULT 0,
          retry_count INTEGER NOT NULL DEFAULT 0
      );
      CREATE INDEX IF NOT EXISTS idx_findings_buffer_undelivered
          ON findings_buffer(delivered) WHERE delivered = 0;
      ```
    - `PRAGMA journal_mode=WAL` на init
    - Public API: ТОЛЬКО kwargs (per test 9):
      ```
      def emit_finding(*, rule_id: str, severity: str, title: str,
                       source: str, matched_pattern: str, tool_name: str) -> None:
          masked = mask_secrets(matched_pattern)[:200]
          ts = datetime.now(timezone.utc).isoformat()
          conn = _get_conn()  # cached connection
          conn.execute(
              "INSERT INTO findings_buffer (ts, rule_id, severity, title, source, matched_pattern, tool_name) "
              "VALUES (?, ?, ?, ?, ?, ?, ?)",
              (ts, rule_id, severity, title, source, masked, tool_name),
          )
          conn.commit()
      ```
    - `_get_conn()` — кешированный module-level connection (см. audit_hook/buffer.py паттерн)

    3) Import `from ccguard.agent.masking import mask_secrets` (existing util из Phase 3 per RESEARCH §"Pitfall 7"). Если модуль/функция называется иначе — найти grep'ом по `def mask_secrets` или `def mask`; использовать существующую.

    4) Создать `tests/agent/test_findings_hook_buffer.py`:
    - Использовать `tmp_path` + monkeypatch `CCGUARD_HOME` env var для изоляции
    - Тест 4 (mask_secrets): передать matched_pattern содержащий `sk-ant-api03-xxx` shape — assert в DB хранится маскированный
    - Тест 7: `cur.execute("PRAGMA journal_mode").fetchone()[0]` == "wal"
    - Тест 3 (latency): `time.perf_counter` around emit_finding; < 0.010s
  </action>
  <verify>
    <automated>pytest tests/agent/test_findings_hook_buffer.py -x</automated>
  </verify>
  <done>
    - findings_hook/ package создан
    - emit_finding() работает < 10ms
    - WAL mode подтверждён
    - mask_secrets() применён, truncation до 200
    - 10 тестов зелёные
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: findings_hook/flusher.py + flusher_main.py + server endpoint extension</name>
  <files>src/ccguard/agent/findings_hook/flusher.py, src/ccguard/agent/findings_hook/flusher_main.py, src/ccguard/server/api/findings.py, tests/agent/test_findings_hook_flusher.py</files>
  <behavior>
    - Test 1 (flush success): mock httpx POST → 200; flush() читает все undelivered, отправляет batch'ем (один POST со списком), помечает delivered=1
    - Test 2 (flush network error): mock → ConnectError; flush() инкрементирует retry_count для всех затронутых, оставляет delivered=0
    - Test 3 (flush server 5xx): mock → 503; retry_count++; delivered=0
    - Test 4 (flush 4xx — invalid payload): mock → 400; retry_count++ но ≥ 3 → mark delivered=1 (DLQ-style — не зацикливаемся на bad payload). Pragmatic.
    - Test 5 (empty buffer): flush() — no-op, exit 0
    - Test 6 (batch size limit): 500 records в буфере → flush ОТПРАВЛЯЕТ batch (или несколько batches ≤ 100 per request); все помечены delivered
    - Test 7 (flusher_main entry): `python -m ccguard.agent.findings_hook.flusher_main` — запускается, делает один flush, выходит (oneshot mode; long-running daemon отложен — match audit_hook)
    - Test 8 (X-CCGuard-Token header): POST содержит auth header из existing settings (как audit flusher)
    - Test 9 (server endpoint accepts batch): integration с TestClient — POST /api/v1/findings с JSON array → 200; записи появляются в Finding таблице (existing v0.1 table)
  </behavior>
  <action>
    1) Скопировать `src/ccguard/agent/audit_hook/flusher.py` в `findings_hook/flusher.py`. Адаптировать:
    - Source table: `findings_buffer` (не audit_buffer)
    - Target endpoint: `/api/v1/findings` (не /api/v1/audit)
    - Payload shape: `[{"ts": ..., "rule_id": ..., "severity": ..., "title": ..., "source": ..., "matched_pattern": ..., "tool_name": ...}, ...]` — соответствует FindingRecord (или Pydantic FindingCreate schema на сервере)
    - Batch size: 100 records per POST (matches audit pattern)
    - DLQ-like guard: если retry_count >= 3 → mark delivered=1 (per behavior test 4) — НЕ оставляем зацикленные failures

    2) Скопировать `audit_hook/flusher_main.py` в `findings_hook/flusher_main.py`. Адаптировать: вызывает `flush()` из flusher.py, не from audit_hook.

    3) Server endpoint `src/ccguard/server/api/findings.py`:
    - Сначала grep: `grep -n "/api/v1/findings" src/ccguard/server/` чтобы понять есть ли endpoint
    - Если есть и принимает single record → расширить: добавить отдельный handler для batch (List[FindingCreate]) ИЛИ изменить существующий принимать union (single dict или list of dicts) с backward-compat
    - Если НЕТ → создать router с `@router.post("/findings", status_code=201) async def create_findings_batch(records: list[FindingCreate], request: Request, db: Session = Depends(get_db))`:
        * Validate X-CCGuard-Token header (как в audit endpoint)
        * Resolve machine_id из token (existing pattern)
        * Bulk insert в существующую `Finding` таблицу (v0.1) — НЕТ новых таблиц per RESEARCH §"Project Constraints"
        * Return `{"accepted": len(records)}`
    - НЕ менять Finding SQLModel схему — используем существующие поля (rule_id, severity, title, description=matched_pattern, source, machine_id, ts). Если каких-то полей нет — расхождение flag'нуть в SUMMARY (план 06 может потребовать field add — пока assume v0.1 Finding достаточна).

    4) Создать `tests/agent/test_findings_hook_flusher.py`:
    - Helper для setup buffer с фейк-записями через emit_finding (план 04 task 1)
    - Mock httpx.Client через MockTransport (как в LG тестах плана 02)
    - 9 тестов из behavior

    5) НЕ запускать flusher автоматически в этом плане — он будет вызываться:
    - cron / systemd timer (отложено в admin docs, vне scope)
    - opportunistic: при следующем `ccguard sync` (если есть такой CLI command — grep по `ccguard sync` / `ccguard agent` — если есть, добавить вызов flush(); если нет — оставить как manual `python -m ccguard.agent.findings_hook.flusher_main`)

    Если изменения в server/api/findings.py трогают existing routes — добавить smoke test что old behavior сохранён (TestClient POST с single record не сломан).
  </action>
  <verify>
    <automated>pytest tests/agent/test_findings_hook_flusher.py -x</automated>
  </verify>
  <done>
    - flusher.py + flusher_main.py созданы по audit_hook pattern
    - server /api/v1/findings принимает batch
    - DLQ-style guard (retry_count >= 3 → mark delivered)
    - X-CCGuard-Token auth работает
    - 9 тестов зелёные
    - Existing audit_hook остаётся unchanged
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent buffer → flusher subprocess | local FS shared via SQLite WAL |
| flusher → server REST | authenticated via X-CCGuard-Token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | I (Information Disclosure) | matched_pattern содержит секреты | mitigate | mask_secrets() ДО persist в буфер |
| T-05-04-02 | I (Information Disclosure) | matched_pattern слишком длинный, утечка контекста | mitigate | Truncate до 200 chars |
| T-05-04-03 | DoS | бесконечный retry циклит ресурсы | mitigate | retry_count >= 3 → mark delivered (DLQ-style) |
| T-05-04-04 | Tampering | unauthenticated POST на /api/v1/findings | mitigate | X-CCGuard-Token header verified (existing v0.1 auth) |
| T-05-04-05 | DoS | flusher запускается одновременно дважды → SQLite contention | accept | WAL mode позволяет concurrent readers; одновременные writers редки (oneshot CLI) |
| T-05-04-06 | Tampering | подмена findings_buffer.db | accept | Local user space — admin user может всё; trust на OS-level |
| T-05-04-SC | Tampering | npm/pip/cargo installs | mitigate | НЕТ новых пакетов |
</threat_model>

<verification>
- `pytest tests/agent/test_findings_hook_buffer.py tests/agent/test_findings_hook_flusher.py -x` зелёный
- `pytest -x` baseline + новые тесты зелёные
- Manual smoke: создать запись через emit_finding в test env → запустить `python -m ccguard.agent.findings_hook.flusher_main` → запись в server Finding таблице
- Existing audit_hook/* остался unchanged: `git diff src/ccguard/agent/audit_hook/` пусто
</verification>

<success_criteria>
- findings_hook/ модуль создан как clone audit_hook (D-1)
- emit_finding hot-path < 10 ms
- mask_secrets() применён
- Batch endpoint работает, X-CCGuard-Token verified
- DLQ-style guard защищает от retry-loop
- НЕТ новых таблиц (Finding из v0.1 reused)
</success_criteria>

<output>
Create `.planning/phase-05-prompt-injection/05-04-SUMMARY.md` when done
</output>
