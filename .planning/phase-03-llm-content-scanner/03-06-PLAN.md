---
phase: 03-llm-content-scanner
plan: 06
type: execute
wave: 6
depends_on: [03-05]
files_modified:
  - tests/unit/test_llm_phase_regression.py
  - tests/integration/test_llm_phase_e2e.py
  - tests/integration/test_severity_critical_badge.py
  - tests/integration/test_agent_masking_regression.py
autonomous: true
requirements: [LLM-01, LLM-02, LLM-03, LLM-04]
must_haves:
  truths:
    - "Full e2e flow: agent collects → masks → POSTs /scan-content → server scans (mocked Anthropic) → ScanResult persisted → Finding emitted → UI renders badge in correct color"
    - "Cache hit on identical content avoids second Anthropic call (assertion on mock.call_count)"
    - "Budget exhaustion returns per-item error='budget_exhausted' in batch response without crashing batch"
    - "Severity=critical (score>70) renders bg-red-600 badge in /findings HTML"
    - "Masking regression: previously-masked v0.1 inventory paths still produce identical output after the masking module refactor"
    - "Scanner disabled path: agent calls /scanner-config → enabled=false → no /scan-content POST → no LLMCallLog rows written"
    - "Phase 1+2 test suite (443+ tests) remains green; 25+ new tests across plans 01-06 add up to ≥468 total"
  artifacts:
    - path: "tests/integration/test_llm_phase_e2e.py"
      provides: "full vertical-slice e2e test from agent payload to rendered UI"
      contains: "scanner_e2e"
    - path: "tests/integration/test_severity_critical_badge.py"
      provides: "UI rendering proof for critical severity"
      contains: "bg-red-600"
  key_links:
    - from: "tests/integration/test_llm_phase_e2e.py"
      to: "all scan_service + api + UI layers"
      via: "ASGI TestClient + mocked anthropic SDK"
      pattern: "AsyncMock"
---

<objective>
Close the test suite. This plan adds regression and end-to-end tests proving
every locked decision (D-01 critical severity, D-02 one-pass, D-03 scheduler,
D-04 threshold=30, D-05 strict tool, D-06 Haiku 4.5 pricing) is honored across
the integrated stack. Previous plans tested in isolation; this plan tests the
seams between them.

Output: 4 test modules totaling 12+ tests covering e2e flow, severity badge
rendering, masking regression, scanner-disabled path, and a final count
assertion that the phase added ≥25 net new tests.
</objective>

<execution_context>
@/Users/timderbak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/timderbak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phase-03-llm-content-scanner/03-CONTEXT.md
@.planning/phase-03-llm-content-scanner/03-UI-SPEC.md
@.planning/phase-03-llm-content-scanner/03-01-SUMMARY.md
@.planning/phase-03-llm-content-scanner/03-02-SUMMARY.md
@.planning/phase-03-llm-content-scanner/03-03-SUMMARY.md
@.planning/phase-03-llm-content-scanner/03-04-SUMMARY.md
@.planning/phase-03-llm-content-scanner/03-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: End-to-end vertical slice + severity=critical badge rendering + cache assertion</name>
  <files>tests/integration/test_llm_phase_e2e.py, tests/integration/test_severity_critical_badge.py</files>
  <behavior>
    - `test_llm_phase_e2e.py::test_scanner_e2e_happy_path`: enable scanner via SettingsRecord; mock `anthropic.AsyncAnthropic` so `messages.create` returns a tool_use ScanOutcome with risk_score=85, category="jailpath" wait — must be in enum so pick "jailbreak". Agent test helper synthesizes 2 files with crafted content, runs `collect_scannable_files` + `send_scan_batch` against the ASGI TestClient. Assert: (a) /scanner-config returned enabled=true, (b) POST /scan-content returned 200 with 2 items including risk_score=85, (c) 2 ScanResult rows in DB, (d) 2 LLMCallLog rows in DB with model="claude-haiku-4-5-20251001", (e) 2 Findings emitted with rule_id="llm.scan.jailbreak" and severity="critical", (f) GET /findings response HTML contains `bg-red-600` (critical badge) and `Пересканировать` button.
    - `test_llm_phase_e2e.py::test_cache_hit_avoids_second_call`: scan file → mock.call_count==1. Scan identical content again → mock.call_count still ==1; response item has `cached=true`.
    - `test_llm_phase_e2e.py::test_budget_exhausted_mid_batch`: set daily_call_budget=2; submit batch of 3 distinct files; assert first 2 items succeed (have risk_score), third item has `error="budget_exhausted"`; LLMCallLog count == 2.
    - `test_llm_phase_e2e.py::test_scanner_disabled_path`: SettingsRecord.llm_scanner_enabled=false; agent calls /scanner-config → enabled=false; agent skips POST; assert zero LLMCallLog rows; UI usage counter renders "Сканер выключен.".
    - `test_severity_critical_badge.py::test_critical_score_renders_red`: insert Finding with severity="critical" + details.risk_score=85 + details.category="data-exfil" + details.file_hash="abc..."; GET /findings; response contains the exact substring `bg-red-600` AND `data-exfil` AND a form with `hx-post="/admin/scan/abc.../rescan"` (truncated hash interpolated). Then verify the per-row "Пересканировать" button is present (rule_id starts with `llm.scan.`).
    - `test_severity_critical_badge.py::test_warn_score_renders_amber`: same shape but score=50, severity="warn" → `bg-amber-600` present.
    - `test_severity_critical_badge.py::test_info_score_renders_emerald`: score=15, severity="info" → `bg-emerald-600` present.
    - `test_severity_critical_badge.py::test_non_llm_row_renders_emdash`: existing anomaly finding (rule_id="anomaly.bash_calls") → row renders `—` in both new columns and no re-scan button.
  </behavior>
  <action>Create both test modules. Use the project's standard ASGI TestClient fixture (likely `tests/conftest.py` exposes `client` and `db_session`). Mock anthropic at the import boundary: `with patch("ccguard.server.services.llm_client.anthropic.AsyncAnthropic")` — verify the patch path matches the actual import in `llm_client.py`. For the e2e test, drive the agent code path by calling `agent.inventory_scan.collect_scannable_files` against a `tmp_path` containing `agents/foo.md` and `skills/bar/SKILL.md`, then `send_scan_batch(client.base_url, token, items)` — adapt to use the ASGI client directly via `httpx.Client(transport=httpx.ASGITransport(app=app))`. For the budget test, seed LLMCallLog with 0 rows and set budget=2 in SettingsRecord directly; for the cache test, scan the same content twice and assert mock.call_count via the AsyncMock returned by the patch. For badge tests, write Findings directly into the DB (no need to drive the full pipeline) and GET /findings; use `assert b"bg-red-600" in response.content` etc.</action>
  <verify>
    <automated>pytest tests/integration/test_llm_phase_e2e.py tests/integration/test_severity_critical_badge.py -x -q</automated>
  </verify>
  <done>4 e2e tests + 4 badge tests = 8 tests pass; full vertical slice exercised with Anthropic mocked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Agent masking regression + phase-wide regression count</name>
  <files>tests/integration/test_agent_masking_regression.py, tests/unit/test_llm_phase_regression.py</files>
  <behavior>
    - `test_agent_masking_regression.py::test_v01_inventory_masking_unchanged`: read a fixture file (or synthesize an MCP-args dict) with embedded JWT + sk-ant + AKIA + ghp tokens. Run the v0.1 inventory MCP-args masking path (the existing function that previously lived in `inventory.py` and now imports from `agent/masking.py`). Assert output exactly equals the byte-for-byte expected masked output (locked in test as a string constant) — proving the Plan 04 refactor introduced no behavior drift.
    - `test_agent_masking_regression.py::test_masking_idempotent_across_layers`: applying mask_secrets to already-masked text leaves it unchanged.
    - `test_agent_masking_regression.py::test_content_scan_masks_before_send`: synthesize file content containing all 6 secret types from Plan 04; run `collect_scannable_files` over a tmp dir holding that file; base64-decode the returned item content; assert NONE of the raw secret bytes remain (use `assert b"sk-ant-" not in decoded` for each pattern).
    - `test_llm_phase_regression.py::test_phase_3_test_count_baseline`: a count test that confirms `pytest --collect-only -q` reports at least 468 tests total (Phase 1+2 baseline 443 + Phase 3 minimum 25). This is a tripwire — if a future refactor accidentally deletes tests, this trips. Implement via subprocess call to pytest collect, parse the trailing summary line, assert count ≥ 468. Skip on Windows if subprocess pytest invocation is flaky in CI.
    - `test_llm_phase_regression.py::test_severity_critical_round_trip`: end-to-end through schema validation, DB write, DB read, API serialization. Confirms D-01 stays additive: severity="block" still works (Phase 1+2 contract preserved) AND severity="critical" works (Phase 3 addition).
    - `test_llm_phase_regression.py::test_haiku_pricing_constant_locked`: imports the cost constants from `llm_client.py` and asserts `INPUT_CENTS_PER_MTOK == 100` and `OUTPUT_CENTS_PER_MTOK == 500` — tripwire against accidentally reverting to Haiku 3.5 pricing per D-06.
  </behavior>
  <action>Create both test modules. For the v0.1 masking regression, find one existing Phase 1+2 test that exercises MCP-args masking (grep `tests/` for "mask" or "REDACTED"); copy its expected-output assertion shape and re-run it through the refactored `agent/masking.py` import path. The test_phase_3_test_count_baseline test uses `subprocess.run(["pytest", "--collect-only", "-q"], capture_output=True, text=True)` and parses `result.stdout` for the `N tests collected` line. test_haiku_pricing_constant_locked is a literal `from ccguard.server.services.llm_client import INPUT_CENTS_PER_MTOK, OUTPUT_CENTS_PER_MTOK; assert ...`.</action>
  <verify>
    <automated>pytest tests/integration/test_agent_masking_regression.py tests/unit/test_llm_phase_regression.py -x -q && pytest -q --tb=no | tail -1</automated>
  </verify>
  <done>6 additional tests pass; total test count ≥ 468; phase 1+2 suite stays green; cost constants and severity round-trip locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds tests only; no new code paths or trust boundaries. Reuses
boundaries declared in plans 01-05.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-17 | Repudiation | regression coverage | mitigate | tripwire on test count + locked-constant assertion + Russian-copy verbatim assertion in plan 05 tests collectively prevent silent scope reduction |
</threat_model>

<verification>
- `pytest -q` — full suite green, ≥468 tests
- `pytest tests/integration/test_llm_phase_e2e.py -v` — every e2e flow named
- Cost constants test fails if someone reverts to Haiku 3.5 rates
</verification>

<success_criteria>
- 14+ new tests added in this plan (8 e2e/badge + 6 regression)
- Phase total: ≥25 new tests added across plans 01-06
- Total suite ≥468 tests; all green
- Every locked decision (D-01..D-06) has at least one assertion test
- Masking refactor proven behavior-preserving via byte-for-byte assertion
</success_criteria>

<output>
Create `.planning/phases/03-llm-content-scanner/03-06-SUMMARY.md` when done. Include: test inventory by plan, total test count before/after phase, locked-decision → test-name mapping table.
</output>
