---
phase: 03-llm-content-scanner
plan: 05
type: execute
wave: 5
depends_on: [03-04]
files_modified:
  - src/ccguard/server/web/routes.py
  - src/ccguard/server/web/templates/findings_feed.html
  - src/ccguard/server/web/templates/settings.html
  - src/ccguard/server/web/templates/components/_finding_row.html
  - src/ccguard/server/web/templates/components/_risk_badge.html
  - src/ccguard/server/web/templates/components/_llm_usage_counter.html
  - src/ccguard/server/scheduler.py
  - tests/integration/test_llm_admin_routes.py
  - tests/integration/test_findings_ui_extension.py
autonomous: true
requirements: [LLM-03, LLM-04]
must_haves:
  truths:
    - "/findings table gains 'Риск' column (after 'Серьёзность') and 'Действия' column (rightmost) with badge and per-row re-scan button per UI-SPEC"
    - "/findings filter bar gains <select name='scope'> with options all|llm|non_llm; URL contract additive"
    - "/settings page gains 'LLM-сканер' section with toggle + budget input + usage counter (HTMX polled every 30s) + last 10 scans list + 'Пересканировать всё' button — Russian copy verbatim per UI-SPEC"
    - "POST /admin/llm-settings persists Settings.llm_scanner_enabled + daily_call_budget (validates 0..10000); 303 redirect"
    - "POST /admin/scan/{file_hash}/rescan invalidates cache and returns single <tr> partial (_finding_row.html) for HTMX outerHTML swap; budget-exhausted path returns same row with inline 'бюджет исчерпан' notice"
    - "POST /admin/scan/rescan-all enqueues APScheduler one-shot job that expires all ScanResult.ttl rows and lets next agent inventory cycle repopulate (per D-03)"
    - "GET /_partials/settings/llm-usage returns _llm_usage_counter.html with used/budget/cost from scan_service.get_daily_usage()"
  artifacts:
    - path: "src/ccguard/server/web/templates/components/_risk_badge.html"
      provides: "reusable risk score pill (emerald/amber/red by 0-29/30-70/71-100)"
      contains: "bg-emerald-600"
    - path: "src/ccguard/server/web/templates/components/_finding_row.html"
      provides: "single <tr> finding partial for HTMX swap"
      contains: "hx-swap=\"outerHTML\""
    - path: "src/ccguard/server/web/routes.py"
      provides: "5 new routes per UI-SPEC"
      contains: "rescan"
  key_links:
    - from: "_finding_row.html re-scan form"
      to: "POST /admin/scan/{file_hash}/rescan"
      via: "hx-post + hx-target='closest tr' + hx-swap='outerHTML'"
      pattern: "hx-post"
    - from: "settings.html usage strip"
      to: "GET /_partials/settings/llm-usage"
      via: "hx-trigger='every 30s'"
      pattern: "every 30s"
    - from: "POST /admin/scan/rescan-all"
      to: "APScheduler one-shot job"
      via: "scheduler.add_job(rescan_all_files, run_date=now)"
      pattern: "add_job"
---

<objective>
All UI wiring per the locked UI-SPEC (Russian copy verbatim, Tailwind classes
verbatim, no new JS beyond HTMX + one inline `onsubmit confirm()` already
sanctioned by UI-SPEC). Plus the APScheduler hook for global re-scan-all
(per D-03 locked: extend Phase 2's existing APScheduler, no new infra).

Output: extended findings_feed.html, settings.html, three new component
partials, five admin routes, scheduler hook, and tests for admin routes +
findings UI extension.
</objective>

<execution_context>
@/Users/timderbak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/timderbak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phase-03-llm-content-scanner/03-CONTEXT.md
@.planning/phase-03-llm-content-scanner/03-UI-SPEC.md
@.planning/phase-03-llm-content-scanner/03-04-SUMMARY.md
@src/ccguard/server/web/routes.py
@src/ccguard/server/web/templates/findings_feed.html
@src/ccguard/server/web/templates/settings.html
@src/ccguard/server/web/csrf.py
@src/ccguard/server/scheduler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Templates — risk badge, finding row partial, findings_feed extension, usage counter partial, settings LLM section</name>
  <files>src/ccguard/server/web/templates/components/_risk_badge.html, src/ccguard/server/web/templates/components/_finding_row.html, src/ccguard/server/web/templates/components/_llm_usage_counter.html, src/ccguard/server/web/templates/findings_feed.html, src/ccguard/server/web/templates/settings.html</files>
  <action>Create `components/_risk_badge.html` exactly as specified in UI-SPEC "Risk badge spec" block: 0-29 emerald-600, 30-70 amber-600, 71-100 red-600, null → em-dash. Create `components/_finding_row.html` containing one `<tr>` rendering all 7 columns per UI-SPEC "Findings table — extended column layout" (existing 5 columns preserved verbatim from the current `findings_feed.html` body — read it and copy the cell markup; insert the new `Риск` cell using `{% include "components/_risk_badge.html" %}` after the severity cell; append the `Действия` cell with the per-row re-scan form OR an em-dash for non-LLM rows per UI-SPEC selector logic: button only if `finding.rule_id.startswith('llm.scan.') and finding.details.file_hash`). The per-row form uses the exact HTMX attributes from UI-SPEC: `hx-post="/admin/scan/{{ finding.details.file_hash }}/rescan"`, `hx-target="closest tr"`, `hx-swap="outerHTML"`, `hx-confirm="Пересканировать этот файл? Списание из дневного бюджета."`. Create `components/_llm_usage_counter.html` exactly as the UI-SPEC "Usage counter partial" block (Russian: "Сканер выключен." vs "Использовано: X/Y calls сегодня • $Z.ZZ"). Edit `findings_feed.html`: (a) insert the scope `<select>` per UI-SPEC "/findings filter bar — additive change" (with `Тип` sr-only label + 3 options), (b) update the `<thead>` to add `Риск` after `Серьёзность` and `Действия` as the last column (preserve all existing headers byte-for-byte), (c) replace the existing per-row body markup with `{% include "components/_finding_row.html" %}`, (d) update any `colspan` on the empty state row to account for the 2 new columns. Edit `settings.html`: append the entire `LLM-сканер` `<section>` block from UI-SPEC verbatim at the bottom of the main content area, including: API-key-missing notice (conditional on `has_api_key`), combined toggle+budget form, usage counter `<div hx-get hx-trigger>`, "Последние 10 сканов" `<ul>`, "Пересканировать всё" form with inline `onsubmit confirm`. Use Russian copy verbatim per UI-SPEC "Copywriting Contract" section. Do NOT introduce any new Tailwind classes beyond what UI-SPEC enumerates. The route handlers (next task) supply the template variables: `has_api_key`, `settings.llm_scanner_enabled`, `settings.daily_call_budget`, `scans` (list of last 10 ScanResult), `enabled`, `used`, `budget`, `cost_dollars`.</action>
  <verify>
    <automated>python -c "from jinja2 import Environment, FileSystemLoader; e=Environment(loader=FileSystemLoader('src/ccguard/server/web/templates')); [e.get_template(t) for t in ['findings_feed.html','settings.html','components/_finding_row.html','components/_risk_badge.html','components/_llm_usage_counter.html']]; print('ok')"</automated>
  </verify>
  <done>All 5 template files parse; Russian copy matches UI-SPEC verbatim; only existing Tailwind tokens used.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Admin routes (settings save, per-row rescan, rescan-all, usage partial, /findings scope filter) + scheduler hook</name>
  <files>src/ccguard/server/web/routes.py, src/ccguard/server/scheduler.py, tests/integration/test_llm_admin_routes.py, tests/integration/test_findings_ui_extension.py</files>
  <behavior>
    - `POST /admin/llm-settings`: cookie admin auth + CSRF (existing pattern). Form fields: `enabled` (checkbox, present=true), `daily_call_budget` (int). Validate 0 ≤ budget ≤ 10000; on invalid → re-render `/settings` with the locked validation message "Бюджет должен быть целым числом от 0 до 10000." atop the LLM section (return template with `validation_error` var). On success → upsert SettingsRecord keys → 303 to /settings.
    - `POST /admin/scan/{file_hash}/rescan`: cookie admin auth + CSRF. Calls `scan_service.rescan_file(file_hash)` which expires the cache. Then immediately attempts a fresh scan IF the agent's content is still available — but per Plan 03 architecture, server doesn't store content. Resolution: the endpoint expires cache and returns the *existing* ScanResult row rendered through `_finding_row.html` (with a notice if budget exhausted or scanner disabled). Next agent inventory cycle will trigger the real re-scan. Inline notice contract per UI-SPEC: budget exhausted → render row + `<span class="text-xs text-amber-600 ml-2">бюджет исчерпан</span>`; disabled → same with `сканер выключен`. Returns single `<tr>` HTML for HTMX outerHTML swap.
    - `POST /admin/scan/rescan-all`: cookie admin auth + CSRF. Enqueues APScheduler one-shot job `rescan_all_files` via the existing scheduler instance (D-03). The job runs in-process and sets `ttl_expires_at = utcnow - 1s` on every ScanResult row in a single UPDATE statement. 303 to /settings immediately.
    - `GET /_partials/settings/llm-usage`: cookie admin auth (no CSRF — GET). Calls `scan_service.get_daily_usage()` → renders `_llm_usage_counter.html` with `enabled`, `used`, `budget`, `cost_dollars = cost_cents / 100`.
    - `GET /findings`: existing route, extend to read new `scope` query param. When `scope=llm` → add `AND rule_id LIKE 'llm.scan.%'`; `scope=non_llm` → `AND rule_id NOT LIKE 'llm.scan.%'`; `all` or missing → no extra filter. Compose with existing `rule_id` fragment filter via AND. Pass `scope` to template for `selected` rendering. Pass per-row `finding.details.risk_score` and `finding.details.category` and `finding.details.file_hash` already populated by Plan 03 emit code.
    - Scheduler hook in `src/ccguard/server/scheduler.py`: add `rescan_all_files(session_factory)` function that does `UPDATE scan_result SET ttl_expires_at = :expired`. Add a helper `enqueue_rescan_all()` that gets the existing APScheduler instance (grep for how Phase 2 baseline jobs are registered) and adds a `DateTrigger(run_date=datetime.now(timezone.utc))` job.
  </behavior>
  <action>Edit `src/ccguard/server/web/routes.py` to add the five route handlers above. Reuse the existing admin-cookie auth dependency (grep `def get_current_admin` or similar in existing routes.py) and existing CSRF middleware. For the /findings scope filter, locate the existing handler and extend its SQL WHERE building. Pass `has_api_key = bool(settings.anthropic_api_key)` to the settings template. In `src/ccguard/server/scheduler.py` add `rescan_all_files` and `enqueue_rescan_all`. Write `tests/integration/test_llm_admin_routes.py`: (a) POST /admin/llm-settings without admin cookie → 401/403, (b) with cookie + valid form → 303 + SettingsRecord updated, (c) with budget=99999 → re-renders /settings + Russian validation message present in body, (d) POST /admin/scan/{hash}/rescan → returns single `<tr>` with HTMX-swap-friendly markup (assert `<tr` is the first tag, `<table>` absent), (e) rescan when budget exhausted → response contains "бюджет исчерпан", (f) rescan when scanner disabled → response contains "сканер выключен", (g) POST /admin/scan/rescan-all → 303 + every ScanResult.ttl_expires_at < utcnow, (h) GET /_partials/settings/llm-usage when enabled → response contains "Использовано:" and a dollar sign; when disabled → contains "Сканер выключен.". Write `tests/integration/test_findings_ui_extension.py`: (i) GET /findings → response contains `Риск` and `Действия` column headers, (j) GET /findings?scope=llm → only rule_id matching `llm.scan.%` rendered, (k) GET /findings?scope=non_llm → no `llm.scan.*` rows, (l) row with risk_score=85 includes `bg-red-600` class in body; risk_score=15 includes `bg-emerald-600`; non-LLM row shows em-dash in the new cells, (m) per-row form HTML includes `hx-confirm="Пересканировать этот файл? Списание из дневного бюджета."` exactly.</action>
  <verify>
    <automated>pytest tests/integration/test_llm_admin_routes.py tests/integration/test_findings_ui_extension.py -x -q</automated>
  </verify>
  <done>5 routes implemented per UI-SPEC; scheduler one-shot job expires all caches; 13+ integration tests pass; Russian copy matches UI-SPEC byte-for-byte (verified via test (m) and the validation message test).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → /admin/* | cookie auth + CSRF; existing pattern |
| HTMX poll → /_partials/settings/llm-usage | cookie auth; GET-safe; returns no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Tampering | POST /admin/llm-settings | mitigate | existing cookie auth + CSRF; server-side numeric validation 0..10000 |
| T-03-15 | DoS | POST /admin/scan/rescan-all | mitigate | sets ttl flag only; actual LLM calls still gated by daily budget — no way to exceed budget via this button |
| T-03-16 | Tampering | per-row rescan endpoint | mitigate | file_hash is path-validated as hex; nonexistent hash → 404; budget/disabled errors rendered as inline notices, not unhandled exceptions |
</threat_model>

<verification>
- `pytest tests/integration/test_llm_admin_routes.py tests/integration/test_findings_ui_extension.py -x -q`
- Manual: visit /settings, see "LLM-сканер" section; visit /findings, see "Риск" + "Действия" columns
- Russian copy spot-check: `grep -F "Пересканировать этот файл? Списание из дневного бюджета." src/ccguard/server/web/templates/components/_finding_row.html` returns 1 line
</verification>

<success_criteria>
- 5 new routes wired and tested
- 3 new template partials + 2 extended templates
- APScheduler one-shot rescan-all job works
- Russian copy matches UI-SPEC verbatim
- 13+ new integration tests pass
- Phase 1+2 suite stays green
</success_criteria>

<output>
Create `.planning/phases/03-llm-content-scanner/03-05-SUMMARY.md` when done. Include: route table, template inventory, scheduler hook signature, test counts.
</output>
